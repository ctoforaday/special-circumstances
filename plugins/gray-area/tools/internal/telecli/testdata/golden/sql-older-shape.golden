exit: 1
--- stdout
--- stderr
telepathy: catalogue: <STORE> is stamped 1; this binary reads shape 5. The next session start or turn end (gray-area's `SessionStart`, `Stop` and `SessionEnd` hooks) rebuilds it, or run `telepathy backfill`.
