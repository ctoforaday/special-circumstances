Final assembly for topic "the seat prompt contract", run directory research/2026-01-01_golden. Debate outcome: VERIFIED after 2 round(s). Assemble research/2026-01-01_golden/report.md by UNION per the report template (references/report_template.md): verdict stamp, TL;DR, the Catechism COPIED VERBATIM from blue/report.md's audited "## The Catechism" section (UNION-COPY, NEVER AUTHORSHIP — the run-5 post-capture audit found an assembly-authored catechism DEFECTIVE with 6/7 answers regressing to pre-repair phrasings; if blue's section is missing, say so in one line and DO NOT write one yourself), technical foundations, analysis, graded risk matrix (including risk_accepted items with rationale), then blue/report.md IN FULL, red's board IN FULL (research/2026-01-01_golden/red/ledger.md then research/2026-01-01_golden/red/archive.md), per-round debate synopsis pointing at debate.md, an "Open questions carried past this run" section from blue's final envelope: [], and the consolidated footnotes. Never compress the research into a digest.  Collated friction so far (report any of your own as well): []. FRICTION: if you report any friction, ALSO append each entry to research/2026-01-01_golden/friction.md as one attributed line ("assemble: ...") in addition to the envelope's friction field, so the complaint survives even if a later phase aborts. SPEED: every message you send costs a ~20s round-trip regardless of content — batch INDEPENDENT tool calls into a single message (read several files at once; fire independent fetches together); only serialize calls that truly depend on a prior result. Peek and search files with the native Read (offset/limit), Grep, and Glob tools — NEVER sed/awk/head/tail/cat/grep through Bash for file access (a shell spawn costs 10-100x a native read and buys nothing). KNOWN HARNESS LIMIT (W1.11, on file three times — do NOT re-log it as friction): Glob/Grep may refuse paths outside the session's registered working directories ("Path does not exist") while Read and Bash reach them; for searches under the run directory the SANCTIONED fallback is Bash grep/ls — this is the one exception to the no-shell-file-access rule. Return a 5-line synopsis of the final report, plus your own friction if any.