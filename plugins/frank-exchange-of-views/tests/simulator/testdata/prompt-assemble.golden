prompt sha256: 73cb02cd29b537a89bb0a4de293e0adc77be2095b3136d43392e9bcc169bc3da

Final assembly for topic "the seat prompt contract", run directory research/2026-01-01_golden.
Debate outcome: VERIFIED after 2 round(s).
Assemble research/2026-01-01_golden/report.md by UNION per the report template (references/report_template.md): verdict stamp, TL;DR, the Catechism COPIED VERBATIM from blue/report.md's audited "## The Catechism" section (UNION-COPY, NEVER AUTHORSHIP — the run-5 post-capture audit found an assembly-authored catechism DEFECTIVE with 6/7 answers regressing to pre-repair phrasings; if blue's section is missing, say so in one line and DO NOT write one yourself), technical foundations, analysis, graded risk matrix (including risk_accepted items with rationale), then blue/report.md IN FULL, red's board IN FULL (pull each FRESH through the tool and copy verbatim: feov-record bench show --run research/2026-01-01_golden --view ledger then feov-record bench show --run research/2026-01-01_golden --view archive), per-round debate synopsis pointing at debate.md, the "## Lines of Inquiry" section copied from research/2026-01-01_golden/records/render-shadow/lines-of-inquiry.md if it exists (the exploration space — what was pursued, abandoned and declined; a report that shows only its conclusions hides the roads not taken, and the abandoned ones are what a future run needs most), an "Open questions carried past this run" section from blue's final envelope: [], and the consolidated footnotes.
Never compress the research into a digest.
REPORT WRITE PATH: research/2026-01-01_golden/report.md hits a filename-keyed Write-guard — assemble into your scratchpad under a NEUTRAL name and copy it to research/2026-01-01_golden/report.md with bash cp; a direct Write of the report path fails and wastes a round-trip.
Collated friction so far (report any of your own as well): [].
FRICTION (assemble): report any friction in the envelope's friction field AND through the tool's friction verb — the verb is the durable record that survives a later phase abort.
Do not hand-write a friction.md; the verb IS the record (the pre-tool dual-write was retired 2026-07-19).
SPEED: every message you send costs a ~20s round-trip regardless of content — batch INDEPENDENT tool calls into a single message (read several files at once; fire independent fetches together); only serialize calls that truly depend on a prior result.
Peek and search files with the native Read (offset/limit), Grep, and Glob tools — NEVER sed/awk/head/tail/cat/grep through Bash for file access (a shell spawn costs 10-100x a native read and buys nothing).
KNOWN HARNESS LIMIT (W1.11, on file three times — do NOT re-log it as friction): Glob/Grep may refuse paths outside the session's registered working directories ("Path does not exist") while Read and Bash reach them; for searches under the run directory the SANCTIONED fallback is Bash grep/ls — this is the one exception to the no-shell-file-access rule.
Return a 5-line synopsis of the final report, plus your own friction if any.
