$ register --run {RUN} --seat-id judge-r2
exit 0
stdout:
registered judge-r2 (shard nonce NONCE001)

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES, AND THE SECOND IS THE EXPENSIVE ONE:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  DO NOT REGISTER AGAIN. If a later call says "this agent has not registered", it is THIS, not a lost registration — re-registering rotates your shard nonce and replay then discards everything the first sitting recorded.

$ opinion --run {RUN} --seat-id judge-r2 --id R2-1 --as repaired
exit 2
stderr:
feov-record: required flag(s) "principle", "review-flag", "settled", "tension" not set

dispose of one contested gap with a written ruling — the bench's answer to a docketed item

A ruling is an OPINION, not a disposition — the principle applied and the values in tension are required because a bare fate teaches the next round nothing.

Every value ends the gap except `carried`, which defers it with a stated direction.

For a holding that binds how the record is READ and moves no gap, use `declare`: this verb needs an id and a fate, and cannot carry one.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record opinion [flags]

Flags:
      --as carried           REQUIRED — your ruling AND the gap's fate. Every value ends the gap except carried, which defers it to a later round with a stated direction. One vocabulary with red's closure classes since #342
      --final                nothing would reopen this: settled on the merits, and more evidence cannot change it. The assertable form of an empty --reopens-on, so a decided question is distinguishable from a skipped field
  -h, --help                 help for opinion
      --id gap-id            REQUIRED — the gap being ruled on
      --principle string     REQUIRED — the principle applied — a ruling is an OPINION, not a disposition
      --reason string        REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -        read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --reopens-on string    what would change this outcome — the evidence or condition that would make it worth raising again. If nothing would, pass --final instead: that is a different answer, not a missing one
      --review-flag string   REQUIRED — why a human should, or should not, look at this
      --settled string       REQUIRED — THE PROPOSITION NOW BARRED, as one sentence — not the gap id, and not the disposition. A finding is a claim, its evidence and its demand; a fate says which of the three fell only to whoever wrote it. Say what the losing party may no longer assert
      --tension string       REQUIRED — the values in tension (e.g. correctness vs economy)

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES --superseded-by naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES --supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    carried                   NOT a closure: the gap survives to the next round with a stated research direction the coming seat owes

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ opinion --run {RUN} --seat-id judge-r2 --id R2-1 --as repaired --principle p --tension t
exit 2
stderr:
feov-record: required flag(s) "review-flag", "settled" not set

dispose of one contested gap with a written ruling — the bench's answer to a docketed item

A ruling is an OPINION, not a disposition — the principle applied and the values in tension are required because a bare fate teaches the next round nothing.

Every value ends the gap except `carried`, which defers it with a stated direction.

For a holding that binds how the record is READ and moves no gap, use `declare`: this verb needs an id and a fate, and cannot carry one.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record opinion [flags]

Flags:
      --as carried           REQUIRED — your ruling AND the gap's fate. Every value ends the gap except carried, which defers it to a later round with a stated direction. One vocabulary with red's closure classes since #342
      --final                nothing would reopen this: settled on the merits, and more evidence cannot change it. The assertable form of an empty --reopens-on, so a decided question is distinguishable from a skipped field
  -h, --help                 help for opinion
      --id gap-id            REQUIRED — the gap being ruled on
      --principle string     REQUIRED — the principle applied — a ruling is an OPINION, not a disposition
      --reason string        REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -        read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --reopens-on string    what would change this outcome — the evidence or condition that would make it worth raising again. If nothing would, pass --final instead: that is a different answer, not a missing one
      --review-flag string   REQUIRED — why a human should, or should not, look at this
      --settled string       REQUIRED — THE PROPOSITION NOW BARRED, as one sentence — not the gap id, and not the disposition. A finding is a claim, its evidence and its demand; a fate says which of the three fell only to whoever wrote it. Say what the losing party may no longer assert
      --tension string       REQUIRED — the values in tension (e.g. correctness vs economy)

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES --superseded-by naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES --supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    carried                   NOT a closure: the gap survives to the next round with a stated research direction the coming seat owes

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

═══ EVENTS ═══
-- events-judge-r2-NONCE001.jsonl
{"key":"judge-r2:register:NONCE001","nonce":"NONCE001","payload":{"tool_version":"0.72.0"},"role":"bench","round":2,"seatId":"judge-r2","seq":0,"ts":0,"type":"register"}
