$ register --run {RUN} --seat-id red-chair
exit 0
stdout:
registered red-chair

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ register --run {RUN} --seat-id red-lens-evidence
exit 0
stdout:
registered red-lens-evidence

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ position --run {RUN} --seat-id red-chair --reason red's round position
exit 0
stdout:
position recorded

$ mint --run {RUN} --seat-id red-lens-evidence --class scope-creep --check-kind document --check x --severity medium --likelihood medium --impact medium --problem docketed
exit 0
stdout:
minted G1
  acceptance check RECORDED as: x
  (read it back — a shell may have rewritten it before this tool saw it, and no verb amends a check after mint)

$ closing --run {RUN} --seat-id red-chair --id G1 --reason red's closing
exit 0
stdout:
closing filed for G1

$ position --run {RUN} --seat-id blue-respond --reason blue's round position
exit 0
stdout:
position recorded

$ closing --run {RUN} --seat-id blue-respond --id G1 --reason blue's closing
exit 0
stdout:
closing filed for G1

$ revision --run {RUN} --seat-id blue-respond --reason repairs landed
exit 0
stdout:
revision recorded — the round is on the record

$ manifest-row --run {RUN} --seat-id blue-respond --id G1 --reason figures recomputed; check run: pass
exit 0
stdout:
manifest row recorded for G1

$ motion grade file --run {RUN} --seat-id blue-respond --id G1 --dimension likelihood --proposed low --reason the harm needs two failures
exit 0
stdout:
motion M1 filed (grade)

$ verify --run {RUN} --seat-id red-lens-evidence --quote throughput doubled --title https://example.invalid/paper --trust high --access-date 2026-07-18
exit 2
stderr:
feov-record: unknown flag: --title

judge a citation blue authored, when the anchor exists and you have read what it points at

THIS VERB JUDGES A CITATION THAT EXISTS. A claim carrying NO citation at all is not verified as `absent` — `absent` means you read blue's source and the claim is not in it.

An unevidenced claim is not this verb's, and it is not automatically a finding either: if a source exists and you can reach it, `corroborate` is how you go and get it, and it answers whether the claim is true in the WORLD. `finding` is for when there is nothing to fetch, and answers whether the TEXT stands up.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

FREE TEXT AND THE SHELL. Bash RUNS a backtick inside double quotes before this tool sees
your text, and records whatever the command printed — or nothing — in its place. Pass every
free-text value by capturing it first with a QUOTED heredoc, then give the flag the variable:

  X=$(cat <<'EOF'
  your text — backticks, apostrophes and $ signs are all literal here
  EOF
  )
  … "$X"

Usage:
  feov-record verify [flags]

Flags:
      --access-date YYYY-MM-DD       YYYY-MM-DD you actually read it; drives the staleness re-fetch trigger
      --anchor <!--cite:c-…-->     the c-<hex> of the citation you checked, from the report's <!--cite:c-…--> token — resolve it with `lens show evidence`
      --as as-value                  REQUIRED — what the source ACTUALLY DID for the claim. It has a negative half: refutes and absent are findings, not failures to grade
      --confidence high|medium|low   REQUIRED — how sure you are of THAT determination, whichever it was. A separate question from --as: a refutation you would defend and one you are unsure of are different facts
  -h, --help                         help for verify
      --quote string                 REQUIRED — the EXACT text from blue/report.md, quoted verbatim and NOTHING else — no section heading, no dash, no pipe: this whole string is matched against the report, so anything you prepend makes it match nothing. Name the section in --reason, where prose belongs (the claim you are checking)
      --reason string                REQUIRED — your THINKING for this act, never your process — why you graded, closed, ruled or edited as you did, which is the substance the other side answers. The ledger already holds WHAT you did, in order, so an account of the verbs you ran narrates what the record reconstructs. Pass it as "$X" after X=$(cat <<'EOF' … EOF): bash RUNS a backtick inside double quotes and records its output instead of your words

Enumerated values:
  --as
    supports              you read the source at the leaf and it says what the claim says
    supports_with_bridge  it supports the claim but you had to bridge something — a summary, a secondary citation, a near-restatement
    weak                  it gestures at the claim, or is itself uncorroborated: thin support, not none
    refutes               you read the source and it CONTRADICTS the claim — the strongest finding this verb can carry, and until 0.60.0 it had no field at all
    absent                you read the source and the claim is simply not in it. Distinct from `refutes`: silence is not contradiction, and a reader deciding what to do about it needs to know which it was
    unreachable           you could not read it — paywall, dead link, a format you could not extract. Say what you tried in --reason; an untried "unable to corroborate" is an incomplete audit

  --confidence
    high    you read the source at the leaf and would defend this determination as it stands
    medium  you are reasonably sure, but the reading bridges something — a summary, a secondary source, a near-restatement rather than the exact statement
    low     your reading may be wrong: an ambiguous passage, thin evidence, or a source you could only partly read. This is a call for more evidence, NOT an automatic fail — blue digs further

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ register --run {RUN} --seat-id judge
exit 0
stdout:
registered judge

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ motion docket file --run {RUN} --seat-id red-chair --id G1 --reason contested, and not red's to close
exit 0
stdout:
motion M2 filed (docket)

$ motion docket rule --run {RUN} --seat-id judge --id M2 --as carried --principle correctness over economy --tension thoroughness vs cost --review-flag the figure was never recomputed --settled the proposition this ruling bars --final --reason the rationale body
exit 0
stdout:
motion M2 ruled carried

$ certify --run {RUN} --seat-id judge --reason what a human should re-examine
exit 0
stdout:
certification recorded

═══ EVENTS ═══
{"key":"blue-synthesize:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"blue-synthesize","ts":0,"type":"EVENT_TYPE_REGISTER"}
{"base_ingest":{"text":"## S2\n\nA claim sits under S2.\n\n## S4\n\nA claim sits under S4.\n"},"key":"blue-synthesize:base_ingest:#1","seat_id":"blue-synthesize","ts":1,"type":"EVENT_TYPE_BASE_INGEST"}
{"key":"red-chair:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"red-chair","ts":2,"type":"EVENT_TYPE_REGISTER"}
{"key":"red-lens-evidence:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"red-lens-evidence","ts":3,"type":"EVENT_TYPE_REGISTER"}
{"key":"red-chair:position:#1","position":{"text":"red's round position"},"seat_id":"red-chair","ts":4,"type":"EVENT_TYPE_POSITION"}
{"key":"red-lens-evidence:mint:G1","mint":{"acceptance_check":"x","check_kind":"CHECK_KIND_DOCUMENT","class":"scope-creep","class_new":false,"fix_basis":"proposed","gap_id":"G1","impact":"GRADE_MEDIUM","likelihood":"GRADE_MEDIUM","location":"","mint_key":"","problem":"docketed","required_fix":"","severity":"GRADE_MEDIUM"},"seat_id":"red-lens-evidence","ts":5,"type":"EVENT_TYPE_MINT"}
{"closing":{"gap_id":"G1","text":"red's closing"},"key":"red-chair:closing:#1:G1","seat_id":"red-chair","ts":6,"type":"EVENT_TYPE_CLOSING"}
{"key":"blue-respond:position:#0","position":{"text":"blue's round position"},"seat_id":"blue-respond","ts":7,"type":"EVENT_TYPE_POSITION"}
{"closing":{"gap_id":"G1","text":"blue's closing"},"key":"blue-respond:closing:#0:G1","seat_id":"blue-respond","ts":8,"type":"EVENT_TYPE_CLOSING"}
{"key":"blue-respond:revision:#0","revision":{"text":"repairs landed"},"seat_id":"blue-respond","ts":9,"type":"EVENT_TYPE_REVISION"}
{"key":"blue-respond:manifest_row:#0:G1","manifest_row":{"gap_id":"G1","row":"figures recomputed; check run: pass"},"seat_id":"blue-respond","ts":10,"type":"EVENT_TYPE_MANIFEST_ROW"}
{"key":"blue-respond:motion:#1","motion":{"basis":"the harm needs two failures","grade":{"dimension":"GRADE_DIMENSION_LIKELIHOOD","gap_id":"G1","proposed":"GRADE_LOW"},"motion_id":"M1","subject":"MOTION_SUBJECT_GRADE"},"seat_id":"blue-respond","ts":11,"type":"EVENT_TYPE_MOTION"}
{"key":"judge:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"judge","ts":12,"type":"EVENT_TYPE_REGISTER"}
{"key":"red-chair:motion:#1","motion":{"basis":"contested, and not red's to close","docket":{"gap_id":"G1"},"motion_id":"M2","subject":"MOTION_SUBJECT_DOCKET"},"seat_id":"red-chair","ts":13,"type":"EVENT_TYPE_MOTION"}
{"key":"judge:motion_rule:#1","motion_rule":{"docket":{"disposition":"DISPOSITION_CARRIED","final":true,"principle":"correctness over economy","review_flag":"the figure was never recomputed","settled":"the proposition this ruling bars","tension":"thoroughness vs cost"},"motion_id":"M2","opinion":"the rationale body","subject":"MOTION_SUBJECT_DOCKET"},"seat_id":"judge","ts":14,"type":"EVENT_TYPE_MOTION_RULE"}
{"certify":{"statement":"what a human should re-examine"},"key":"judge:certify:#1","seat_id":"judge","ts":15,"type":"EVENT_TYPE_CERTIFY"}

═══ RENDERS ═══
-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

## Epoch 1

### RED
red's round position

### RED CLOSING (epoch 1) — G1
red's closing

### BLUE
blue's round position

### BLUE CLOSING (epoch 1) — G1
blue's closing

### LEAD
- G1: carried — principle: correctness over economy; tension: thoroughness vs cost; review: the figure was never recomputed
the rationale body

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)

_No inquiries recorded. On a run past epoch 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._


═══ REPORT ═══
# _(blue/report.md has no title — not authored here)_

**Report** · [Board](docket.md) · [Debate](debate.md) · [Judgments](judgments.md) · [Run](run.md) · [Changelog](CHANGELOG.md)

---

**Verdict:** NONE (no terminal outcome on the record)

## TL;DR

_(blue/report.md has no "## TL;DR" section — not authored here; blue owns this surface and red audits it.)_

## The Catechism

_(blue/report.md has no "## The Catechism" section — not authored here; blue owns this surface and red audits it.)_

## Technical foundations

_(blue/report.md has no "## Technical foundations" section — not authored here; blue owns this surface and red audits it.)_

## Analysis

_(blue/report.md has no "## Analysis" section — not authored here; blue owns this surface and red audits it.)_

## Risk matrix

| Risk | Likelihood | Impact | Complexity to mitigate | Mitigation / disposition |
|---|---|---|---|---|
| docketed | medium | medium | — | — |

The matrix is a scan surface: each row's full problem statement, required fix and acceptance check are in [the board](docket.md).

## Research areas

_(none on the record)_

## Future research directions

_(none on the record)_

## Alternatives considered

_(none on the record)_

## Open questions

_(blue/report.md has no "## Open questions" section — not authored here; blue owns this surface and red audits it.)_

## Additional findings

## S2

A claim sits under S2.

## S4

A claim sits under S4.
