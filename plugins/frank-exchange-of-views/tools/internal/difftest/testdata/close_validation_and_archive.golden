$ merge register --run {RUN} --seat-id red-merge-r1
exit 0
stdout:
registered red-merge-r1 (shard nonce NONCE001)

$ merge mint --run {RUN} --seat-id red-merge-r1 --class citation-drift --check refetch --severity high --likelihood high --impact high --cx medium --problem source moved
exit 0
stdout:
minted R1-1

$ merge close --run {RUN} --seat-id red-merge-r1 --id R1-1
exit 2
stderr:
feov-record: merge: record: close requires the attestation anchor (--anchor-seat --anchor-tool --anchor-target) or --carried-from <round> — an unanchored closure is unauditable (E0.5a)

$ merge close --run {RUN} --seat-id red-merge-r1 --id R9-9 --anchor-seat L1 --anchor-tool git show --anchor-target 7bc501e:x
exit 2
stderr:
feov-record: merge: record: close of unknown gap R9-9

$ merge close --run {RUN} --seat-id red-merge-r1 --id R1-1 --as closed_with_regression --anchor-seat L1 --anchor-tool git show --anchor-target 7bc501e:x
exit 2
stderr:
feov-record: merge: record: closed_with_regression requires --successor (lineage never drops)

$ merge close --run {RUN} --seat-id red-merge-r1 --id R1-1 --as closed --anchor-seat L1 --anchor-tool WebFetch --anchor-target https://example.invalid/spec#s3 --reason refetched; the source now resolves and supports the claim
exit 0
stdout:
closed R1-1 (closed)

═══ EVENTS ═══
-- events-red-merge-r1-NONCE001.jsonl
{"key":"red-merge-r1:register:NONCE001","nonce":"NONCE001","payload":{"tool_version":"0.23.0"},"round":1,"seatId":"red-merge-r1","seq":0,"ts":0,"type":"register"}
{"key":"red-merge-r1:mint:R1-1","nonce":"NONCE001","payload":{"acceptance_check":"refetch","class":"citation-drift","class_new":false,"complexity_cost":"medium","found_by":[],"gap_id":"R1-1","impact":"high","likelihood":"high","problem":"source moved","severity":"high","supersedes":[]},"round":1,"seatId":"red-merge-r1","seq":1,"ts":1,"type":"mint"}
{"key":"red-merge-r1:close:R1-1","nonce":"NONCE001","payload":{"anchor_seat":"L1","anchor_target":"https://example.invalid/spec#s3","anchor_tool":"WebFetch","closure_class":"closed","gap_id":"R1-1","prose":"refetched; the source now resolves and supports the claim"},"round":1,"seatId":"red-merge-r1","seq":2,"ts":2,"type":"close"}

═══ RENDERS ═══
-- ledger
# red/ledger.md — RENDERED PROJECTION (source of truth: records/ event log; do not hand-edit)

## OPEN GAPS (0)

## CLOSURE INDEX

R1-1 | closed | source moved | -


-- archive
# red/archive.md — RENDERED PROJECTION (append-only by construction in the event log)

## R1-1 — closed
source moved
verification anchor: L1 | WebFetch | https://example.invalid/spec#s3

-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- changelog
# blue CHANGELOG — RENDERED PROJECTION

-- citation-ledger
# red citation-ledger — RENDERED PROJECTION

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)


