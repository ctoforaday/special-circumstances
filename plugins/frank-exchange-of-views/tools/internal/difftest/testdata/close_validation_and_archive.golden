$ register --run {RUN} --seat-id red-merge-r1
exit 0
stdout:
registered red-merge-r1 (shard nonce NONCE001)

$ mint --run {RUN} --seat-id red-merge-r1 --class citation-drift --check-kind document --check refetch --severity high --likelihood high --impact high --complexity medium --problem source moved
exit 0
stdout:
minted R1-1

$ close --run {RUN} --seat-id red-merge-r1 --id R1-1
exit 2
stderr:
feov-record: required flag(s) "verified-by" not set

close a gap with the verification you did THIS round, naming what you checked it against

THE VERIFICATION TRIPLE IS THE POINT: this verb asserts the repair was checked THIS ROUND, by you — who verified it, with what, and against which exact file or ref.

For a closure the record already holds — work an earlier round did, which you are restating rather than re-attesting — use `carry`, which needs no triple because it makes no fresh claim. Re-closing carried work here double-counts the closure history.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record close [flags]

Flags:
      --as as-value               HOW the gap ended. One vocabulary with the bench's dispositions since #342 — a reader no longer has to know which verb produced a closure before it can interpret the word
  -h, --help                      help for close
      --id gap-id                 REQUIRED — the gap id
      --reason string             REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -             read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --superseded-by gap-id      the gap id carrying the unresolved remainder forward
      --verified-against string   AGAINST WHAT — the exact file, ref or URL read
      --verified-by string        WHO verified it — the seat that read the evidence
      --verified-with string      WITH WHAT — the tool or command that showed it

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES --superseded-by naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES --supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ close --run {RUN} --seat-id red-merge-r1 --id R9-9 --verified-by L1 --verified-with git show --verified-against 7bc501e:x
exit 2
stderr:
feov-record: at least one of the flags in the group [reason reason-file] is required

close a gap with the verification you did THIS round, naming what you checked it against

THE VERIFICATION TRIPLE IS THE POINT: this verb asserts the repair was checked THIS ROUND, by you — who verified it, with what, and against which exact file or ref.

For a closure the record already holds — work an earlier round did, which you are restating rather than re-attesting — use `carry`, which needs no triple because it makes no fresh claim. Re-closing carried work here double-counts the closure history.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record close [flags]

Flags:
      --as as-value               HOW the gap ended. One vocabulary with the bench's dispositions since #342 — a reader no longer has to know which verb produced a closure before it can interpret the word
  -h, --help                      help for close
      --id gap-id                 REQUIRED — the gap id
      --reason string             REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -             read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --superseded-by gap-id      the gap id carrying the unresolved remainder forward
      --verified-against string   AGAINST WHAT — the exact file, ref or URL read
      --verified-by string        WHO verified it — the seat that read the evidence
      --verified-with string      WITH WHAT — the tool or command that showed it

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES --superseded-by naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES --supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ close --run {RUN} --seat-id red-merge-r1 --id R1-1 --as repaired_with_regression --verified-by L1 --verified-with git show --verified-against 7bc501e:x
exit 2
stderr:
feov-record: at least one of the flags in the group [reason reason-file] is required

close a gap with the verification you did THIS round, naming what you checked it against

THE VERIFICATION TRIPLE IS THE POINT: this verb asserts the repair was checked THIS ROUND, by you — who verified it, with what, and against which exact file or ref.

For a closure the record already holds — work an earlier round did, which you are restating rather than re-attesting — use `carry`, which needs no triple because it makes no fresh claim. Re-closing carried work here double-counts the closure history.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record close [flags]

Flags:
      --as as-value               HOW the gap ended. One vocabulary with the bench's dispositions since #342 — a reader no longer has to know which verb produced a closure before it can interpret the word
  -h, --help                      help for close
      --id gap-id                 REQUIRED — the gap id
      --reason string             REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -             read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --superseded-by gap-id      the gap id carrying the unresolved remainder forward
      --verified-against string   AGAINST WHAT — the exact file, ref or URL read
      --verified-by string        WHO verified it — the seat that read the evidence
      --verified-with string      WITH WHAT — the tool or command that showed it

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES --superseded-by naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES --supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ close --run {RUN} --seat-id red-merge-r1 --id R1-1 --as repaired --verified-by L1 --verified-with WebFetch --verified-against https://example.invalid/spec#s3 --reason refetched; the source now resolves and supports the claim
exit 0
stdout:
closed R1-1 (repaired)

═══ EVENTS ═══
-- events-red-merge-r1-NONCE001.jsonl
{"key":"red-merge-r1:register:NONCE001","nonce":"NONCE001","payload":{"tool_version":"0.72.0"},"role":"merge","round":1,"seatId":"red-merge-r1","seq":0,"ts":0,"type":"register"}
{"key":"red-merge-r1:mint:R1-1","nonce":"NONCE001","payload":{"acceptance_check":"refetch","check_kind":"document","class":"citation-drift","class_new":false,"complexity_cost":"medium","fix_basis":"proposed","found_by":[],"gap_id":"R1-1","impact":"high","likelihood":"high","problem":"source moved","severity":"high","supersedes":[]},"role":"merge","round":1,"seatId":"red-merge-r1","seq":1,"ts":1,"type":"mint"}
{"key":"red-merge-r1:close:R1-1","nonce":"NONCE001","payload":{"anchor_seat":"L1","anchor_target":"https://example.invalid/spec#s3","anchor_tool":"WebFetch","closure_class":"repaired","gap_id":"R1-1","reason":"refetched; the source now resolves and supports the claim"},"role":"merge","round":1,"seatId":"red-merge-r1","seq":2,"ts":2,"type":"close"}
