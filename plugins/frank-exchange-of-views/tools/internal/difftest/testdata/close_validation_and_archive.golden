$ register --run {RUN} --seat-id red-chair
exit 0
stdout:
registered red-chair

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ register --run {RUN} --seat-id red-lens-evidence
exit 0
stdout:
registered red-lens-evidence

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ mint --run {RUN} --seat-id red-lens-evidence --class citation-drift --check-kind document --check refetch --severity high --likelihood high --impact high --complexity medium --problem source moved
exit 0
stdout:
minted G1
  acceptance check RECORDED as: refetch
  (read it back — a shell may have rewritten it before this tool saw it, and no verb amends a check after mint)

$ close --run {RUN} --seat-id red-lens-evidence --id G1
exit 2
stderr:
feov-record: required flag(s) "reason", "verified-by" not set

close a gap with the verification you did THIS sitting, naming what you checked it against

THE VERIFICATION TRIPLE IS THE POINT: this verb asserts the repair was checked THIS SITTING, by you — who verified it, with what, and against which exact file or ref.

For a closure the record already holds — work an earlier sitting did, which you are restating rather than re-attesting — use `carry`, which needs no triple because it makes no fresh claim. Re-closing carried work here double-counts the closure history.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

FREE TEXT AND THE SHELL. Bash RUNS a backtick inside double quotes before this tool sees
your text, and records whatever the command printed — or nothing — in its place. Pass every
free-text value by capturing it first with a QUOTED heredoc, then give the flag the variable:

  X=$(cat <<'EOF'
  your text — backticks, apostrophes and $ signs are all literal here
  EOF
  )
  … "$X"

Usage:
  feov-record close [flags]

Flags:
      --as as-value               HOW the gap ended. One vocabulary with the bench's dispositions since #342 — a reader no longer has to know which verb produced a closure before it can interpret the word
  -h, --help                      help for close
      --id gap-id                 REQUIRED — the gap id
      --reason string             REQUIRED — your THINKING for this act, never your process — why you graded, closed, ruled or edited as you did, which is the substance the other side answers. The ledger already holds WHAT you did, in order, so an account of the verbs you ran narrates what the record reconstructs. Pass it as "$X" after X=$(cat <<'EOF' … EOF): bash RUNS a backtick inside double quotes and records its output instead of your words
      --superseded-by gap-id      the gap id carrying the unresolved remainder forward
      --verified-against string   AGAINST WHAT — the exact file, ref or URL read
      --verified-by string        WHO verified it — the seat that read the evidence
      --verified-with string      WITH WHAT — the tool or command that showed it

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES a successor naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    moot                      the gap's predicate expired: the claim or artifact it attached to is no longer in the report, so there is nothing left to repair or to argue about. NOT not_a_defect, which asserts blue's argument held, and NOT repaired, which asserts a verified fix

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ close --run {RUN} --seat-id red-lens-evidence --id G2 --verified-by L1 --verified-with git show --verified-against 7bc501e:x
exit 2
stderr:
feov-record: required flag(s) "reason" not set

close a gap with the verification you did THIS sitting, naming what you checked it against

THE VERIFICATION TRIPLE IS THE POINT: this verb asserts the repair was checked THIS SITTING, by you — who verified it, with what, and against which exact file or ref.

For a closure the record already holds — work an earlier sitting did, which you are restating rather than re-attesting — use `carry`, which needs no triple because it makes no fresh claim. Re-closing carried work here double-counts the closure history.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

FREE TEXT AND THE SHELL. Bash RUNS a backtick inside double quotes before this tool sees
your text, and records whatever the command printed — or nothing — in its place. Pass every
free-text value by capturing it first with a QUOTED heredoc, then give the flag the variable:

  X=$(cat <<'EOF'
  your text — backticks, apostrophes and $ signs are all literal here
  EOF
  )
  … "$X"

Usage:
  feov-record close [flags]

Flags:
      --as as-value               HOW the gap ended. One vocabulary with the bench's dispositions since #342 — a reader no longer has to know which verb produced a closure before it can interpret the word
  -h, --help                      help for close
      --id gap-id                 REQUIRED — the gap id
      --reason string             REQUIRED — your THINKING for this act, never your process — why you graded, closed, ruled or edited as you did, which is the substance the other side answers. The ledger already holds WHAT you did, in order, so an account of the verbs you ran narrates what the record reconstructs. Pass it as "$X" after X=$(cat <<'EOF' … EOF): bash RUNS a backtick inside double quotes and records its output instead of your words
      --superseded-by gap-id      the gap id carrying the unresolved remainder forward
      --verified-against string   AGAINST WHAT — the exact file, ref or URL read
      --verified-by string        WHO verified it — the seat that read the evidence
      --verified-with string      WITH WHAT — the tool or command that showed it

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES a successor naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    moot                      the gap's predicate expired: the claim or artifact it attached to is no longer in the report, so there is nothing left to repair or to argue about. NOT not_a_defect, which asserts blue's argument held, and NOT repaired, which asserts a verified fix

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ close --run {RUN} --seat-id red-lens-evidence --id G1 --as repaired_with_regression --verified-by L1 --verified-with git show --verified-against 7bc501e:x
exit 2
stderr:
feov-record: required flag(s) "reason" not set

close a gap with the verification you did THIS sitting, naming what you checked it against

THE VERIFICATION TRIPLE IS THE POINT: this verb asserts the repair was checked THIS SITTING, by you — who verified it, with what, and against which exact file or ref.

For a closure the record already holds — work an earlier sitting did, which you are restating rather than re-attesting — use `carry`, which needs no triple because it makes no fresh claim. Re-closing carried work here double-counts the closure history.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

FREE TEXT AND THE SHELL. Bash RUNS a backtick inside double quotes before this tool sees
your text, and records whatever the command printed — or nothing — in its place. Pass every
free-text value by capturing it first with a QUOTED heredoc, then give the flag the variable:

  X=$(cat <<'EOF'
  your text — backticks, apostrophes and $ signs are all literal here
  EOF
  )
  … "$X"

Usage:
  feov-record close [flags]

Flags:
      --as as-value               HOW the gap ended. One vocabulary with the bench's dispositions since #342 — a reader no longer has to know which verb produced a closure before it can interpret the word
  -h, --help                      help for close
      --id gap-id                 REQUIRED — the gap id
      --reason string             REQUIRED — your THINKING for this act, never your process — why you graded, closed, ruled or edited as you did, which is the substance the other side answers. The ledger already holds WHAT you did, in order, so an account of the verbs you ran narrates what the record reconstructs. Pass it as "$X" after X=$(cat <<'EOF' … EOF): bash RUNS a backtick inside double quotes and records its output instead of your words
      --superseded-by gap-id      the gap id carrying the unresolved remainder forward
      --verified-against string   AGAINST WHAT — the exact file, ref or URL read
      --verified-by string        WHO verified it — the seat that read the evidence
      --verified-with string      WITH WHAT — the tool or command that showed it

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES a successor naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    moot                      the gap's predicate expired: the claim or artifact it attached to is no longer in the report, so there is nothing left to repair or to argue about. NOT not_a_defect, which asserts blue's argument held, and NOT repaired, which asserts a verified fix

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ close --run {RUN} --seat-id red-lens-evidence --id G1 --as repaired --verified-by L1 --verified-with WebFetch --verified-against https://example.invalid/spec#s3 --reason refetched; the source now resolves and supports the claim
exit 0
stdout:
closed G1 (repaired)

═══ EVENTS ═══
{"key":"blue-synthesize:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"blue-synthesize","ts":0,"type":"EVENT_TYPE_REGISTER"}
{"base_ingest":{"text":"## S2\n\nA claim sits under S2.\n\n## S4\n\nA claim sits under S4.\n"},"key":"blue-synthesize:base_ingest:#1","seat_id":"blue-synthesize","ts":1,"type":"EVENT_TYPE_BASE_INGEST"}
{"key":"red-chair:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"red-chair","ts":2,"type":"EVENT_TYPE_REGISTER"}
{"key":"red-lens-evidence:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"red-lens-evidence","ts":3,"type":"EVENT_TYPE_REGISTER"}
{"key":"red-lens-evidence:mint:G1","mint":{"acceptance_check":"refetch","check_kind":"CHECK_KIND_DOCUMENT","class":"citation-drift","class_new":false,"complexity_cost":"GRADE_MEDIUM","fix_basis":"proposed","gap_id":"G1","impact":"GRADE_HIGH","likelihood":"GRADE_HIGH","location":"","mint_key":"","problem":"source moved","required_fix":"","severity":"GRADE_HIGH"},"seat_id":"red-lens-evidence","ts":4,"type":"EVENT_TYPE_MINT"}
{"close":{"anchor_seat":"L1","anchor_target":"https://example.invalid/spec#s3","anchor_tool":"WebFetch","closure_class":"DISPOSITION_REPAIRED","gap_id":"G1","prose":"refetched; the source now resolves and supports the claim"},"key":"red-lens-evidence:close:#1:G1","seat_id":"red-lens-evidence","ts":5,"type":"EVENT_TYPE_CLOSE"}

═══ RENDERS ═══
-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)

_No inquiries recorded. On a run past epoch 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._


═══ REPORT ═══
# _(blue/report.md has no title — not authored here)_

**Report** · [Board](docket.md) · [Debate](debate.md) · [Run](run.md)

---

**Verdict:** NONE (no terminal outcome on the record)

## TL;DR

_(blue/report.md has no "## TL;DR" section — not authored here; blue owns this surface and red audits it.)_

## The Catechism

_(blue/report.md has no "## The Catechism" section — not authored here; blue owns this surface and red audits it.)_

## Technical foundations

_(blue/report.md has no "## Technical foundations" section — not authored here; blue owns this surface and red audits it.)_

## Analysis

_(blue/report.md has no "## Analysis" section — not authored here; blue owns this surface and red audits it.)_

## Risk matrix

| Risk | Likelihood | Impact | Complexity to mitigate | Mitigation / disposition |
|---|---|---|---|---|
| _(no open gaps)_ |  |  |  |  |

The matrix is a scan surface: each row's full problem statement, required fix and acceptance check are in [the board](docket.md).

## Research areas

_(none on the record)_

## Future research directions

_(none on the record)_

## Alternatives considered

_(none on the record)_

## Open questions

_(blue/report.md has no "## Open questions" section — not authored here; blue owns this surface and red audits it.)_

## Additional findings

## S2

A claim sits under S2.

## S4

A claim sits under S4.
