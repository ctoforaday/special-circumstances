$ merge mint --seat-id red-merge-r1
exit 2
stderr:
feov-record: merge: --run <runDir> is required

$ merge mint --run {RUN}
exit 2
stderr:
feov-record: merge: --seat-id is required (the engine assigns it; it is in your prompt)

$ merge not-a-verb --run {RUN} --seat-id red-merge-r1
exit 2
stderr:
feov-record: verb "not-a-verb" is outside this seat's role (available: register, mint, close, dispose, regrade, dispute-respond, spot-check, near-match, position, closing, verdict, friction, petition, show)

═══ EVENTS ═══

═══ RENDERS ═══
-- ledger
# red/ledger.md — RENDERED PROJECTION (source of truth: records/ event log; do not hand-edit)

## OPEN GAPS (0)

## CLOSURE INDEX



-- archive
# red/archive.md — RENDERED PROJECTION (append-only by construction in the event log)

-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- changelog
# blue CHANGELOG — RENDERED PROJECTION

-- citation-ledger
# red citation-ledger — RENDERED PROJECTION

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)


