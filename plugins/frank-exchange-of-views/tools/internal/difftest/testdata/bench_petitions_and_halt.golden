$ merge petition --run {RUN} --seat-id red-merge-r1 --petition-class safety --reason the design erodes a consent gate --relief halt and escalate
exit 2
stderr:
feov-record: verb "petition" is outside this seat's role (available: register, mint, close, regrade, spot-check, near-match, position, closing, verdict, friction, show)

$ bench register --run {RUN} --seat-id judge-petition
exit 0
stdout:
registered judge-petition (shard nonce NONCE001)

$ bench petition-rule --run {RUN} --seat-id judge-petition --petitioner red-merge-r1 --petition-class safety --as granted --reason the relief binds the coming seats
exit 2
stderr:
feov-record: verb "petition-rule" is outside this seat's role (available: register, opinion, halt, certify, outcome, assemble, friction, show)

$ bench halt --run {RUN} --seat-id judge-petition --reason continuing would compromise the consent gate
exit 0
stdout:
JUDICIAL HALT recorded — capture relays this verbatim

═══ EVENTS ═══
-- events-judge-petition-NONCE001.jsonl
{"key":"judge-petition:register:NONCE001","nonce":"NONCE001","payload":{"tool_version":"0.49.0"},"role":"bench","round":0,"seatId":"judge-petition","seq":0,"ts":0,"type":"register"}
{"key":"judge-petition:halt:#1","nonce":"NONCE001","payload":{"opinion":"continuing would compromise the consent gate"},"role":"bench","round":0,"seatId":"judge-petition","seq":1,"ts":1,"type":"halt"}

═══ RENDERS ═══
-- ledger
# red/ledger.md — RENDERED PROJECTION (source of truth: records/ event log; do not hand-edit)

## OPEN GAPS (0)

## CLOSURE INDEX



-- archive
# red/archive.md — RENDERED PROJECTION (append-only by construction in the event log)

-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- changelog
# blue CHANGELOG — RENDERED PROJECTION

-- citation-ledger
# red citation-ledger — RENDERED PROJECTION

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)

_No avenues recorded. On a run past round 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._

