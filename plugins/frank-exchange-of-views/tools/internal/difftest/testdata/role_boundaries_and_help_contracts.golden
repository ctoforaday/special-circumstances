$ mint --run {RUN} --seat-id red-lens-evidence --class x
exit 2
stderr:
feov-record: required flag(s) "check", "check-kind", "impact", "likelihood", "severity" not set

turn lens findings into a graded gap on the board, when a defect is real and belongs there

The id is TOOL-assigned. A stable key makes a retry idempotent rather than minting the same gap twice.

Screen first with `near-match`: a candidate overlapping a closed gap is a REOPEN, and a reopen carries its lineage rather than arriving as a fresh gap.

FOUND_BY IS CHECKED AT THE WRITE: a label naming no recorded finding is REFUSED, so a role alone (`L1`) or an invented label fails here rather than resolving to nothing.

ESTOPPEL IS ENFORCED, NOT ADVISED: a mint against text blue applied VERBATIM from your own proposal is REFUSED, and the refusal is logged as friction. Argue it on the ORIGINAL gap, where your prescription sits beside your complaint, or mint declaring that gap as its ancestor. Text blue COUNTER-EDITED is blue's authorship and you audit it normally.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record mint [flags]

Flags:
      --about string                     the reference --about-kind names: a section heading, an avenue id, or a gap id. It is CHECKED against the record, which a borrowed quote never was
      --about-kind section|inquiry|gap   anchor this gap to something that is NOT report text — use instead of --quote when the defect is an ABSENCE. The same pair the lens finding verb takes
      --check string                     REQUIRED — the acceptance check red will RUN at re-audit — the pre-agreed contract, not a description
      --check-kind computation           REQUIRED — what would SETTLE that check — read a document, RUN a computation, or verify a source. A computation check cannot be closed by prose: it closes only when a proof answers this gap
      --class slug                       REQUIRED — the gap's slug — what KIND of defect this is. A slug the registry has; coin a missing one first with the class-new verb
      --complexity grade                 what fixing it costs, on the same scale
      --fix string                       the required fix, as prose — what must become true. This is the substantive channel: research it, enumerate it, qualify it
      --found-by list                    comma-separated lens findings that surfaced it (L5-F3,L6-F2)
  -h, --help                             help for mint
      --impact grade                     REQUIRED — how bad the consequence is if it lands
      --key string                       a stable local handle of your own (C1, F2, P3 …) making a retried call idempotent — a repeat under the same handle returns the first result instead of acting twice
      --likelihood grade                 REQUIRED — how likely the CONSEQUENCE is — never how likely the defect is to BE there, which is what the grade meant before v2 split them
      --new string                       OPTIONAL concrete proposal, TEXTUAL DEFECTS ONLY: the exact text --quote should become. Bounded — a replacement more than 120 characters longer than the span is refused as AUTHORING, because a substantive addition is blue's to write and you say so in --fix. Its presence is what DERIVES fix_basis: verified
      --problem string                   REQUIRED — what is wrong (or pass it via --reason)
      --quote string                     the EXACT text from blue/report.md, quoted verbatim and NOTHING else — no section heading, no dash, no pipe: this whole string is matched against the report, so anything you prepend makes it match nothing. Name the section in --reason, where prose belongs
      --reason string                    your THINKING for this act, never your process — why you graded, closed, ruled or edited as you did, which is the substance the other side answers. The ledger already holds WHAT you did, in order, so an account of the verbs you ran narrates what the record reconstructs; --reason-file for anything long or from stdin with -
      --reason-file -                    read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --severity grade                   REQUIRED — how bad this is: low | low_medium | medium | medium_high | high | certain | realized | trivial
      --supersedes list                  comma-separated ancestor ids this gap replaces; lineage is never dropped

Enumerated values:
  --about-kind
    section  a named report section, for something MISSING from it — the anchor a quote cannot give, because the text you object to is not there
    inquiry  a line of inquiry, by its avenue id: an argument against the REASON it was declined, deferred or abandoned
    gap      a gap already on the docket, by its id — a defect in the record rather than in the report

  --check-kind
    document     reading a shipped artifact settles it — the check is answered by prose that quotes what is there
    computation  RUNNING something settles it. This check CANNOT be closed by prose: it closes only when a proof answers the gap. Reach for it wherever the answer would be PRODUCED rather than asserted — arithmetic, a simulation, a forecast, a parse, a count, a re-derivation are common cases and not the whole of it; if you can imagine a script that would end the argument, this is the kind
    source       verifying an external source settles it — the claim stands or falls on what the cited material actually says

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ mint --run {RUN} --seat-id blue-respond --class x
exit 2
stderr:
feov-record: "mint" is not on your surface — it is the lens seat's verb. That is a wrong-SEAT error, not a missing capability, so do not work around it: if the act is yours to perform, you are dispatched as the wrong seat, and if it is not, the seat that holds it is named here. The commands below are everything you can run.

feov-record — blue seats — revisions, manifest rows, directions. No board verbs at all.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record [flags]
  feov-record [command]

Available Commands:
  cite            attach a source to a sentence, when a claim needs evidence a reader can follow to the leaf
  claim-index     locate every site of each FOOTNOTED claim in your report (read-only)
  closing         your closing argument on one docketed gap, when the bench is about to rule on it
  completion      Generate the autocompletion script for the specified shell
  count-claims    count the FOOTNOTED declarative claims in blue's report (read-only)
  edit            change the report — the only path into report.md, and how every repair lands
  fetch           cached, hash-verified web read (replaces WebFetch); serves both sides the same bytes
  help            Help about any command
  ingest          freeze the synthesized report into the record and remove the file — done once, by its author
  log             an entry for the operator who can retool you — what it asserts, said in the positive
  manifest-row    your receipt for one repaired gap: what you checked, and what checking it showed
  manual          every command on your surface, each with its own help, run live — read it once, first thing in a sitting
  position        your sitting's narrative — the argument the other side answers, rendered as this sitting's BLUE section
  prove           settle a claim by RUNNING something, when computing the answer beats arguing about it
  register        your first act at the seat, and the one call that needs your seat id — nothing else runs until it does
  retire          take a claim out of the report, when it cannot stand and no edit will save it
  revision        the sitting record — what changed this sitting, once, after your edits have landed

Command groups — each holds commands THIS page does not list:
  line-of-inquiry a direction the report could take, and what became of it — `propose` one, `move` one you already proposed
  motion          file and rule on a motion — grade (the merge rules), petition (the bench rules), inquiry (the merge rules), docket (the bench rules). One mechanism, one id.
  show            read a projection of the record — the tool is the read path, and the .md files are for human verification. Bare, it answers with YOUR PENDING WORK

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

Use "feov-record [command] --help" for more information about a command.

$ close --run {RUN} --seat-id blue-respond --id G1
exit 2
stderr:
feov-record: "close" is not on your surface — it is the lens seat's verb. That is a wrong-SEAT error, not a missing capability, so do not work around it: if the act is yours to perform, you are dispatched as the wrong seat, and if it is not, the seat that holds it is named here. The commands below are everything you can run.

feov-record — blue seats — revisions, manifest rows, directions. No board verbs at all.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record [flags]
  feov-record [command]

Available Commands:
  cite            attach a source to a sentence, when a claim needs evidence a reader can follow to the leaf
  claim-index     locate every site of each FOOTNOTED claim in your report (read-only)
  closing         your closing argument on one docketed gap, when the bench is about to rule on it
  completion      Generate the autocompletion script for the specified shell
  count-claims    count the FOOTNOTED declarative claims in blue's report (read-only)
  edit            change the report — the only path into report.md, and how every repair lands
  fetch           cached, hash-verified web read (replaces WebFetch); serves both sides the same bytes
  help            Help about any command
  ingest          freeze the synthesized report into the record and remove the file — done once, by its author
  log             an entry for the operator who can retool you — what it asserts, said in the positive
  manifest-row    your receipt for one repaired gap: what you checked, and what checking it showed
  manual          every command on your surface, each with its own help, run live — read it once, first thing in a sitting
  position        your sitting's narrative — the argument the other side answers, rendered as this sitting's BLUE section
  prove           settle a claim by RUNNING something, when computing the answer beats arguing about it
  register        your first act at the seat, and the one call that needs your seat id — nothing else runs until it does
  retire          take a claim out of the report, when it cannot stand and no edit will save it
  revision        the sitting record — what changed this sitting, once, after your edits have landed

Command groups — each holds commands THIS page does not list:
  line-of-inquiry a direction the report could take, and what became of it — `propose` one, `move` one you already proposed
  motion          file and rule on a motion — grade (the merge rules), petition (the bench rules), inquiry (the merge rules), docket (the bench rules). One mechanism, one id.
  show            read a projection of the record — the tool is the read path, and the .md files are for human verification. Bare, it answers with YOUR PENDING WORK

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

Use "feov-record [command] --help" for more information about a command.

$ mint --run {RUN} --seat-id judge --class x
exit 2
stderr:
feov-record: "mint" is not on your surface — it is the lens seat's verb. That is a wrong-SEAT error, not a missing capability, so do not work around it: if the act is yours to perform, you are dispatched as the wrong seat, and if it is not, the seat that holds it is named here. The commands below are everything you can run.

feov-record — the bench — rulings, halt, certification. Never originates.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with your role's 'friction' verb — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

Usage:
  feov-record [flags]
  feov-record [command]

Available Commands:
  assemble     assemble <run>/report.md from the record — blue's audited sections lifted verbatim, the rest composed from the event log; no inputs
  certify      what a human should re-examine after the run ends — the bench keeps no memory between runs, so this is it
  completion   Generate the autocompletion script for the specified shell
  count-claims count the FOOTNOTED declarative claims in blue's report (read-only)
  declare      a holding that binds how the whole record is READ, when the dispute is over what a term MEANS and no gap moves
  fetch        cached, hash-verified web read (replaces WebFetch); serves both sides the same bytes
  halt         stop the run at a safety or consent boundary, when continuing would itself be the harm
  help         Help about any command
  log          an entry for the operator who can retool you — what it asserts, said in the positive
  manual       every command on your surface, each with its own help, run live — read it once, first thing in a sitting
  outcome      stamp how the run ENDED, as a fact — a different question from red's PASS or FAIL
  register     your first act at the seat, and the one call that needs your seat id — nothing else runs until it does

Command groups — each holds commands THIS page does not list:
  motion       file and rule on a motion — grade (the merge rules), petition (the bench rules), inquiry (the merge rules), docket (the bench rules). One mechanism, one id.
  show         read a projection of the record — the tool is the read path, and the .md files are for human verification. Bare, it answers with YOUR PENDING WORK

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

Use "feov-record [command] --help" for more information about a command.

$ lens help
exit 2
stderr:
feov-record: a command was named and the surface is scoped to whoever is asking.

--seat-id IS REQUIRED HERE, because nothing identified you: this agent has not registered, so the record cannot say which seat it holds, and no --seat-id was given. The surface is scoped to whoever is asking, so there is no tree to show until you say who that is — a seat id, or `operator`. You type it ONCE, at `register`; after that the binding is on the record and every later call resolves it for you.

feov-record — the seat-side record runtime (the verb set IS the role boundary)

A lens structurally cannot mint or close a board gap: no such verb exists in its
namespace. Blue has no board verbs at all. The bench rules and never originates.

Usage:
  feov-record [flags]

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

$ merge help
exit 2
stderr:
feov-record: a command was named and the surface is scoped to whoever is asking.

--seat-id IS REQUIRED HERE, because nothing identified you: this agent has not registered, so the record cannot say which seat it holds, and no --seat-id was given. The surface is scoped to whoever is asking, so there is no tree to show until you say who that is — a seat id, or `operator`. You type it ONCE, at `register`; after that the binding is on the record and every later call resolves it for you.

feov-record — the seat-side record runtime (the verb set IS the role boundary)

A lens structurally cannot mint or close a board gap: no such verb exists in its
namespace. Blue has no board verbs at all. The bench rules and never originates.

Usage:
  feov-record [flags]

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

$ blue help
exit 2
stderr:
feov-record: a command was named and the surface is scoped to whoever is asking.

--seat-id IS REQUIRED HERE, because nothing identified you: this agent has not registered, so the record cannot say which seat it holds, and no --seat-id was given. The surface is scoped to whoever is asking, so there is no tree to show until you say who that is — a seat id, or `operator`. You type it ONCE, at `register`; after that the binding is on the record and every later call resolves it for you.

feov-record — the seat-side record runtime (the verb set IS the role boundary)

A lens structurally cannot mint or close a board gap: no such verb exists in its
namespace. Blue has no board verbs at all. The bench rules and never originates.

Usage:
  feov-record [flags]

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

$ bench help
exit 2
stderr:
feov-record: a command was named and the surface is scoped to whoever is asking.

--seat-id IS REQUIRED HERE, because nothing identified you: this agent has not registered, so the record cannot say which seat it holds, and no --seat-id was given. The surface is scoped to whoever is asking, so there is no tree to show until you say who that is — a seat id, or `operator`. You type it ONCE, at `register`; after that the binding is on the record and every later call resolves it for you.

feov-record — the seat-side record runtime (the verb set IS the role boundary)

A lens structurally cannot mint or close a board gap: no such verb exists in its
namespace. Blue has no board verbs at all. The bench rules and never originates.

Usage:
  feov-record [flags]

Flags:
  -h, --help               help for feov-record
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed
  -v, --version            version for feov-record

═══ EVENTS ═══
{"key":"blue-synthesize:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"blue-synthesize","ts":0,"type":"EVENT_TYPE_REGISTER"}
{"base_ingest":{"text":"## S2\n\nA claim sits under S2.\n\n## S4\n\nA claim sits under S4.\n"},"key":"blue-synthesize:base_ingest:#1","seat_id":"blue-synthesize","ts":1,"type":"EVENT_TYPE_BASE_INGEST"}

═══ RENDERS ═══
-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)

_No inquiries recorded. On a run past epoch 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._


═══ REPORT ═══
# _(blue/report.md has no title — not authored here)_

**Report** · [Board](docket.md) · [Debate](debate.md) · [Run](run.md)

---

**Verdict:** NONE (no terminal outcome on the record)

## TL;DR

_(blue/report.md has no "## TL;DR" section — not authored here; blue owns this surface and red audits it.)_

## The Catechism

_(blue/report.md has no "## The Catechism" section — not authored here; blue owns this surface and red audits it.)_

## Technical foundations

_(blue/report.md has no "## Technical foundations" section — not authored here; blue owns this surface and red audits it.)_

## Analysis

_(blue/report.md has no "## Analysis" section — not authored here; blue owns this surface and red audits it.)_

## Risk matrix

| Risk | Likelihood | Impact | Complexity to mitigate | Mitigation / disposition |
|---|---|---|---|---|
| _(no open gaps)_ |  |  |  |  |

The matrix is a scan surface: each row's full problem statement, required fix and acceptance check are in [the board](docket.md).

## Research areas

_(none on the record)_

## Future research directions

_(none on the record)_

## Alternatives considered

_(none on the record)_

## Open questions

_(blue/report.md has no "## Open questions" section — not authored here; blue owns this surface and red audits it.)_

## Additional findings

## S2

A claim sits under S2.

## S4

A claim sits under S4.
