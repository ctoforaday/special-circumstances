$ lens observe --run {RUN} --seat-id red-lens-r1-L5 --kind note --label N1 --reason below bar but real
exit 0
stdout:
observation recorded: FINDING001 (N1) — awaiting merge disposition, which names it by ID

$ lens observe --run {RUN} --seat-id red-lens-r1-L5 --reason unlabelled, ordinal-keyed
exit 2
stderr:
feov-record: lens: record: observe requires --label — findings are addressed BY LABEL, so an unlabelled one can never be given a fate and stays open forever

$ lens observe --run {RUN} --seat-id red-lens-r1-L5 --reason second unlabelled
exit 2
stderr:
feov-record: lens: record: observe requires --label — findings are addressed BY LABEL, so an unlabelled one can never be given a fate and stays open forever

$ merge register --run {RUN} --seat-id red-merge-r1
exit 0
stdout:
registered red-merge-r1 (shard nonce NONCE002)

$ merge dispose --run {RUN} --seat-id red-merge-r1 --observation N1 --as declined --reason covered by R1-1
exit 0
stdout:
disposed N1: declined

$ merge dispose --run {RUN} --seat-id red-merge-r1 --observation N-missing --as banked
exit 2
stderr:
feov-record: merge: record: dispose --observation names N-missing, which no observe or finding event created — the disposal would name nothing and the real observation would stay undisposed. If the lens wrote it in prose but never recorded it, the finding is the thing that is missing

$ merge dispose --run {RUN} --seat-id red-merge-r1 --observation N2
exit 2
stderr:
feov-record: merge: record: dispose --observation names N2, which no observe or finding event created — the disposal would name nothing and the real observation would stay undisposed. If the lens wrote it in prose but never recorded it, the finding is the thing that is missing

═══ EVENTS ═══
-- events-red-lens-r1-L5-NONCE001.jsonl
{"key":"red-lens-r1-L5:register:NONCE001","nonce":"NONCE001","payload":{"tool_version":"0.36.0"},"round":1,"seatId":"red-lens-r1-L5","seq":0,"ts":0,"type":"register"}
{"key":"red-lens-r1-L5:observe:N1","nonce":"NONCE001","payload":{"finding_id":"FINDING001","kind":"note","label":"N1","text":"below bar but real"},"round":1,"seatId":"red-lens-r1-L5","seq":1,"ts":1,"type":"observe"}
-- events-red-merge-r1-NONCE002.jsonl
{"key":"red-merge-r1:register:NONCE002","nonce":"NONCE002","payload":{"tool_version":"0.36.0"},"round":1,"seatId":"red-merge-r1","seq":0,"ts":2,"type":"register"}
{"key":"red-merge-r1:dispose:N1","nonce":"NONCE002","payload":{"disposition":"declined","observation":"N1","reason":"covered by R1-1"},"round":1,"seatId":"red-merge-r1","seq":1,"ts":3,"type":"dispose"}

═══ RENDERS ═══
-- ledger
# red/ledger.md — RENDERED PROJECTION (source of truth: records/ event log; do not hand-edit)

## OPEN GAPS (0)

## CLOSURE INDEX



-- archive
# red/archive.md — RENDERED PROJECTION (append-only by construction in the event log)

-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- changelog
# blue CHANGELOG — RENDERED PROJECTION

-- citation-ledger
# red citation-ledger — RENDERED PROJECTION

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)

_No avenues recorded. On a run past round 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._

