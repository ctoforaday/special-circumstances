$ register --run {RUN} --seat-id red-chair
exit 0
stdout:
registered red-chair

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ register --run {RUN} --seat-id blue-respond
exit 0
stdout:
registered blue-respond

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ revision --run {RUN} --seat-id blue-respond --reason first pass
exit 0
stdout:
revision recorded — this sitting's changes are on the record [key blue-respond:revision:#1]

$ log --run {RUN} --seat-id blue-respond --reason no PDF extraction
exit 2
stderr:
feov-record: required flag(s) "type" not set

an entry for the operator who can retool you — what it asserts, said in the positive

An event that survives aborts, so something you hit is on the record even if the sitting does not finish.

SAY WHAT THE ENTRY ASSERTS: the operator triages this channel by FILTERING on it instead of reading every entry, so it is the field that makes the channel worth reading. An impediment you are merely NOTING has its own word, and is not a request for change.

MOST REFUSALS ARE YOURS and belong in no entry — a wrong verb, flag or quote: take the correction and move on. A refusal naming a verb you cannot find, or a fact the record holds that no view shows you, IS what the log is for: report it rather than engineer around it, because a workaround leaves no trace and the missing capability then looks exactly like one nobody wanted.

THE RECORD HOLDS WHAT YOU DID — which verbs you ran, which help pages you read, in what order — so an entry retelling it is a diary the operator must read through to find the one sentence meant for them. Write what you CONCLUDED about the tooling: what you wanted and could not reach, what behaved differently from its own documentation, what cost you a sitting. A clean sitting says so in one line and stops.

YOUR AUDIENCE IS THE OPERATOR who can retool you, not the other seats: nothing here is debate material, and the other side answers none of it.

If you need a verb or a flag that is not listed here, it does not exist for you:
do not improvise around it, and do not hand-write the artifact. Record what you
needed and what you would have done with 'log', as a request — a missing
capability is a finding about the tooling, and that channel is how it gets fixed.

FREE TEXT AND THE SHELL. Bash RUNS a backtick inside double quotes before this tool sees
your text, and records whatever the command printed — or nothing — in its place. Pass every
free-text value by capturing it first with a QUOTED heredoc, then give the flag the variable:

  X=$(cat <<'EOF'
  your text — backticks, apostrophes and $ signs are all literal here
  EOF
  )
  … "$X"

CORRECTING WHAT YOU RECORDED. If an act this command recorded came out wrong — a lost word, a
wrong figure — run it again with what you meant, adding --corrects <key> (the key its success line
printed as [key …]) and --correction-why <what was wrong>. The record keeps the first act, shown
struck beside its replacement. You may correct only your own act, only in the sitting that recorded
it, and only until another seat has acted; after that, say it in a new act. A correction may change
anything the act says.

Usage:
  feov-record log [flags]

Flags:
      --correction-why string   what was wrong with the act you are correcting, in one sentence; a reader sees it beside the struck text
      --corrects string         the key of your own act, written this sitting, that this invocation corrects — its success line printed it as [key …]
  -h, --help                    help for log
      --reason string           the entry: what you concluded about the tooling
      --type type-value         REQUIRED — what this entry asserts

Enumerated values:
  --type
    nominal   the surface met the work — the clean sitting, said in the POSITIVE: silence cannot say it
    defect    something is broken: it did the wrong thing, or failed where it should have worked
    request   a capability that does not exist — the act you wanted was on no surface, so there was nothing to get wrong
    friction  the work was impeded and you are NOTING it; NOT necessarily actionable and not necessarily advisable to change, which is why it has its own word rather than posing as a defect

Global Flags:
      --json             emit a structured JSON result (and structured errors) instead of human text
      --run string       the run directory — the PreToolUse hook injects it in a real run, so you rarely type it. A value that DISAGREES with the run you were dispatched into is refused
      --schema           print the event-schema epoch this binary writes, and exit
      --seat-id string   your seat id, as the dispatch prompt states it (SEAT_ID). Pass it ONCE, at register, which binds it to you on the record; every later call resolves it, so typing it is optional. It SELECTS this surface (the verbs listed are the ones your seat may run); a value disagreeing with your registration is refused

$ finding --run {RUN} --seat-id red-lens-logic --key F1 --severity medium --likelihood high --impact medium --quote ## S2 --reason unfounded leap
exit 0
stdout:
finding recorded: logic-F1 — the run-unique label a gap's found_by names (id FINDING001)

═══ EVENTS ═══
{"key":"blue-synthesize:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"blue-synthesize","ts":0,"type":"EVENT_TYPE_REGISTER"}
{"base_ingest":{"text":"## S2\n\nA claim sits under S2.\n\n## S4\n\nA claim sits under S4.\n"},"key":"blue-synthesize:base_ingest:#1","seat_id":"blue-synthesize","ts":1,"type":"EVENT_TYPE_BASE_INGEST"}
{"key":"red-chair:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"red-chair","ts":2,"type":"EVENT_TYPE_REGISTER"}
{"key":"blue-respond:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"seat_id":"blue-respond","ts":3,"type":"EVENT_TYPE_REGISTER"}
{"key":"blue-respond:revision:#1","revision":{"text":"first pass"},"seat_id":"blue-respond","ts":4,"type":"EVENT_TYPE_REVISION"}
{"finding":{"finding_id":"FINDING001","finding_key":"F1","impact":"GRADE_MEDIUM","label":"logic-F1","likelihood":"GRADE_HIGH","location":"## S2","severity":"GRADE_MEDIUM","text":"unfounded leap"},"key":"red-lens-logic:finding:logic-F1","seat_id":"red-lens-logic","ts":5,"type":"EVENT_TYPE_FINDING"}
{"anchor":{"id":"FINDING001","location":"## S2"},"key":"red-lens-logic:anchor:FINDING001","seat_id":"red-lens-logic","ts":6,"type":"EVENT_TYPE_ANCHOR"}

═══ RENDERS ═══
-- debate
# debate.md — RENDERED PROJECTION (source of truth: the record, records/record.db)

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: the record, records/record.db)

_No inquiries recorded. On a run past epoch 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._


═══ REPORT ═══
# _(blue/report.md has no title — not authored here)_

**Report** · [Board](docket.md) · [Debate](debate.md) · [Run](run.md) · [Changelog](CHANGELOG.md)

---

**Verdict:** NONE (no terminal outcome on the record)

## TL;DR

_(blue/report.md has no "## TL;DR" section — not authored here; blue owns this surface and red audits it.)_

## The Catechism

_(blue/report.md has no "## The Catechism" section — not authored here; blue owns this surface and red audits it.)_

## Technical foundations

_(blue/report.md has no "## Technical foundations" section — not authored here; blue owns this surface and red audits it.)_

## Analysis

_(blue/report.md has no "## Analysis" section — not authored here; blue owns this surface and red audits it.)_

## Risk matrix

| Risk | Likelihood | Impact | Complexity to mitigate | Mitigation / disposition |
|---|---|---|---|---|
| _(no open gaps)_ |  |  |  |  |

The matrix is a scan surface: each row's full problem statement, required fix and acceptance check are in [the board](docket.md).

## Research areas

_(none on the record)_

## Future research directions

_(none on the record)_

## Alternatives considered

_(none on the record)_

## Open questions

_(blue/report.md has no "## Open questions" section — not authored here; blue owns this surface and red audits it.)_

## Additional findings

## S2

A claim sits under S2.

## S4

A claim sits under S4.
