$ register --run {RUN} --seat-id red-merge-r1
exit 0
stdout:
registered red-merge-r1

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ mint --run {RUN} --seat-id red-merge-r1 --class scope-creep --check-kind document --check x --severity medium --likelihood medium --impact medium --problem docketed
exit 0
stdout:
minted R1-1

$ motion docket file --run {RUN} --seat-id red-merge-r1 --id R1-1 --reason contested, and not red's to close
exit 0
stdout:
motion M1 filed (docket)

$ register --run {RUN} --seat-id judge-r2
exit 0
stdout:
registered judge-r2

YOUR IDENTITY DID NOT REACH THE TOOL, so nothing on the record binds this agent to this seat. You are registered and your events are recorded; what is missing is the binding that lets a LATER call resolve who you are without being told.

TWO CONSEQUENCES:
  PASS --seat-id ON EVERY CALL. Without it the tool cannot tell which seat is asking and will refuse you.
  REGISTERING AGAIN WILL NOT FIX IT. If a later call says "this agent has not registered", it is THIS, not a lost registration — and a second register cannot supply an agent id the hook never sent. Your work is safe either way; the record is append-only and keeps both sittings.

$ motion docket rule --run {RUN} --seat-id judge-r2 --id M1 --as repaired --reason r
exit 2
stderr:
feov-record: record: motion rule requires --principle to say something (the rule the bench applied, stated so a later sitting can apply the same one)

rule on a docket motion — this verb is the bench seat's, and it appears only on that surface.

THREE SUBJECTS, THREE GAVELS. A grade dispute and a direction are ruled by the MERGE; a petition is ruled by the BENCH, before the debate continues. A motion is filed by any seat and ruled by one — that asymmetry is the mechanism, not an obstacle, and it is why `rule` is missing from the surfaces that do not hold the gavel.

Usage:
  feov-record motion docket rule [flags]

Flags:
      --as as-value          your ruling
      --final                nothing would reopen this. The assertable empty case for --reopens-on: pass exactly one of the two
  -h, --help                 help for rule
      --id string            REQUIRED — the motion id (M1, M2 …)
      --principle string     the rule you applied, stated so a later sitting can apply the same one
      --reason string        REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -        read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --reopens-on string    the evidence or condition that would make this worth raising again — or pass --final to say nothing would
      --review-flag string   what a human should look at again — empty when nothing needs it
      --settled string       what the losing party may no longer assert, in one sentence
      --tension string       the values that pulled against each other — empty is a real answer when none did

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES a successor naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    carried                   NOT a closure: the gap survives to the next round with a stated research direction the coming seat owes

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

$ motion docket rule --run {RUN} --seat-id judge-r2 --id M1 --as repaired --reason r --principle p --tension t
exit 2
stderr:
feov-record: record: motion docket rule requires --reopens-on (what would change this outcome) or --final (nothing would). A ruling that says neither leaves the losing party unable to tell a settled question from an unanswered one, which is the difference between an appeal and a wasted round

rule on a docket motion — this verb is the bench seat's, and it appears only on that surface.

THREE SUBJECTS, THREE GAVELS. A grade dispute and a direction are ruled by the MERGE; a petition is ruled by the BENCH, before the debate continues. A motion is filed by any seat and ruled by one — that asymmetry is the mechanism, not an obstacle, and it is why `rule` is missing from the surfaces that do not hold the gavel.

Usage:
  feov-record motion docket rule [flags]

Flags:
      --as as-value          your ruling
      --final                nothing would reopen this. The assertable empty case for --reopens-on: pass exactly one of the two
  -h, --help                 help for rule
      --id string            REQUIRED — the motion id (M1, M2 …)
      --principle string     the rule you applied, stated so a later sitting can apply the same one
      --reason string        REQUIRED — your reasoning or argument for this act — the substance the report renders and the other side answers; --reason-file for anything long or from stdin with -
      --reason-file -        read --reason from a file, or from stdin with - — the same field as --reason, for anything long or that would fight shell quoting
      --reopens-on string    the evidence or condition that would make this worth raising again — or pass --final to say nothing would
      --review-flag string   what a human should look at again — empty when nothing needs it
      --settled string       what the losing party may no longer assert, in one sentence
      --tension string       the values that pulled against each other — empty is a real answer when none did

Enumerated values:
  --as
    repaired                  the repair was verified at the leaf and nothing regressed
    repaired_with_regression  repaired, but something else broke — REQUIRES a successor naming the gap that carries the regression forward
    amends_prior              a defect found BETWEEN two repairs that each closed clean earlier — REQUIRES supersedes so the lineage is explicit
    not_a_defect              blue argued the finding was wrong and the argument held; nothing was repaired because nothing needed to be
    defect_accepted           the fix costs more than the defect (complexity above likelihood x impact) and the risk is taken KNOWINGLY, with the argument on the record
    defect_owed_elsewhere     a real defect whose fix is owned outside this debate; it leaves here and is not silently dropped
    carried                   NOT a closure: the gap survives to the next round with a stated research direction the coming seat owes

Global Flags:
      --json               emit a structured JSON result (and structured errors) instead of human text
      --run string         the run directory. The PreToolUse hook injects it in a real run, which is why you rarely type it. A value that DISAGREES with the run you were dispatched into is refused rather than obeyed
      --schema             print the event-schema epoch this binary writes, and exit
      --seat-id register   your seat id, as the dispatch prompt states it (SEAT_ID). You pass it ONCE, at register, which binds it to you on the record; after that every call resolves it for you and typing it is optional. It SELECTS this surface — the verbs listed are the ones your seat may run — and a value disagreeing with what you registered as is refused rather than obeyed

═══ EVENTS ═══
{"key":"red-merge-r1:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"round":1,"seat_id":"red-merge-r1","ts":0,"type":"EVENT_TYPE_REGISTER"}
{"key":"red-merge-r1:mint:R1-1","mint":{"acceptance_check":"x","check_kind":"CHECK_KIND_DOCUMENT","class":"scope-creep","class_new":false,"fix_basis":"proposed","gap_id":"R1-1","impact":"GRADE_MEDIUM","likelihood":"GRADE_MEDIUM","location":"","mint_key":"","problem":"docketed","required_fix":"","severity":"GRADE_MEDIUM"},"round":1,"seat_id":"red-merge-r1","ts":1,"type":"EVENT_TYPE_MINT"}
{"key":"red-merge-r1:motion:#1","motion":{"basis":"contested, and not red's to close","docket":{"gap_id":"R1-1"},"motion_id":"M1","subject":"MOTION_SUBJECT_DOCKET"},"round":1,"seat_id":"red-merge-r1","ts":2,"type":"EVENT_TYPE_MOTION"}
{"key":"judge-r2:register:#1","register":{"run_via":"flag","tool_version":"REVISION"},"round":2,"seat_id":"judge-r2","ts":3,"type":"EVENT_TYPE_REGISTER"}

═══ RENDERS ═══
-- debate
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- lines-of-inquiry
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)

_No inquiries recorded. On a run past round 0 that is itself a finding: the exploration
either did not happen or was not written down, and a report with no roads-not-taken is
indistinguishable from one that never looked._


═══ REPORT ═══
# _(blue/report.md has no title — not authored here)_

**Report** · [Board](docket.md) · [Debate](debate.md) · [Judgments](judgments.md) · [Run](run.md)

---

**Verdict:** _(no terminal outcome recorded — `bench outcome` was not run before assembly)_

## How this run was conducted

_Composed from the record, never from the run's configuration: these are the models that ANSWERED, measured at each seat's own first turn. A run can be configured for one tier and served by another, and a seat cannot see which — it reads the request._

| tier | what answered | seats |
|---|---|---|
| judgment | _NOT MEASURED — nothing observed what replied_ | 2 of 2 |

## Read this first

1 open gap(s) remain, most severe first — full statements in [the board](docket.md).

1. **[medium]** docketed (R1-1) — 

## TL;DR

_(blue/report.md has no "## TL;DR" section — not authored here; blue owns this surface and red audits it.)_

## The Catechism

_(blue/report.md has no "## The Catechism" section — not authored here; blue owns this surface and red audits it.)_

## Technical foundations

_(blue/report.md has no "## Technical foundations" section — not authored here; blue owns this surface and red audits it.)_

## Analysis

_(blue/report.md has no "## Analysis" section — not authored here; blue owns this surface and red audits it.)_

## Risk matrix

| Risk | Likelihood | Impact | Complexity to mitigate | Mitigation / disposition |
|---|---|---|---|---|
| docketed | medium | medium | — | — |

The matrix is a scan surface: each row's full problem statement, required fix and acceptance check are in [the board](docket.md).

## Research areas

_(none on the record)_

## Future research directions

_(none on the record)_

## Alternatives considered

_(none on the record)_

## Open questions

_(blue/report.md has no "## Open questions" section — not authored here; blue owns this surface and red audits it.)_

## Blue team report (sections not composed above)

## S2

A claim sits under S2.

## S4

A claim sits under S4.
