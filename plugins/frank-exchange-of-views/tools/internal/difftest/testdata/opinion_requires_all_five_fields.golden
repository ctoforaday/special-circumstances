$ bench register --run {RUN} --seat-id judge-r2
exit 0
stdout:
registered judge-r2 (shard nonce NONCE001)

$ bench opinion --run {RUN} --seat-id judge-r2 --id R2-1 --as closed
exit 2
stderr:
feov-record: bench: record: opinion --id names gap R2-1, which no mint event created — a dangling reference is accepted here and DROPPED at replay, which is how eight judicial closures vanished from a board that went on reporting them open

$ bench opinion --run {RUN} --seat-id judge-r2 --id R2-1 --as closed --principle p --tension t
exit 2
stderr:
feov-record: bench: record: opinion --id names gap R2-1, which no mint event created — a dangling reference is accepted here and DROPPED at replay, which is how eight judicial closures vanished from a board that went on reporting them open

═══ EVENTS ═══
-- events-judge-r2-NONCE001.jsonl
{"key":"judge-r2:register:NONCE001","nonce":"NONCE001","payload":{"tool_version":"0.18.0"},"round":2,"seatId":"judge-r2","seq":0,"ts":0,"type":"register"}

═══ RENDERS ═══
-- CHANGELOG.md
# blue CHANGELOG — RENDERED PROJECTION

-- archive.md
# red/archive.md — RENDERED PROJECTION (append-only by construction in the event log)

-- board-telemetry.jsonl

-- citation-ledger.md
# red citation-ledger — RENDERED PROJECTION

-- debate.md
# debate.md — RENDERED PROJECTION (source of truth: records/ event log)

-- findings.md
# red findings — RENDERED PROJECTION (source of truth: records/ event log)

-- ledger.md
# red/ledger.md — RENDERED PROJECTION (source of truth: records/ event log; do not hand-edit)

## OPEN GAPS (0)

## CLOSURE INDEX



-- lines-of-inquiry.md
# Lines of Inquiry — RENDERED PROJECTION (source of truth: records/ event log)


