{
  "_comment": "frank-exchange-of-views's own tool dependencies — aggregated by /prosthetic-conscience:doctor (each plugin owns its manifest; the doctor reports the whole suite). PLUGIN-SHIPPED BINARIES DO NOT BELONG HERE: this manifest lists EXTERNAL tools, checked by running them on PATH. A binary the plugin ships lives in its own bin/ and is discovered by sc-doctor's binary scan, which reports it and can fetch it; listing it here as well produced a check that could never pass (installed to <plugin>/bin, checked on PATH) and so a permanently DEGRADED verdict after a fully successful --fix.",
  "eventSchema": 2,
  "_eventSchema_comment": "The EVENT-SCHEMA EPOCH of the feov-record binary this plugin ships. It answers ONE question — does this binary write the event shape these prompts read — and it moves only when that shape changes incompatibly. It is deliberately NOT a version: a release number moves for reasons the schema does not care about, and comparing release numbers made this check track shipping instead of compatibility. `feov-record --schema` prints it; `setup` refuses to start a run when the binary's epoch differs from this one. There is no compatibility window and no delta list: equal runs, unequal refuses. It lives here rather than in .claude-plugin/plugin.json because that manifest has a fixed published schema and an unknown field there makes `claude plugin validate` warn on every run.",
  "tools": [
    {
      "name": "node",
      "purpose": "runs the debate engine (debate.js) and the zero-token simulator",
      "tier": "required",
      "check_cmd": "node --version",
      "install": {
        "windows": "winget install --id OpenJS.NodeJS.LTS -e",
        "darwin": "brew install node",
        "linux": "see https://nodejs.org/en/download"
      }
    },
    {
      "name": "uv",
      "purpose": "Python tool runner — uvx launches the arxiv-latex MCP server (FEOV document extraction)",
      "tier": "recommended",
      "check_cmd": "uv --version",
      "install": {
        "windows": "winget install --id astral-sh.uv -e",
        "darwin": "brew install uv",
        "linux": "curl -LsSf https://astral.sh/uv/install.sh | sh"
      }
    },
    {
      "name": "jq",
      "purpose": "filter a record projection in ONE call — every read verb emits JSON on stdout, and the `show` group's help instructs every seat to pipe it here. REQUIRED rather than recommended because its absence is SILENT: nothing refuses, the seat falls back to spooling the projection to a scratch file and re-parsing it in python, and the run merely costs three process spawns and three round-trips per read instead of one (measured: 251 json.load re-parses and 25 key-dumps in a single run). A degradation nobody is told about is the plausible zero this suite exists to refuse, so the doctor stops at the door instead",
      "tier": "required",
      "check_cmd": "jq --version",
      "install": {
        "windows": "winget install --id jqlang.jq -e",
        "darwin": "brew install jq",
        "linux": "apt-get install jq (or the distro equivalent)"
      }
    },
    {
      "name": "buf",
      "purpose": "convert a raw record shard line to JSON for inspection (`buf convert internal/record/recordpb/record.proto --type feov.record.v1.Event --from -#format=txtpb --to -#format=json`) — diagnostics only; nothing in the build or a run needs it, and `scripts/protogen` deliberately does not",
      "tier": "optional",
      "check_cmd": "buf --version",
      "install": {
        "windows": "go install github.com/bufbuild/buf/cmd/buf@latest",
        "darwin": "brew install bufbuild/buf/buf",
        "linux": "go install github.com/bufbuild/buf/cmd/buf@latest"
      }
    }
  ]
}
