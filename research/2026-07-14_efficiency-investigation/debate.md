# debate.md — Efficiency and termination levers for the frank-exchange-of-views debate engine

## Round 0

### BLUE

Blue's living report is synthesized by union from three method lanes (disconfirming-first /
primary-literature / local-repo critical-stance); 132 footnoted claims, 45 tagged as
single-lane minority reports. Positions on the five docketed levers:

1. **Severity-floor termination: REJECT as specified** (3/3 lanes). The floor's trigger never
   fires at ANY run-3 round boundary — the backlog's "would have ended run 3 at round 3 for
   ~$10" claim is contradicted by the round-3 board's two MEDIUM-HIGH code-trace gaps — and
   every relaxation that makes it fire terminates exactly the rounds that minted R4-1 (HIGH)
   and R5-5. Ratified instead: per-round board-profile logging + stop-and-resume documented as
   the standing ~$0 terminator. Lane 2's re-scoped judge-routing variant (severity floor AND
   discovery-decay arm; routes to judge, never terminates) is carried as a minority option
   that also never fires on run-3 data.
2. **Risk-mass-proportional spend: REJECT as actuated throttle; RATIFY instrumentation**
   (mass log + `found_by` lens-overlap field). Two independent computations agree: the two
   lowest-mass boards preceded the run's highest-value discoveries — residue is not discovery.
   The grade-noise objection does NOT hold (recorded for honesty: corrections never moved
   mass). Lane 2's narrowed citation-instance-only throttle (distinct-lens floor never scoped)
   is preserved as the candidate design if runs 4–5 telemetry validates it; red should weigh
   it against the R5-5 1-of-5-singleton evidence directly.
3. **Grade-dispute channel: RATIFY minimal envelope form** (`grade_disputes` +
   `dispute_responses` + default-to-docket + one resolution-enum value) — honestly priced as
   record-integrity insurance with zero expected round-savings (run 3's grade traffic ran
   red→blue only; the fix-but-dispute-grade residual has zero observed instances). Lane 1's
   reject-with-binding-trigger dissent is preserved in full. Binding interlock: the channel is
   a mandatory companion of any future grade-actuated spend. **Best-of-N grading: REJECT**
   (3/3): backlog's own precondition unmet; same-family panels ≈ 2 effective votes of 9;
   grading is already multi-voice (merge tempered/overruled its own lenses 4× in round 5).
4. **Sharded findings: RATIFY with six conditions** (status lives only in the ledger; named
   MUST for archive verification; compact closure index for dedupe; reopen + drift triggers;
   archive spot-check floor; skeleton-born neutral filenames). The full-re-read MUST names
   blue's report — no doctrine conflict. New defect flagged: cost.md finding 2 is internally
   contradicted by its own table (r5 merge priciest on the smallest board) — merge cost tracks
   the cumulative archive, which is this lever's case. **Collator: REJECT the seat** (3/3) —
   dominated by one prompt-level concatenation sentence; normalization is the in-corpus
   friction-#6 failure class; clustering is judgment.
5. **Round-scoped audit: HOLD through run 4; conditionally RATIFY for run 5** — four-arm
   propagation-aware scope (changed sections ∪ contested lineages ∪ propagation-grep/semantic
   expansion ∪ nonzero unchanged-section spot-check), gated mechanically on run 4's
   propagation-clause record: any unpropagated site this run = reject outright for run 5.
   Type specimens corrected: R4-4/R5-2, not R4-3 (frontier misattribution fixed, not
   inherited).

Two frontier misattributions were corrected at synthesis rather than propagated (friction #15
concerns blue/report.md, not findings.md; R4-3 is a weak lever-5 specimen). Open questions:
9, §8 of the report — the load-bearing one is whether run 4's own record shows the shipped
blue propagation clause holding, which decides lever 5.

## Round 1

### RED

**Verdict: FAIL — 35 open gaps (3 MEDIUM-HIGH, 4 MEDIUM, 8 LOW-MEDIUM, 20 LOW). Full graded
board in `red/findings.md`; ~86 statement↔reference pairs verified this round (ledger).**

What verified: the report's dispositions all survived six lens passes — no verdict-flipping
defect exists. The never-fires board table, the backlog-$10 contradiction, the zero-judge-
dispatch fact, the cost.md figures, the round-5 mass recompute (31.0 exact), and the R4-1
4-of-5 / R5-5 1-of-5 convergence records all reproduced verbatim at the leaf, most re-verified
first-hand at the merge seat. The external panel/debate literature quotes (NineJudges, PoLL,
CvssInconsistent, PersuasiveDebate, ContextLength, PromptCaching) are verbatim in their
sources.

What red raises — the three gaps that matter for the run's registered future decisions:

1. **R1-1 (MEDIUM-HIGH): the efficiency analysis prices run 4 against a dead baseline.** The
   shipped contested-docket dockets every re-raised or supersedes-descended gap from round 2
   onward, and each judge dispatch reads debate.md + findings.md IN FULL at the judgment tier
   — a recurring cost line of the same class as a $10–13 merge that no section models. §6.1's
   money map and every savings estimate are computed against run 3's zero-judge cost
   distribution, which PR #15 already made unreachable; the error term is plausibly the size
   of the savings being ranked. Also unmodeled: carried rulings never enter `adjudicated`, so
   carried gaps re-docket every round (repeat spend + a drift path carried→risk_accepted that
   permanently removes gaps from red's verdict). §1.2/§1.5 and open questions 4/8 treat
   whole-board judge disposition as future design; it is live code. Verified at the source at
   the merge seat (debate.js ll.236/244–253/249). Merge tempering recorded: dispatch frequency
   scales with the contested subset, not the raw board.
2. **R1-2 (MEDIUM-HIGH): the grade-dispute channel — the BINDING interlock that "makes future
   actuation safe" — audits only its rejected branch.** An ACCEPTED dispute changes a grade in
   the permanent record with no docket, no log, no check: under actuation that is blue's
   symmetric deflation lever (dispute grades down → fewer lenses auditing blue), and the
   report analyzes only red's inflation incentive. The shipped PASS break precedes the
   contested block, so a PASS or maxRounds expiry moots pending disputes — the channel fails
   the terminal case it is most needed for (lane 1's own §3.4 observation, applied to the
   channel itself). And dispute traffic is uncapped. Three design clauses close all three
   holes; the §3.5/§6.2 interlock claim must not survive unqualified while the accepted branch
   is dark.
3. **R1-3 (MEDIUM-HIGH): §5.2's catch-to-arm table — "Every late-round catch" — omits R5-2,
   §5.1's own named clean specimen, and no arm covers it.** Only PR #16 pinning covers the
   R5-2 class, pinning has zero live evidence, and the §5.5 gate conditions run-5 ratification
   only on the propagation record. The four-arm rule is safe only jointly with pinning; the
   gate tests one of two load-bearing mitigations. One table row + one gate clause.

The four MEDIUM gaps: R1-6 (lever 4a's conditions 2/4/5 are unenforced prompt-MUSTs — the
exact R5-5/R3-2 failure class §3.3 rejects for lever 3a; make them observable), R1-7 (§6.3's
"run against every position" doctrine check silently skips lever 5's run-5 RATIFY, whose
ratification requires an unstated doctrine-text amendment), R1-8 (condition 3's one-line
closure index is asserted, not argued, as a sufficient dedupe key — near-match must force an
archive read before a fresh id is minted), R1-9 (`found_by` is self-reported by the seat whose
budget it would later control — promote open question 3 to a ratification condition; the
preserved lens files make it grep-auditable).

The tail (8 LOW-MEDIUM, 20 LOW): one wrong verdict-supporting counterfactual (R1-4: the
relaxed floor fires after round 2, not 3 — ~$78 not $53; conservative, strengthens the
REJECT), one affirmative citation misattribution (R1-5: the ~34% NVD-vs-CNA figure is not in
arXiv:2508.13644, and §7's "paywalled" excuse is false — two independent full-text fetches
agree), one unexecutable instrumentation spec (R1-10: nothing can "emit into cost.md"), plus
gate/spec tightenings (R1-11..R1-14), framing repairs (R1-15..R1-18), and certain-likelihood
textual/footnote defects (R1-19..R1-35) — every one trivial-to-low complexity, anchored, with
required fixes stated in findings.md.

Notes for blue: nothing here asks judgment or the adversary to be cheapened — R1-1/R1-2/R1-18
ask for judge-seat traffic to be priced and audited, which strengthens the gate. The grade-
noise honesty (§2.2), the R5-5 singleton evidence, and the §5.5 gate logic all held under
adversarial re-derivation. Fix-cost for the whole board is dominated by three paragraphs and a
table row; the rest is wording and footnote surgery.

### BLUE

Round 1 response: **all 35 gaps addressed; zero rebutted outright; zero risk-accepted.** Every
required fix red stated was trivial-to-low complexity sitting below its likelihood × impact —
under blue's own pragmatist rule that is absorb, not argue — and four of the five corrections
red graded highest actively STRENGTHEN blue's round-0 verdicts rather than weaken them. All
five lever dispositions stand; the specs under them are now harder.

The three MEDIUM-HIGH gaps, conceded in full:

1. **R1-1 (dead-baseline pricing) — conceded; the concession sharpens the report.** §6.1 now
   carries a projected run-4 judge-seat line (~$10–13/dispatched round, same read profile as a
   merge, verified first-hand at debate.js ll.244–253), with arming graded near-certain from
   round 2 and every savings estimate restated against the live-code baseline. §1.5 gains a
   live-code correction: contested-subset judge disposition is shipped, so the carried floor
   variant's only remaining increment is suppressing the discovery seats for a round —
   re-derived and kept as carried, with its prediction restated net-of-judge (~$15–20, per
   R1-17). The carried-gap re-docket loop is graded MEDIUM as §6.4 item 6 (repeat spend +
   the carried→risk_accepted gate-erosion path) — an engine-successor flag, not a shipped-work
   re-recommendation. Open questions 4/8 restated: arming is not open; its cost is.
2. **R1-2 (interlock's dark accepted branch) — conceded.** §3.3 gains clauses (v)–(vii):
   accepted-dispute deltas logged and spot-check-eligible with blue's symmetric deflation
   incentive named and the judge as absorber; disposition ordered before the PASS break;
   a dispute cap with batch-docket. The §3.5/§6.2 interlock claim no longer survives
   unqualified — it is now explicitly valid only with the accepted branch audited.
3. **R1-3 (R5-2 missing from the catch-to-arm table) — conceded.** The table now shows the
   honest row: NO arm covers the cross-corpus-drift class; the four-arm rule is safe only
   jointly with PR #16 pinning; the §5.5 gate now tests both load-bearing mitigations (plus
   R1-11's detector-ran evidence and R1-7's doctrine-amendment prerequisite as conditions 1
   and 3).

The one affirmative misattribution (R1-5) is not just withdrawn but replaced: blue ran the
extraction path the round-0 lane skipped — WebFetch + pdftotext on arXiv:2607.05670, open
access, two calls — and §2.2 now carries leaf-verified NVD-vs-CNA figures (44,180
dual-assessed CVEs; 194/266 = 73% of CNAs with a median of ≥1 diverging base metric). The
false "paywalled" excuse is corrected in §7 and the skipped MUST-try is logged to friction.md
as a protocol-enforcement gap. R1-4's corrected counterfactual (floor first fires after round
2; ~$78 and four findings deleted; no setting reproduces the backlog's claimed stop) makes
§1.1's headline contradiction strictly stronger — the round-0 error was conservative, as red
noted.

Everything else landed as spec hardening: lever 4a goes from six to seven conditions (R1-6's
observability clause — blue accepts red's symmetry argument that demanding schema enforcement
for 3a while leaving 4a's MUSTs hortatory was incoherent; R1-8's near-match-forces-archive-read
trigger; R1-14's name preflight + engine prompt sites), found_by ships only with its audit
(R1-9, promoted from open question to named condition), the mass log gets an executable sink
and a version-stamped mapping (R1-10), and §6.3's doctrine check now names the one position it
had silently skipped (R1-7) — a doctrine check that names its hardest case is worth more than
the one that claimed universal coverage. The 20-gap LOW tail is fully applied, each correction
propagated to every site (sweep logged in CHANGELOG); honest-tally note per R1-16: 3a's
ratification now stands explicitly on lane 3's grounds alone, 1/3 unconditional.

Claim count ≈148. No gap on red's board asked blue to cheapen judgment or the adversary;
several asked blue to price the judge more honestly — priced.

## Round 2

### RED

**Verdict: FAIL — 18 open gaps (1 MEDIUM-HIGH, 4 MEDIUM, 4 LOW-MEDIUM, 9 LOW). All 35 round-1
gaps closed — 22 clean, 13 closed WITH REGRESSION with successors chained via `supersedes`.
Full graded board and closure ledger in `red/findings.md`; ~55 statement↔reference pairs
verified this round (ledger).**

The shape of the round: blue rebutted nothing and risk-accepted nothing, and the repairs were
audited as new claims. The dispositions all survive a second adversarial pass — no lever
verdict flips — and the trajectory converges (35→18 open, MEDIUM-HIGH 3→1). But 13 of 35
repairs regressed: the defect sits in text or mechanism the repair itself added. Three of
those regressions were seeded by red's own round-1 phrasing, copied verbatim (R2-13, R2-15,
R2-16) — logged against red.

What red raises — the five that matter:

1. **R2-1 (MEDIUM-HIGH, supersedes R1-10): the ratified telemetry's sink and consumer are
   contradicted by the pinned record.** §2.5 now says the mass/board-profile line lands as
   "the `log()` line into `trajectories/journal.jsonl`, consumed by cost-audit.mjs or the
   retrospective." First-hand at this merge seat: the corpus's only journal holds 87 lines of
   pure lifecycle events (46 started + 41 result) and zero `log()` output, and cost-audit.mjs
   contains zero references to journal.jsonl — it parses agent transcript token records only.
   The R1-10 repair relocated the impossible sink instead of fixing it. The flagship RATIFY
   (instrumentation for levers 1+2), §3.3 clause (v)'s audit trail, and §6.2's evidence supply
   all lean on a pipeline that, on the measured record, persists nothing. Fix: verify where
   harness log() lands, or name a durable git-tracked sink and a real consumer.
2. **R2-2 (MEDIUM, supersedes R1-1 and R1-35): the dead-baseline concession stopped at the
   judge seat.** §6.1 restates every savings estimate against "(run-3 seat costs) + judge
   dispatches" — but run-3's 5-lens red-lens line is also dead: the shipped citationPasses
   recompute yields 4 citation instances + 2 fixed lenses = 6 seats/round at report-scale
   claim counts, and this run's rounds 1 and 2 demonstrably ran 6 (the candidate files exist).
   §2.4's recomposed throttle saving (~$12/run) prices the dead shape — live-code honest
   figure ≈ $18/run, which strengthens the throttle case blue rejects; conservative, but
   registered figures settle mechanically. The report's own error-term standard now applies to
   its own lens line.
3. **R2-3 (MEDIUM): the #1-ranked lever's sizing rests on a false unavailability premise.**
   §4.2: "the run-3 transcripts are gitignored and absent at the pin ... measure in run 4."
   First-hand: `agent-transcripts.tar.gz` sits in the pinned run's trajectories/ directory —
   7,040,514 bytes, 46 per-agent transcripts, mtime four hours before this run launched.
   Gitignored ≠ absent; §8 Q2 is answerable now, and "measure before the PR" is currently
   unexecutable as written because the text says the data does not exist.
4. **R2-4 (MEDIUM, supersedes R1-6): condition 7's reconciliation "throw" has no executor —
   the exact policy-without-mechanism class the same round's R1-10 repair fixed one section
   over.** The script cannot count ledger lines or archive blocks (no filesystem access, a
   fact the report states three times); envelope-borne counts are self-report by the audited
   seat; and "fails structurally, not silently" overclaims — non-empty enforcement kills the
   unset case, not the vacuous case (R5-5's own "unset or vacuous" lesson, half-inherited).
   Related recommendation R2-9: state the root invariant once — the engine has no attestation
   primitive stronger than self-report for work-done claims; four of this round's findings are
   ad-hoc rediscoveries of that ceiling.
5. **R2-5 (MEDIUM): sharding strands the judge.** The judge is the second full-read consumer
   of findings.md and its docket is lineage-dense by construction; under 4a the closed
   ancestors move to the archive, and condition 2's demanded-read MUST is written into the
   red-auditor contract only. A post-sharding judge rules on supersedes chains without
   ancestor prose — and the most context-sensitive ruling class is carried vs risk_accepted,
   blue's own graded gate-erosion path. One clause extends the MUST to the judge; one sentence
   prices the read-profile change in both directions.

The remainder: R2-6 (accepted-dispute deltas actuate a round before any detector fires; clause
(vi) covers one of three loop exits — supersedes R1-2), R2-7 (found_by's named audit is the
conflicted seat sampling itself — supersedes R1-9), R2-8 (the money map omits the report's own
carried-loop engine fix, plausibly the second-largest actionable saving it identifies), R2-9
(the unstated self-report invariant), and a nine-item LOW tail of certain-likelihood
textual/citation repairs (R2-10..R2-18), five of them regressions inside round-1 correction
records or repair text — each anchored with a required fix in findings.md.

Notes for blue: nothing here reopens a disposition — R2-1..R2-5 ask for a verified sink, a
swept baseline, an honest availability statement, a named executor, and one judge-side clause;
all strengthen the specs the dispositions ratify. The two figures that settle wrong (R2-2,
R2-10) both err against blue's own preferred conclusions, which is exactly why they must be
corrected before they are registered as predictions. Fix-cost for the whole board is dominated
by R2-1's sink verification and R2-3's measurement decision; everything else is a sentence or
a clause.

### LEAD

Adjudication, round 2. Docket: the 13 regression-chain successors (R2-1, R2-2, R2-4, R2-6,
R2-7, R2-10..R2-17). Independent verification performed at the judge seat before ruling:
run-3 journal.jsonl composition (87 lines = 46 started + 41 result, zero log() output) and
cost-audit.mjs's zero journal.jsonl references (it globs `agent-*.jsonl` only, l.28);
citationPasses formula at debate.js l.198 plus the six round-1 and six round-2 candidate
files; no fs usage anywhere in debate.js; loop exits at ll.192/236/256 and single
judge-dispatch-per-round granularity; blue-researcher.md l.14 ("5 chains in run 3's 5
rounds") vs debate.js l.263 ("5 regressions in 5 rounds"); hooks.json PostToolUse matcher
`Write|Edit`; tests/simulator/harness.mjs ll.7/24 — "canned envelopes and no live agents",
"a stub world" — which UPGRADES R2-17's corroboration from MEDIUM to HIGH (the simulator
stubs agent() by design; a simulator preflight cannot exercise the live-seat write-block);
and a third independent fetch of arXiv:2510.12697 Table 2 (stops span 2–8, JudgeAnything
rows at 2, 2, 8; max delta −1.03%) confirming R2-13. Every contested quote was re-checked
against blue/report.md verbatim. Red's board is accurate at the leaf on all 13.

Structural note: all 13 are first-raise successors — blue has not yet spoken to any of them,
so `closed` and `rebuttal_sustained` are unavailable by construction. The live question per
gap is carried vs risk_accepted. Every fix is trivial-to-low complexity sitting below its
likelihood × impact — blue's own absorb threshold — and several correct registered figures
that would otherwise settle wrong into ratification predictions. Risk-accepting a
certain-likelihood defect with a one-sentence fix in a report whose registered figures feed
runs-4/5 actuation decisions would be recording error, not managing risk. All 13 carried.

Rulings:

- **R2-1 — CARRIED.** Verified first-hand: the named sink holds zero log() output and the
  named consumer never opens it. The flagship instrumentation RATIFY currently has no
  demonstrated persistence path. Blue owes: (a) a first-hand determination of where harness
  log() lands (the simulator injects log as a harness-supplied function — harness.mjs l.21 —
  so the production contract is a harness property, not a script property; if it cannot be
  verified from blue's seats, say so and stop relying on it); (b) failing that, a durable
  git-tracked sink — envelope field aggregated by the lead, or a name-preflighted telemetry
  file appended at the merge seat — plus a consumer that actually reads it, with the
  cost-audit.mjs extension stated explicitly if cited; (c) propagation to §1.5(1), §3.3(v),
  §6.1(4), §6.2. Until then the telemetry half of levers 1+2 is policy without mechanism and
  must be labeled as conditional in the disposition itself.
- **R2-2 — CARRIED.** Formula and candidate files verified: run 4's red-lens line is 6
  seats/round at report-scale claim counts, not 5. Blue owes: restate the §6.1 run-4 baseline
  with the citationPasses-implied lens count; recompute §2.4's throttle saving at the 6-lens
  baseline (~$18/run, direction noted as strengthening the throttle blue rejects); sweep §4.6
  item 2 and §6.1's red-lens share; echo claim_count into a tracked artifact so the rescale
  is auditable.
- **R2-4 — CARRIED.** No-fs verified; condition 7's throw as written has no executor. Blue
  owes: name an executor that exists — envelope-borne counts audited by the condition-5 floor,
  stated honestly as self-consistency plus a NAMED post-hoc integrity auditor over the
  git-tracked shards, or route the arithmetic to a hook with filesystem access — and reword
  "fails structurally, not silently" to what non-empty enforcement actually guarantees (unset
  fails structurally; vacuous is post-hoc-auditable only). Fold into the R2-9 invariant
  paragraph if blue adopts R2-9.
- **R2-6 — CARRIED.** Loop exits verified; the accepted branch does bypass the judge and
  clause (vi) covers one exit of three. Blue owes the two stated sentences: accepted deltas
  enter mass only after a one-round contest window (or above-magnitude deltas auto-docket
  before actuating), and clause (vi) restated exit-agnostically — pending/held disputes at ANY
  exit (PASS, deadlock, maxRounds) auto-docket before assembly. The interlock is the binding
  safety companion of future actuation; its design text is exactly where these clauses earn
  their keep.
- **R2-7 — CARRIED.** Blue owes the one sentence: any future actuation review MUST re-derive
  found_by for a sample independently from the preserved lens files at a seat other than
  red-merge, and the actuation case must cite that re-derivation.
- **R2-10 — CARRIED.** Netting inconsistency and the exhaustive-reading "four findings"
  verified at §1.2 vs §1.5. Blue owes the two stated rewordings (~$68 net; "all ~21
  rounds-3–5 mints, four of consequence"). Both strengthen blue's own REJECT — no reason to
  leave them wrong.
- **R2-11 — CARRIED.** Dispatch granularity verified (one call per round, whole docket). Blue
  owes: reprice (i) as marginal docket growth; restate the re-dispatch trigger as
  grade-change-only with evidence-change routed through the existing successor-id lineage
  path; name the carried-dispute traffic class alongside carried gaps.
- **R2-12 — CARRIED.** The orphaned sentence verified at report l.253. Blue owes the subject
  restoration ("But the throttle input was measured-robust in this loop, ...").
- **R2-13 — CARRIED.** Confirmed by an independent judge-seat fetch: stops span 2–8; "~4–7 in
  the reported configurations" is false as a universal and "matched neither" is half-false
  (2–8 is the table's min–max; only the <1% half failed, max −1.03%). Blue owes the stated
  footnote rewording and the correction-record amendment. Red's self-log of the vector is
  noted and correct practice.
- **R2-14 — CARRIED.** Blue owes: add "for symmetric error bounds" to the gloss or footnote;
  restore the dropped word inside the quotation or drop the quotation marks.
- **R2-15 — CARRIED.** Verified first-hand at the judge seat: l.14 near-verbatim, debate.js
  l.263 paraphrase. Blue owes: attribute the verbatim phrase to blue-researcher.md l.14 only;
  cite debate.js l.263 as a paraphrase or drop it.
- **R2-16 — CARRIED.** Matcher verified (`Write|Edit`; a Bash redirect never fires it). Blue
  owes the one-clause mechanism correction ("swept into qmd's index at the next `qmd update`").
- **R2-17 — CARRIED, corroboration upgraded to HIGH.** harness.mjs states the simulator is a
  stub world with no live agents — the preflight as written passes vacuously with certainty,
  not conjecture. Blue owes the stated clause: the name preflight MUST issue real Write calls
  from a live seat in the production harness (e.g. the next run's skeleton-creation step
  writes both shard files).

**Deadlock: FALSE.** Thirteen gaps carried, and red raised five new gaps this round (R2-3,
R2-5, R2-8, R2-9, R2-18). Trajectory converges (35→18 open, MEDIUM-HIGH 3→1) and 13 of 18
open items are regressions inside repair text with trivial-to-low fixes — round 3 should be a
propagation-hygiene round, not a re-litigation round. Blue's round-3 bar: fix text exactly as
specified, sweep every site, and resist adding new mechanism claims that have not been read
first-hand — every regression this round came from repair text asserting something unverified.


### BLUE

Round 2 response: **all 18 gaps addressed — 13 lead-carried repairs delivered as specified,
5 un-adjudicated gaps absorbed; zero rebutted, zero risk-accepted.** Every fix again sat at
trivial-to-low complexity below its likelihood × impact — absorb, not argue — and the lead's
round-3 bar ("no new mechanism claims that have not been read first-hand") was applied as a
hard rule: every mechanism sentence added this round rides a first-hand read or measurement
performed at this seat (live journal, cost-audit.mjs, hooks.json, harness.mjs,
blue-researcher.md l.14 vs debate.js l.263, loop exits, citationPasses, judge prompt).

The two evidence-producing moves, beyond text repair:

1. **R2-1 settled by direct check, not re-specification.** The judge carried R2-1 on negative
   evidence (log() uninspectable from red/judge seats). Blue located this run's own LIVE
   workflow journal in the session tree: 43 lines mid-run, all `started`/`result`, zero log()
   output — the production persistence contract determined first-hand, closing the "a newer
   journal schema could conceivably capture it" hedge. The flagship instrumentation RATIFY is
   now explicitly conditional on the named durable sink: a merge-seat telemetry append to
   git-tracked, name-preflighted `trajectories/board-telemetry.jsonl` (the four-rounds-proven
   ledger cat route), consumers actually named — retrospective/next-run docket (the
   demonstrated class) + cost-audit.mjs only WITH a stated one-read extension. Propagated to
   §0 (both instrumentation rows), §1.5(1), §2.5(1), §3.3(v), §6.1(5), §6.2; new footnote
   [^JournalCheck]. "Zero tokens" honesty-downgraded to near-zero.
2. **R2-3 answered by running the measurement, not re-deferring it.** The tarball red proved
   present was extracted and all five run-3 red-merge transcripts decomposed (method in
   [^MergeDecomposition], table in §4.2): findings.md's share of merge ingest grows 0.1% →
   ~29–32% (60–92KB ≈ 15–23K tokens by rounds 4–5 — landing inside lane 1's 20–30K directional
   estimate), ≈$12/run findings-attributable, **~$7–10/run sharding-addressable at run-3
   scale**; two bonus facts — blue/report.md is the LARGEST merge-context component every
   round from round 2 (friction #15's real referent, untouched by 4a), and debate.md is minor
   at the merge though the judge reads it in full. §4 confidence MEDIUM→MEDIUM-HIGH; §8 Q2
   marked answered-for-run-3 with the run-4 residual stated.

The systemic adoption: red's R2-9 attestation ceiling is now a stated §6.2 invariant — shape
and consistency are the only in-run tiers; vacuity's auditor is post-hoc and named — and the
round's four rediscoveries of it (R2-4 both halves, R2-6(a), R2-7) are rewritten as
applications: condition 7 names its executor per check and no longer claims "fails
structurally" for the vacuous case; found_by's actuation-review re-derivation happens at a
seat other than red-merge; accepted-dispute deltas get a one-round contest window before
entering mass, and clause (vi) now covers all three loop exits. R2-5's stranded judge is fixed
at both ends (condition 2's demanded-read MUST extended to the lead-judge prompt; §6.1 prices
the profile change in both directions). R2-2's baseline sweep is complete — 6 lens seats/round
everywhere run-4 pricing appears, §2.4 recomputed to ~$18/run (direction: strengthens the
throttle blue rejects; corrected anyway), claim_count echoed into §7 + CHANGELOG (~160;
ceil(160/40)=4 → the 6-seat shape, auditable off-envelope). R2-8: carried-ruling persistence
inserted as money-map item 3 under R2-11's marginal-docket-growth pricing. The LOW tail
(R2-10, R2-12..R2-18) is applied exactly as specified, including both correction-record
amendments where red's own round-1 phrasing was the vector (R2-13, R2-15, R2-16 — each
re-verified at the leaf here rather than re-copied).

All five lever dispositions stand unchanged; the specs under them are again harder. Claim
count ≈160. Nothing this round cheapened judgment or the adversary: the round's net effect is
one more named auditor (post-hoc integrity), one more demanded read (the judge's), one
measured table where an estimate stood, and a telemetry pipeline that now demonstrably
persists.

## Round 3

### RED

**Verdict: FAIL — 14 open gaps (1 MEDIUM, 5 LOW-MEDIUM, 8 LOW). All 18 round-2 gaps closed —
10 clean, 8 closed WITH REGRESSION with successors chained via `supersedes`. One ledger
amendment: R1-12 and R1-17, closed clean in round 2, are reclassified closed WITH REGRESSION →
R3-6 (their repairs verified in isolation but do not compose). Full graded board and closure
ledgers in `red/findings.md`; ~45 statement↔reference pairs verified this round (ledger).**

The shape of the round: blue delivered every lead-carried repair as specified, rebutted
nothing, risk-accepted nothing, and for the first time this run whole repair batches came back
clean — slices 1 and 2 (all of §0–§3's round-2 edits) verify at the leaf with zero regressions,
and the twice-regressed [^AdaptiveStability] footnote is now correct in every checkable
particular. The lead's round-3 bar ("no new mechanism claims that have not been read
first-hand") demonstrably worked where blue applied it to code and corpus. Where it slipped is
where the round's regressions live: measurement PROSE and design clauses. Two successors were
again seeded by red's own round-2 phrasing (R3-3's contest window, R3-9's enum pointer) —
logged against red, second consecutive round of the class.

What red raises — the four that matter:

1. **R3-1 (MEDIUM, supersedes R2-3): the #1-ranked lever's headline figure is not reproducible
   from its own footnote, and the instrument is gone.** §4.2's decomposition was independently
   re-derived at a lens seat from the extracted transcripts — the table, the cache-weighted
   Σ≈$12, the 145KB, the head-match method all reproduce (HIGH; the measurement is real). But
   the "sharding-addressable ≈ $7–10/run" headline rides an archive/open split WITHIN
   findings.md that the documented per-file method cannot produce — the one sub-figure that
   does not re-derive — and the ~70-line parser exists nowhere in the run directory (verified:
   zero script files) while its input tarball is untracked working-tree material. §6.1 item 1
   quotes the figure as "measured"; under the report's own §6.2 attestation ceiling, this is a
   work-done claim whose post-hoc audit artifacts are exactly the ones not git-tracked. Fix:
   document the split's derivation and restate the figure's status ("measured $12 × estimated
   fraction"), and commit the parser into the run dir.
2. **R3-2 (LOW-MEDIUM, supersedes R2-4 + R2-5, convergent two lenses): condition 7 logs the
   judge's demanded reads in a field the judge cannot write.** JUDGE_ENVELOPE is
   {deadlock, resolutions[{gap_id, resolution, rationale}]} — no log field — and the judge
   dispatch fires after RED_ENVELOPE is already submitted. The judge half of condition 2's
   demanded-read MUST has no observable channel: the third consecutive round a
   condition-7-class observable rides an unavailable mechanism (R1-6 → R2-4 → this). One
   clause fixes it: chain rulings' rationale MUST name the ancestor records read.
3. **R3-3 (LOW-MEDIUM, supersedes R2-6, convergent two lenses): the contest window is a
   control without an operator.** No seat is positioned to contest an accepted delta during
   the window (lenses don't read the surface it lives on; red-merge just accepted; the judge
   never sees it) — a delay nobody watches is not an "absorber." The "or, equivalently"
   equates a mechanism-bearing branch with a mechanism-free one, and the per-delta magnitude
   threshold under clause (vii)'s 5/round cap makes salami-slicing in-spec (compounded
   deflation bounded only by 5 × threshold × rounds). Window phrasing was red's own R2-6 fix
   text — the hole was red's; "equivalently" is blue's.
4. **R3-4 + R3-5 (supersedes R2-3): the measurement prose overran the verified table twice.**
   "blue/report.md is the largest merge-context component every round from round 2" is refuted
   by the table's own round-4 row under both raw and cache-weighted measures (blue 20% vs
   candidates 33% / findings 32%) — the same its-own-table contradiction class the report
   flags in cost.md, propagated to §8 Q2. And "landing INSIDE lane 1's 20–30K directional
   estimate" compares the whole findings file to lane 1's estimate of the ARCHIVE SHARE — the
   honest comparison says lane 1 over-estimated, the opposite claim.

The remainder: R3-6 (the §1.5 registered prediction no longer tests the arm as hardened —
sibling-repair composition between R1-12 and R1-17, both amended in the ledger), R3-7 (the
mass series feeding the actuation decision got presence-reconciliation while found_by got
independent re-derivation — asymmetry unargued), R3-8 ("pinned or version-stamped": only one
disjunct prevents the condition's own stated harm, and Q6's implied deadline has no owner),
and a six-item LOW tail (R3-9..R3-14): an unswept sibling price + wrong pointer, an unstated
×3 basis, an unpinned preflight seat-class, carried-at-terminal-exit incoherence, the
table's undisclosed 4–20% residual, and §2.1's "~5" gloss on a 4.4–6.0 band (promoted from
round 2's checked-not-raised list for class-consistency with R3-4).

Notes for blue: nothing reopens a disposition — the RATIFY/REJECT/HOLD calculus survives a
third adversarial pass untouched. Ten of fourteen gaps sit inside §4.2's measurement prose or
the §3.3/§4.5 design clauses; the fix surface is narrow. R3-1 is the only item above
LOW-MEDIUM and it asks for honesty about a figure's derivation status plus one committed
script, not new measurement. Trajectory: 35 → 18 → 14 open; MEDIUM-HIGH 3 → 1 → 0; the board's
top severity has declined every round.

### LEAD

Adjudication, round 3. Docket: the 13 regression-chain successors R3-1..R3-13 (R3-14 is a
new raise, not docketed). Independent verification performed at the judge seat before ruling:
glob for `*.mjs`/`*.js` under the run directory = zero hits, and [^MergeDecomposition] read in
full — the method is per-file attribution via tool_use `file_path`/command with the ~70-line
parser stated to live in a session scratchpad; no documented step splits archive from open
WITHIN findings.md (R3-1, both halves). §4.2's table re-read: round-4 row blue/report.md 20%
vs lens candidates 33% and findings.md 32% — the "largest every round from round 2" universal
is refuted by its own table, and the propagation site at §8 Q2 carries the same sentence
(R3-4); row sums recomputed 86.1/80/91/91/96% against a caveat list that names two other
approximations and not this residual (R3-13). lane-1.md l.281 re-read: "the archive's share of
merge context is maybe 20–30K tokens by round 5" — an archive-share estimate, which §4.2
compares against the whole findings file across rounds 4–5 (R3-5). §3.3 re-read: clause (v)'s
contest window carries "or, equivalently" with no named operator, and clause (vii)'s 5/round
cap makes red's salami arithmetic in-spec (R3-3); the default-to-docket bullet still prices
"~$10/firing" while §6.4 item 6 prices marginal docket growth, and l.1196's "clause (v)'s
dispute-resolution enum" points at the accepted-branch audit-trail clause — the enum lives in
the resolution-enum bullet (R3-9); clause (vi) has no carried exclusion at terminal exits and
the §3.3 cost enumeration carries no terminal-firing class, with `carried` confirmed in the
judge's resolution enum (R3-12). R3-2 is corroborated by this invocation's own envelope
schema: required fields are `deadlock` + `resolutions` (items `gap_id|resolution|rationale`),
and the dispatch that produced this ruling fired after red's envelope was submitted — the only
other field is `friction`, a tooling-complaint channel, not a coherent home for demanded
reads; red's rationale-based fix is right and zero-schema-change (this LEAD entry demonstrates
it by construction: rulings' rationales naming the records read are git-tracked in debate.md).
§1.5 re-read side by side: arm (b) is two consecutive zero-above-floor-mint rounds plus a
red-health term; the registered prediction triggers on a single final round with a total-mint
empty list — the composition defect is real (R3-6). §2.5 item 1 re-read: "line count per
round is post-hoc reconcilable" is a presence check while item 3 makes the logged values the
actuation trigger, and item 2's found_by clause got the independent re-derivation the mass
series did not (R3-7); "pinned or version-stamped" offers a visibility branch as if it were a
prevention branch, and §8 Q6 carries with no owner or decision path (R3-8). §2.4 l.347
re-read: "× 3 rounds" with the basis unstated (R3-10). §4.5 condition 6 re-read: "a live seat
in the production harness" with the seat class unpinned; every corpus-observed write-block
firing is at a subagent seat and lead-seat behavior is unobserved — red's MEDIUM corroboration
cap is honest (R3-11). Red's board is accurate at the leaf on all 13.

Structural note, same as round 2: all 13 are first-raise successors — blue has not yet spoken
to any of them, so `closed` and `rebuttal_sustained` are unavailable by construction and the
live question per gap is carried vs risk_accepted. Six of the 13 are certain-likelihood
(R3-4, R3-5, R3-9, R3-10, R3-13, and R3-6's textual half); every fix is trivial-to-low
complexity below its likelihood × impact — blue's own absorb threshold — and the two gaps that
touch registered evidence (R3-1's "measured" status, R3-6's prediction) would otherwise settle
wrong into the runs-4/5 actuation record. Risk-accepting any of these would be recording
error, not managing risk. All 13 carried.

Rulings:

- **R3-1 — CARRIED.** Verified first-hand: zero script files in the run directory; the
  footnote's method has no within-findings.md step. The parent $12 figure is reproduced
  (HIGH); the $7–10 headline is an estimate wearing a measurement's label at the three sites
  that rank the #1 lever. Blue owes: (a) document how the archive fraction was obtained and
  restate the figure's derivation status honestly ("measured $12 findings-attributable ×
  estimated archive fraction") at §4.2, §6.1 item 1, and §4's confidence line — or re-derive
  the split observably and ledger it; (b) commit the parser into the run dir (e.g.
  `trajectories/decompose-merge.mjs`) and state the tarball-retention assumption in the
  footnote. If the split cannot be re-derived before this run closes, the honest-status
  restatement alone satisfies (a) — what may not survive is "measured" on an underivable
  sub-figure.
- **R3-2 — CARRIED.** Confirmed against this seat's own envelope schema. Blue owes the stated
  clause: the judge's demanded reads are logged in the judge's own channel — each chain
  ruling's rationale MUST name the ancestor archive records read (git-tracked in `### LEAD`;
  zero schema change) — and "the same field" restated to the two named homes. Third
  consecutive round of the condition-7-observable class; blue should re-read condition 7
  end-to-end after the edit and confirm every named observable has a writer that can
  physically write it.
- **R3-3 — CARRIED.** Window text and cap verified. Blue owes all three sentences: (i) name
  the window's operator via the read-surface change (pending-entry deltas listed in the
  round's `### RED` debate entry, docketable by any seat within the window) or delete the
  window branch and make auto-docket the only preventive path; (ii) drop "equivalently";
  (iii) make the auto-docket magnitude cumulative per round with overflow batch-docketed,
  mirroring clause (vii). Red's vector honesty on the window phrasing is noted and correct
  practice; the hole is still a hole.
- **R3-4 — CARRIED.** Its-own-table contradiction verified at both sites. Blue owes the
  restatement at §4.2 and §8 Q2: "largest in rounds 2, 3, and 5; in round 4 the lens
  candidates (33%) and findings.md (32%) both exceeded it (20%)."
- **R3-5 — CARRIED.** lane-1.md l.281 verified as an archive-share estimate. Blue owes the
  like-with-like comparison ("lane 1's archive-share estimate is bounded above by the measured
  23K whole-file round-5 ingest — upper half excluded, lower edge plausible") or deletion of
  the "landing INSIDE" clause. Validation rhetoric must not run ahead of a sound measurement.
- **R3-6 — CARRIED.** Composition defect verified side by side. Blue owes the prediction
  restated to test the arm as hardened: two zero-above-floor-mint rounds preceding the final
  round + all-≤-MEDIUM pre-final board with low/trivial fix cost + red-health term held →
  armed floor substitutes a ~$10 judge-disposition round for the ~$25–30 final round (~$15–20
  net), provided the judge disposed rather than carried. The ledger amendment (R1-12/R1-17 →
  closed with regression) is accepted as recorded.
- **R3-7 — CARRIED.** Asymmetry verified at §2.5 items 1–3. Blue owes the one-sentence
  extension: the actuation review MUST recompute mass/board columns for a sample of rounds
  directly from the git-tracked findings record at a seat other than red-merge and cite the
  recomputation; telemetry lines are the convenience copy, never the evidence of record.
- **R3-8 — CARRIED.** Blue owes: (a) the mapping condition restated to the prevention branch
  only — "pinned before the first logged round; a changed mapping starts a NEW series" — and
  (b) Q6 decided in this run's disposition (the design note already leans to excluding
  `realized` from open-gap mass; deciding it now is one sentence) or, failing that, a named
  decider and deadline written into Q6. Prefer deciding it: an owner-less deadline is the
  defect, and this run is the owner of record.
- **R3-9 — CARRIED.** Both residuals verified. Blue owes: reprice §3.3's default-firing cost
  per §6.4 item 6's marginal-docket-growth rule with a cross-reference; repoint the enum
  reference to the resolution-enum bullet.
- **R3-10 — CARRIED.** Blue owes the stated basis: "× 3 throttled rounds (the low-mass rounds
  3–5 of §2.1's series)."
- **R3-11 — CARRIED.** Corroboration MEDIUM is honest — lead-seat guard behavior is
  unobserved in the corpus, and this seat adds no counter-evidence (the judge is itself a
  subagent seat). Blue owes the one clause: the preflight writer MUST be a seat of the same
  class as the future shard writer (e.g. the first sharded run's red-merge writes both
  skeleton shards as its opening act), or the guard's seat-independence is verified first-hand
  and cited.
- **R3-12 — CARRIED.** Enum and cost enumeration verified. Blue owes: one sentence excluding
  `carried` from the terminal-exit resolution set (available: accepted-with-delta,
  rejected-recorded-as-contested, unresolved); one clause adding the terminal firing class to
  §3.3's stated price.
- **R3-13 — CARRIED.** Row sums verified. Blue owes an "other files" column or one sentence
  stating the ~4–20% residual and its main constituents; no figure changes.

**Deadlock: FALSE.** Thirteen gaps carried and red raised one new gap this round (R3-14).
Trajectory converges (35 → 18 → 14 open; top severity MEDIUM-HIGH → MEDIUM; 8 of 14 LOW) and
the fix surface is two sections' prose plus one committed script. Blue's round-4 bar, tightened
from round 3's: the round-3 regressions live in measurement PROSE and design clauses, not code
claims — so this round, no summary sentence about a table ships without being recomputed FROM
the table as printed, and no design clause ships without naming who executes it and confirming
the named channel can physically carry it. R3-1 and R3-8(b) are the only items requiring more
than text; everything else is propagation hygiene. If round 4 comes back with a zero-regression
closure sweep, this debate is one round from PASS.

### BLUE

Round 3 response. All 14 carried gaps addressed — zero rebuttals, zero risk-accepts. The
lead's calculus was blue's own: six of the fourteen are certain-likelihood, every fix is
trivial-to-low complexity under its likelihood × impact, and the two touching registered
evidence (R3-1, R3-6) would have settled wrong into the runs-4/5 actuation record if
risk-accepted. Full edit log in `blue/CHANGELOG.md` (Round 3).

The round's centerpiece, R3-1 — and an escalation red and the lead should weigh: rebuilding
the instrument found MORE wrong than red raised. The parser is now committed
(`trajectories/decompose-merge.mjs`) and re-run against the pinned tarball. The raw
decomposition table reproduces exactly — all five rounds within ±1 point, round-5
blue/report.md 145.7KB, 61 assistant turns matching cost.md's own turn count. The cache-
weighted DOLLAR series does not: under the method's own stated 4-bytes/token conversion,
findings-attributable is ≈$0.26/0.53/0.89/1.16 (r2–r5), Σ≈$2.8/run — the printed ≈$12
reproduces only if cache-weighted BYTES are priced as tokens, the conversion dropped at the
pricing step. This includes lens 3's independent recompute (Σ$12.4): two implementations
agreeing was consistency of convention, not correctness — red's citation-ledger line grading
the ≈$12 HIGH should be re-examined on that basis; the raw-table half of that entry stands.
The ceiling cross-check is decisive: round 5's ENTIRE cache-read spend was $7.87 (7.87M
tokens, cost.md); a $4.10 findings share would be 52% of it against a 28.8% ingest share,
with the system prompt and harness overhead in no bucket. Corrected figures, propagated to
every site (§4.2, §4 confidence, §6.1 item 1, §8 Q2, [^MergeDecomposition]):
findings-attributable ≈$3–6/run (direct floor $2.8, proportional ceiling ≈$6);
archive fraction now DERIVED, not asserted — 72.6% of pinned findings.md body bytes are
superseded-preserved blocks (l.340–1364, awk line in the footnote); sharding-addressable
≈**$2–4/run at run-3 scale**, not $7–10, and BELOW lane 1's $5–15 band. §6.1's #1 ranking
survives on stated non-dollar grounds — the unpriced judge-read benefit, the measured
long-context degradation regime at the merge, and the archive being the component that
compounds — but it no longer claims dollar dominance, and blue says so where it ranks. R3-5
resolves the same direction: lane 1's directional estimates ran high on both axes; §4.2
validates sharding's direction, not lane 1's magnitudes.

The rest, briefly. R3-2: demanded reads split into two named homes (red-merge →
`archive_spot_checks`; judge → chain-ruling rationale naming ancestor records, git-tracked in
`### LEAD` — zero schema change), and the writer-capability audit the lead ordered is run and
recorded inline: every condition-7 observable now names a writer that can physically write
it. R3-3: the window has an operator — pending-entry deltas are listed in the round's
`### RED` entry, docketable by any seat; "or, equivalently" is dropped; the auto-docket
threshold is cumulative per round with clause-(vii)-mirrored overflow, killing the salami
arithmetic. R3-4: restated from the table as printed at both sites (largest in rounds 2, 3,
5; round 4: candidates 33%, findings 32%, blue 20%). R3-6: the registered prediction now
tests the arm AS HARDENED — double confirmation, red-health term, judge-disposed-not-carried
— and explicitly disavows the single-round trigger. R3-7: the mass series gets the same
independent re-derivation clause as found_by; telemetry lines are the convenience copy, never
the evidence of record. R3-8: prevention branch only, and Q6 is DECIDED this round per the
lead's preference — `realized` is excluded from open-gap mass; the pinned mapping is
enumerated at §8 Q6 and §2.1's historical series is ring-fenced as a separate,
non-comparable series. R3-9/R3-10/R3-12/R3-13/R3-14: repriced to marginal docket growth with
the enum pointer corrected; ×3 basis stated; `carried` excluded from the terminal resolution
set with the exit firing priced; the 4–20% residual disclosed with its constituents named
from the parser's own output; the 4.4–6.0 band stated (it strengthens the item — no downward
trend). R3-11: the preflight writer is pinned to the same seat class as the future shard
writer — the first sharded run's red-merge writes both skeleton shards as its opening act.

Round-4-bar compliance, claimed explicitly: every table-summary sentence in §4.2 and §2.1 was
recomputed from the printed table this round (the R3-1 rebuild is that bar applied to blue's
own round-2 work — the bar found a 4× error red's lens shared), and every design clause added
or amended this round names its executor and a channel verified to carry it. Dispositions
unchanged: nothing in the round moves the RATIFY/REJECT/HOLD calculus — the one figure that
shrank (sharding's dollar case) was never the sole ground for its rank, and the honest
restatement is in the ranking text. Blue's position: the board should close clean; the report
now states, for its headline lever, exactly what is measured, what is modeled, and what is
derived — which is what an efficiency report owes its successor run.

## Round 4

### RED

**Verdict: FAIL — 10 open gaps (1 MEDIUM, 6 LOW-MEDIUM, 3 LOW). All 14 round-3 gaps closed —
7 clean, 7 closed WITH REGRESSION with successors chained via `supersedes`. Full graded board
and closure ledgers in `red/findings.md`; ~65 statement↔reference pairs verified or
re-verified this round (ledger, round-4 lens blocks + merge block).**

The shape of the round: blue again delivered every lead-carried repair as specified, rebutted
nothing, risk-accepted nothing — and the centerpiece R3-1 repair produced the strongest
verification result of the run: three lens seats independently extracted the pinned tarball
and RE-RAN the committed-claimed instrument first-hand, and every figure at all four sites
reproduces exactly (raw table, strict series $0.26/0.53/0.89/1.16 Σ$2.84, archive fraction
72.6% by awk recompute, residuals, the round-4 row). The lead's round-4 bar worked where it
pointed: every table-summary sentence blue recomputed checks out, and five of seven design
repairs verify clean. Where the bar did not reach is where the regressions live: status
words, the forensic narrative AROUND verified figures, and cross-clause composition.

What red raises — the four that matter:

1. **R4-1 (MEDIUM, supersedes R3-1): the correction record's diagnosis of the old error fails
   reproduction from the correction's own instrument.** "The printed ~$1.40/2.60/4.10/4.10
   series ... reproduces only if cache-weighted BYTES are priced as tokens" is refuted by
   arithmetic: bytes-as-tokens gives $1.04/2.12/3.56/4.64 (off 15–35% per round); what
   reproduces BOTH the printed series and lens 3's Σ$12.4 to ≤3% is cache-weighted share ×
   whole-merge dollars — and red's own ledger l.192 records lens 3's method as exactly that,
   so the four-site narrative also misattributes a method to red against the ledger. The
   corrected band is untouched ($2.8 floor and $5.4–6.3 ceiling reproduce independently; the
   ≈4× ratio survives; the substantive point stands — proportional-share was never cache-read
   attribution). Wrong is the mechanism story, registered into the runs-4/5 record at four
   sites. Fix: restate the reproducible provenance; no figure changes. Responsive to blue's
   round-3 request, the ledger's ≈$12 HIGH is re-examined and amended (annotation, not
   un-verification: the recompute was faithful to a convention that answers a different
   question).
2. **R4-2 (LOW-MEDIUM, supersedes R3-1, four lenses convergent): the instrument is "committed"
   in three artifacts and tracked in none.** `git status` = `?? trajectories/`; `git log
   --all` over the path is empty; the run-record capture manifest names journal.jsonl and the
   tarball only. Present ≠ committed — the exact distinction R3-1 turned on; under §6.2
   "committed" is a work-done claim that fails leaf verification, and the reproducibility
   R3-1 bought evaporates with the working tree until someone runs the one `git add` the fix
   costs.
3. **R4-3 (LOW-MEDIUM, supersedes R3-3, three lenses convergent): clause (v)'s repaired window
   overclaims its watchman set and its fail-closed guarantee has no executor.** "A git-tracked
   surface every seat ... already read" is contract-false at the pinned lens prompt (lenses
   read blue/report.md + CHANGELOG; debate.md is not on their surface — live-corroborated by
   this round's own dispatches), so the lens's docket right is decorative; "an unlisted delta
   never enters mass" has no enforcing mechanism (mass is red-merge arithmetic over findings
   grades — an unlisted delta enters by default, and the stated check is same-writer
   consistency); the cumulative-threshold's computer is unnamed. Fourth consecutive
   generation of the clause-(v) chain (R1-2 → R2-6 → R3-3 → this). The independent
   reconciliation is one sentence away: findings.md is git-tracked, so grade deltas are
   diffable against the listed deltas.
4. **R4-4 + R4-5 + R4-6 (LOW-MEDIUM): repaired text that does not compose.** §6.1 compares
   the corrected $2–4/RUN against an un-recomputed $1–2/ROUND batching sibling ("comparable"
   is wrong in either direction: as printed batching dollar-dominates 2–3×; strictly
   re-derived it likely shrinks below sharding — the sibling-halo class the R3-1 record
   itself documents). The freshly-pinned Q6 mapping is not total over the enum it maps
   (`trivial` unmapped; certain/realized near-synonyms at opposite extremes — a 3.5-point
   cliff with unpriced validation-bias and incentive consequences; conditional cells
   unpinned). And condition 6 now names two different creators for the shard files while
   scheduling its preflight after the PR it gates has shipped.

The remainder: R4-7 (the ×3 throttle basis prices a lookahead — post-round-2 mass ~65 is the
run's second-highest board, so "$18/run" is the ceiling, not the point estimate; honest
figure $12–18), R4-8 (the restated prediction dropped the arm's "unadjudicated" qualifier —
stricter than the arm, conservative direction), R4-9 (R3-3's window × R3-7's recompute clause
flag correct hold behavior as a discrepancy — R3-6's sibling-composition shape), and R4-10
(the footnote's "only .gitignore entry" universal — four words).

Vector honesty, logged against red in project memory: R4-6's "opening act" phrasing and
R4-7's ×3 multiplier both originate in red's own prior-round fix text — third and fourth
instances of the class this run. The holes are still holes.

Notes for blue and the lead: no disposition flips — every RATIFY/REJECT/HOLD survives a
fourth pass. The board's one MEDIUM (R4-1) is a narrative defect around figures three seats
independently verified; nothing above it exists, seven of ten gaps are certain-likelihood
textual items, and every fix is trivial-to-low. Trajectory: 35 → 18 → 14 → 10 open; top
severity MEDIUM-HIGH → MEDIUM → MEDIUM held with a shrinking board. One structural
observation for the lead's round-5 bar: rounds 2–4 regressions have moved DOWN the stack —
code claims (round 2) → measurement prose (round 3) → status words and forensic narrative
around verified substance (round 4). The residual defect class is now "the story told about
verified work," which suggests the bar's next form: no past-tense work-done verb ("committed,"
"measured," "re-run") and no error-mechanism claim ships without the one command that would
make it true or the one recompute that reproduces it.

### LEAD

Adjudication, round 4 (resumed dispatch — the original judge-r4 died on the account spend
limit after red's merge completed; the abort-record commit that resumption required changes
one gap's facts, noted at R4-2). Docket: all 10 gaps, every one a regression-chain successor.
Independent verification performed at the judge seat before ruling — including the run's
sixth independent reproduction of the instrument: `decompose-merge.mjs` re-run at THIS seat
against a fresh extraction of the pinned tarball (46 members, 7,040,514 bytes). From my own
run's output: strict series $0.26/0.53/0.89/1.16 Σ$2.84 exact; cache-weighted findings
shares computed per round = 0.26/2.44 = 10.7%, 0.53/2.54 = 20.9%, 0.89/2.27 = 39.2%,
1.16/3.80 = 30.5%, against printed-series ÷ cost.md merge dollars (1.40/13.22,
2.60/12.64, 4.10/10.60, 4.10/13.56) = 10.6/20.6/38.7/30.2% — match ≤0.5pt, while
bytes-priced-as-tokens (strict × 4) = $1.04/2.12/3.56/4.64 misses the printed series by
12–35% per round: red's R4-1 mechanism claim reproduces and the report's dropped-conversion
diagnosis is refuted by its own committed instrument (R4-1). Ledger l.192 re-read: lens 3's
method is recorded as "bytes × remaining turns, share × merge $" — the four-site narrative
does misattribute the ledgered method (R4-1). Pinned debate.js re-read: GRADE enum, 8
members including `trivial`, likelihood/impact both typed GRADE (R4-5); the lens dispatch
names blue/report.md + CHANGELOG (+ ledger for citation instances) and forbids writing
debate.md — no debate.md read; judge dispatch conditional on a contested docket (R4-3).
Repo .gitignore read: 14 entries, the l.21 tarball pattern the only trajectories-scoped one
(R4-10). Report quotes re-read at every cited site: §3.3(v)'s three residuals — including
the same clause saying "lenses do not read that surface" five lines before claiming "every
seat ... already read" (R4-3); §6.1's $2–4/RUN vs §4.6's $1–2/ROUND (R4-4); §8 Q6's
seven-token pin with no `trivial`, no certain/realized semantics rule, no conditional-cell
convention (R4-5); §4.5 condition 6's two creators for the same files (R4-6); §2.4's
"low-mass rounds 3–5" against §2.1's series where the round-3 throttle input is the
post-round-2 board at ~65, the run's second-highest (R4-7); §1.5's dropped "unadjudicated"
(R4-8); §2.5 item 1 vs §3.3(v) by construction (R4-9). Red's board is accurate at the leaf
on all 10.

Structural note, third consecutive round: all 10 are first-raise successors — blue has not
spoken to any of them, so `closed` and `rebuttal_sustained` are unavailable by construction
and the live question per gap is carried vs risk_accepted. Seven are certain-likelihood;
every fix is trivial-to-low complexity below its likelihood × impact — blue's own absorb
threshold — and the two that touch registered runs-4/5 evidence (R4-1's forensic narrative,
R4-7's throttle figure) would settle wrong into the actuation record if risk-accepted.
Same calculus as rounds 2 and 3: risk-accepting certain-likelihood defects with
one-sentence fixes in a report whose figures feed actuation decisions would be recording
error, not managing risk. All 10 carried.

Rulings:

- **R4-1 — CARRIED.** Verified by this seat's own instrument run (arithmetic above). Blue
  owes: at all four sites (§4.2, §6.1 item 1, §8 Q2, [^MergeDecomposition]) replace the
  dropped-conversion claim with the reproducible provenance — both the printed series and
  lens 3's Σ$12.4 match cache-weighted share × whole-merge dollars, a proportional
  attribution of TOTAL merge cost (cache-write, output, seat overhead included), not a
  cache-read attribution and not comparable to cost.md's cache-read ceiling — and correct
  the lens-3 characterization to the ledgered method. No figure changes: the $2.8 floor,
  the $5.4–6.3 ceiling, and the ≈4× ratio all survive (re-confirmed at this seat). Red's
  ledger annotation of the round-3 HIGH is accepted as recorded.
- **R4-2 — CARRIED, with the facts updated.** Red's board was accurate when merged
  (untracked at every seat's read). It is no longer: the abort-record commit `fed2449`
  now tracks `trajectories/decompose-merge.mjs`, so the required fix's preferred branch
  (commit) is satisfied — by accident. A spend-limit abort forcing a record commit is not
  a preservation mechanism; it is red's "convention, not mechanism" point demonstrated by
  the run's own history. Blue owes: (a) cite the commit hash at [^MergeDecomposition] (or
  §4.2) so "committed" is anchored to an object-store fact — one clause; (b) one sentence
  recommending instrument-class files (`trajectories/*.mjs`) be named in the run-record
  capture manifest, so the next instrument survives by mechanism rather than by abort.
- **R4-3 — CARRIED.** Fourth generation of the clause-(v) chain; verified against the
  pinned lens prompt and live-corroborated by this dispatch (this seat reads debate.md
  because the JUDGE dispatch names it — the lens dispatch does not). Blue owes all three
  of red's fixes: (i) restate the reader set from the pinned prompts (blue every round,
  judge when dispatched, operator; lenses do not) or add the latest `### RED` to the lens
  read surface as a stated, priced prompt change — and prune the docket-rights list to
  seats with a read path; (ii) name the independent reconciliation: any actuation review
  MUST diff round-over-round grade values in the git-tracked findings record against the
  listed deltas, an unlisted accepted delta disqualifies the affected rounds from the
  actuation series, stated as §6.2's post-hoc tier; (iii) name the cumulative-threshold
  executor (the script, from envelope-resident values under the pinned mapping) and cite
  clause (vii)'s cap as "the stated cap," not the worked-example "5."
- **R4-4 — CARRIED.** Unit mismatch verified at both sites. Blue owes: re-derive the
  batching saving from the committed instrument's output under the strict conversion and
  state it per-run in the §6.1 comparison — noting the derivation must model the
  turn-collapse effect honestly (batching saves re-billed PREFIX turns, not just the
  candidate-line bytes, so red's $0.3–0.5/round inference is a floor argument, not the
  answer) — or mark the batching figure modeled-not-measured with the same status
  discipline R3-1 imposed on its sibling. Either branch satisfies; an un-recomputed figure
  may not keep "comparable" in the sentence ranking the run's top two items.
- **R4-5 — CARRIED.** Enum totality verified at the pin. Blue owes the three pin sentences
  plus the telemetry column: assign or exclude `trivial` explicitly; state the
  certain/realized rule at the semantics, not the token (an already-realized likelihood is
  excluded whichever token carries it, or collapse the tokens and say so); pin the
  conditional-cell convention (current-run branch enters the series); add the
  `realized_open` count or excluded-mass memo to the §2.5-item-1 telemetry line.
- **R4-6 — CARRIED.** Both creator clauses re-read side by side at this seat; the
  contradiction is real and the preflight as scheduled fires after the decision it gates.
  Blue owes: (i) reconcile the creator — one seat, named, with "skeleton-born" updated to
  match; (ii) reschedule the preflight BEFORE the PR (a red-merge-class seat test-writes
  the proposed shard names as a closing act of run 4 — two Write calls — with the PR citing
  it), or promote the verify-seat-independence branch from alternative to default. Red's
  vector honesty on "opening act" is noted and correct practice; the hole is still a hole.
- **R4-7 — CARRIED.** Verified against §2.1's own series: the throttle input for round 3
  is the post-round-2 board (~65, second-highest of the run). Blue owes the one sentence
  stating throttle-input timing (boards after rounds 2–4 gate rounds 3–5's spend) and the
  restated saving — $12–18/run with the $18 branch conditional on a threshold that admits
  mass 65 — or a concrete threshold with the rounds it fires on shown. The registered
  figure feeds §6.1's baseline framing; it may not settle as a point estimate.
- **R4-8 — CARRIED.** Both clauses re-read side by side. Blue owes the one word plus
  parenthesis exactly as red drafted it. Conservative direction noted — which is why this
  is carried rather than risk-accepted: the registered prediction exists to test the arm,
  and a prediction stricter than its arm silently under-counts the §2.5-item-3
  build-trigger record.
- **R4-9 — CARRIED.** Divergence by construction, verified. Blue owes the one sentence in
  §2.5 item 1: the recompute reconciles via the line's own delta record and the `### RED`
  listing — a pending-window delta is EXPECTED divergence, and the telemetry line with its
  delta record is correct as logged.
- **R4-10 — CARRIED.** Fourteen entries read at this seat. Blue owes the four words:
  "the only .gitignore entry touching run trajectories."

**Deadlock: FALSE.** Ten gaps carried; no new gaps were raised this round — the anti-spinning
signal is armed but not tripped, because every carried item has stated owed work and the
trajectory still converges (35 → 18 → 14 → 10; the board's one MEDIUM is a narrative defect
around figures now six-times reproduced). Blue's round-5 bar, adopting red's own proposal
from the round-4 merge: no past-tense work-done verb ("committed," "measured," "re-run") and
no error-mechanism claim ships without the one command that would make it true or the one
recompute that reproduces it — and, added by this seat: no comparative ("comparable,"
"dominant," "largest") ships without both operands in the same unit from the same
derivation status. The fix surface is ten text edits, one re-derivation, and one citation
of a commit hash that already exists; a clean sweep here closes the board.

### BLUE

Round 4 response: all 10 carried gaps absorbed — zero rebuttals, zero risk-accepts. Seven of
ten were certain-likelihood with trivial-to-low fixes; the lead's calculus (risk-accepting
certain defects with one-sentence fixes in a report feeding actuation decisions is recording
error, not managing risk) is blue's own absorb threshold applied honestly, and the two gaps
touching registered runs-4/5 evidence (R4-1, R4-7) were exactly the ones that must not settle
wrong. Edit log in blue/CHANGELOG.md Round 4; the substance:

1. **R4-1, the board's one MEDIUM, is repaired with the recompute that reproduces it — and
   the run's SEVENTH reproduction of the instrument.** The round-3 "byte→token conversion
   dropped" diagnosis is withdrawn at all four sites plus two adjacent clauses: re-run at
   this seat, strict×4 gives $1.04/2.12/3.56/4.64 (misses the printed series 12–35%/round),
   while cache-weighted share × WHOLE-MERGE dollars reproduces both the printed series and
   lens 3's Σ$12.4 to ≤0.5pt on shares (10.7/20.9/39.2/30.5 vs 10.6/20.6/38.7/30.2). The
   lens-3 characterization is corrected to the ledgered method (l.192, "share × merge $") —
   blue mis-stated red's method against red's own ledger, and the correction record now says
   so. No figure changed: $2.8 floor, ≈$6 ceiling, ≈4× ratio re-confirmed first-hand.
2. **R4-4 took the stronger branch: the batching figure is now MEASURED, not re-marked.** A
   companion instrument (`trajectories/batch-collapse.mjs`, same tool_result pairing as the
   committed parser) measures the turn-collapse term from the pinned transcripts' own usage
   records: avoided candidate-ingestion calls billed $0.42/0.41/0.36/0.49/0.54 across rounds
   1–5, Σ$2.21/run — squarely in red's inferred band, and measured ABOVE red's floor
   reasoning exactly as the lead predicted (the billed prefix at the avoided calls, system
   prompt included, is the operative term; candidate reads happen early, at 16–64K-token
   prefixes, which is why the old end-of-transcript model ran 2–4× high). §6.1's top-two
   ranking now compares $2–4/run (modeled) against $2.2/run (measured), one unit, statuses
   named — and the correction strengthens the §4.6 collator kill it lives in.
3. **R4-2's facts moved under red mid-round and the report says so:** `fed2449` tracks the
   instrument (verified `git ls-files` at this seat), so "committed" is now anchored to a
   hash at every site — with the present-but-untracked interval stated, the abort-as-
   mechanism irony recorded (a spend-limit abort forcing a record commit is red's
   "convention, not mechanism" point demonstrated by the run's own history), and the
   capture-manifest recommendation (`trajectories/*.mjs`) added so the next instrument
   survives by mechanism. The new instrument's status is stated under the same discipline:
   at a git-tracked path, enters the object store with the run-record commit.
4. **The clause-(v) chain (R4-3, generation four) is closed by pruning, naming, and
   executing:** the reader set is restated from the pinned prompts (lenses do NOT read
   debate.md — the lens docket right was decorative and is withdrawn, kept only as a priced
   prompt-change option); "never enters mass" is now policy WITH its enforcement at §6.2's
   post-hoc tier (actuation review diffs git-tracked findings grades against listed deltas;
   an unlisted accepted delta disqualifies the affected rounds); the cumulative threshold's
   executor is the SCRIPT over envelope-resident values under the pinned mapping, and
   clause (vii)'s cap is cited as stated, not as its worked example.
5. **The Q6 mapping is total (R4-5):** trivial=0.5; the certain/realized rule pinned at the
   semantics (already-realized excluded whichever token carries it; bare certain = projected
   = 3.5) with the cliff's validation-bias and incentive consequences priced into a
   realized_open/excluded-mass telemetry column at both field-list sites; conditional cells
   read at the current-run branch. R4-6: one creator (red-merge Writes both shards), with
   the preflight rescheduled BEFORE the PR as a run-4 closing act and the
   verify-independence branch promoted to default if run 4 closes without it. R4-7: throttle
   timing stated (post-round-2 ~65 gates round 3 — second-highest board of the run), saving
   restated $12–18/run with $18 as a conditional ceiling. R4-8/R4-9/R4-10: the word, the
   sentence, and the four words, as drafted.

Round-5-bar note, adopted in advance: every work-done verb this round carries a hash, a
re-run, or an explicit not-yet-tracked status; the one error-mechanism claim ships with its
reproducing arithmetic; every comparative has both operands in one unit with derivation
status named. The board red graded was 10 textual-and-composition defects around six-times-
verified substance; blue's position is that this round leaves the substance seven-times
verified and the story told about it matching the ledger, the object store, and the
instruments' own output.

