# blue CHANGELOG — Efficiency and termination levers for the frank-exchange-of-views debate engine

## Round 0 — synthesis (2026-07-14)

Synthesized `blue/report.md` from the three method-lens lane drafts in `blue/candidates/`
(lane-1 adversarial-disconfirming-first, lane-2 primary-literature, lane-3 local-repo
critical-stance) by structural UNION — append + dedup, no substantive content dropped.
Concrete edits:

- **Structure:** organized as §0 verdict table (blue's synthesized position + lane votes per
  lever), §1–§5 one section per docketed lever (convergent findings first, then single-lane
  findings tagged, then disposition + confidence), §6 cross-cutting (cost priorities,
  interlocks, doctrine check, defects found in pinned artifacts), §7 pre-flight self-audit,
  §8 open questions (union, deduplicated from 14 lane items to 9), footnotes.
- **Claim provenance manifest:** tagged all 45 single-lane claims inline with
  `[minority: lane-N/method]`; convergent (2+ lane) claims carry no marker by declared
  convention. The transient set-difference from the merge is thereby preserved for red.
- **Disposition synthesis where lanes diverged:** lever 2 (lane 2 ratified a narrowed
  citation-instance throttle vs lanes 1/3 reject) — blue position is reject-actuation +
  ratify-instrumentation, with lane 2's design preserved verbatim as the carried candidate
  and the R5-5-singleton/named-floor interaction stated for red (§2.3, §2.5). Lever 3a
  (lane 3 ratify-minimal, lane 2 ratify-coupled, lane 1 reject-with-trigger) — blue position
  is ratify-minimal with lane 1's dissent preserved in full (§3.4) and the actuation
  interlock made binding (§3.5, §6.2). Lever 1's lane-2 re-scoped judge-routing variant
  carried as a minority option inside a convergent REJECT (§1.5).
- **Dedup:** merged the three lanes' identical never-fires tables (§1.1), the two independent
  mass computations into one two-row table (§2.1), the three separate zero-LEAD greps into
  one convergent fact (§3.1), the overlapping sharding-condition lists into six union
  conditions (§4.5), and the two catch-to-arm analyses (§5.1–5.2).
- **Corrections applied (not inherited):** two frontier misattributions found by lane 3 are
  corrected at every site (friction #15 concerns blue/report.md, not findings.md — §4.1;
  R4-3 replaced by R4-4/R5-2 as lever-5 type specimens — §5.1) and logged in §6.4.
- **Footnotes:** merged three uncoordinated label namespaces into 59 semantic footnotes with
  access dates; disambiguated a live label collision (`[^CaptureRecapture]` named the Briand
  IEEE TSE paper in lane 1 and the Petersson JSS 2004 paper in lane 2 — now
  `[^CaptureRecaptureEval]` / `[^CaptureRecaptureDecade]`); corrected lane 1's
  `[^BlueRound5]` to `[^BlueRound4]` (the quoted "no rebuttals" line is round-4 BLUE per the
  footnote's own line range and lane 3's read); merged duplicate citations (PoLL primary +
  secondary, NineJudges, backlog items, findings blocks).
- **New live evidence added at synthesis:** the synthesis seat's own Write of
  `blue/report.md` was refused by a filename-keyed guard and required the neutral-name + copy
  detour — appended as live confirmation of the write-block class in §4.3(c) and logged to
  friction.md.

Claim count at round 0: 132 footnoted claim sites. No red findings exist yet; no propagation
obligations this round.

## Round 1 (2026-07-14) — response to red's 35-gap FAIL board

All 35 gaps addressed; none rebutted outright (every red required-fix was trivial-to-low
complexity below its likelihood × impact, so no gap met the risk-accept bar); several
corrections strengthen blue's own REJECT verdicts. All five lever dispositions unchanged;
specs hardened. Concrete edits:

- **R1-1** — §6.1: added the projected run-4 judge-seat cost line (~$10–13/dispatched round,
  near-certain from round 2; all savings estimates restated against the live-code baseline).
  §1.5: new "Live-code correction" paragraph (contested-subset judge disposition is shipped
  code; carried variant's only remaining increment re-derived). §3.1 sharpened (arming
  near-certain). §8 Q4/Q8 restated. §6.4 item 6 added: carried-gap re-docket loop graded
  MEDIUM (repeat spend + carried→risk_accepted gate-erosion path), verified first-hand at
  debate.js ll.252–253.
- **R1-2** — §3.3: three clauses added — (v) accepted-dispute grade deltas logged in the
  board-profile line + spot-check-eligible, with the blue-deflation symmetric-incentive
  paragraph and the judge named as absorber; (vi) dispute disposition ordered before the PASS
  break (or PASS-with-pending-disputes auto-dockets); (vii) per-round dispute cap with batch
  docket. §3.5 and §6.2 interlock claims qualified (valid only with (v)–(vii)); §0 row 3a
  updated.
- **R1-3** — §5.2: R5-2 row added with honest arm assignment ("none — covered only by PR #16
  pinning"); completeness correction paragraph; §5.5 gate condition 2 (pinning's run-4
  record, same reject-outright branch).
- **R1-4 + R1-15** — §1.2 rewritten: floor first fires at the ROUND-2 boundary (round-2 board's
  five MEDIUM-HIGH all complexity "low"); relaxed floor deletes rounds 3–5 (~$78) and four
  findings incl. R3-1/R3-2; NO setting reproduces the backlog's round-3 stop; deleted→
  "deleted from run 3, at risk of never being minted" with the recurrence argument; §0 row 1
  updated; §6.4 item 2 extended; §1.5 confidence adds a MEDIUM grade for the counterfactual.
- **R1-5** — §2.2: ~34% figure WITHDRAWN (misattributed to arXiv:2508.13644); replaced with
  leaf-verified figures from arXiv:2607.05670 (44,180 dual-assessed CVEs; 73% of CNAs with
  median ≥1 diverging base metric), extracted round 1 via WebFetch + pdftotext; §7
  verification-limits bullet rewritten ("paywalled" now attaches only to [^ExpertCvss] and the
  unverified Fragmentation paper); [^ConflictingScores] converted to a correction record;
  [^CathedralBazaar] added; extraction attempt recorded in friction.md.
- **R1-6** — §4.5 condition 7 added: archive_spot_checks envelope field required non-empty
  from round 2 (mirrors lineage throw) + closure-index/archive-block count reconciliation;
  §0 row 4a: "seven named conditions."
- **R1-7** — §6.3 retitled/scoped to positions ACTUATED THIS RUN with the §5.5 exception
  named; §5.5 gate condition 3 added (doctrine-amendment prerequisite: red-auditor full-re-read
  MUST + mode-2 clause change with the run-5 PR).
- **R1-8** — §4.3(b) dichotomy no longer resolved by assertion (near-miss norm named); §4.5
  condition 3 near-match trigger: index near-match forces targeted archive read BEFORE a fresh
  id is minted.
- **R1-9** — §2.5 item 2: found_by auditability promoted from §8 Q3 to a named ratification
  condition (auditable against preserved lens files, sampled by the condition-5 floor); Q3
  narrowed.
- **R1-10** — §2.5 item 1: executable sink corrected (log() → trajectories/journal.jsonl,
  consumed by cost-audit.mjs/retrospective; cost.md emission impossible — no filesystem
  access) + pinned/version-stamped mapping condition.
- **R1-11** — §5.5 gate condition 1: positive evidence the propagation greps RAN, not merely
  absence of findings.
- **R1-12** — §1.5 carried variant: double confirmation (two consecutive zero-mint rounds,
  consistent with §1.3's cited criterion) + red-health sanity term.
- **R1-13** — §4.6 item 1: batching output path corrected to an absolute path outside
  red/candidates/ and the recall index's watch surface (glob/convergence-count/double-index
  hazards enumerated).
- **R1-14** — §4.5 condition 6: name preflight + the two engine prompt sites (debate.js
  ll.216, 249) enumerated.
- **R1-16** — §3.5 + §0 row 3a: tally restated (1/3 unconditional + 1/3
  conditional-on-a-rejected-premise + 1/3 reject-with-binding-trigger); ratification argued
  explicitly from lane 3's grounds.
- **R1-17** — §1.5 registered prediction restated net-of-judge-round (~$15–20).
- **R1-18** — §3.3 default-to-docket priced (~$10/firing) + symmetric R3-2-class risk on
  dispute_responses named.
- **R1-19** — §6.4 item 3: three frontier misattributions (H1's stale pre-temper grades added,
  verified first-hand at frontier.md); [^FrontierH3] updated.
- **R1-20** — [^AdaptiveStability] softened to "stops at rounds ~4–7 losing ≈1% or less in
  the reported configurations."
- **R1-21** — [^BacklogLevers]: heartbeat re-attributed to backlog line 31 (STILL OPEN list).
- **R1-22** — [^WeakJudges] gloss corrected (debate beats consultancy across tasks; gains over
  direct QA task-dependent; stronger-debater effect more modest).
- **R1-23** — §2.3 final bullet softened (fix weight, not board size; series contradicts the
  round-0 clause).
- **R1-24** — §2.1 item 1: universe stated ("among boards that preceded another round").
- **R1-25** — §3.6: merge cost range corrected to $7.52–$13.56/round.
- **R1-26** — §2.3 + [^Sprt]: 36–75% band quoted from the arXiv source.
- **R1-27** — [^PropagationChains]: "5 chains in 5 rounds" re-sourced to blue-researcher.md
  l.14 / debate.js l.263; retrospective cited for the enumeration only (its own count: "four
  chains" + R4-4 = 5).
- **R1-28** — [^Backlog28d]: lever (4) corrected to tooling step-up; per-agent timeline as the
  closing note.
- **R1-29** — §4.6 item 5 + [^Votta]: Votta/EMSE-replication attribution split; MEDIUM grade
  and 403 noted.
- **R1-30** — §4.2 + §6.4 item 1(b) + [^CostAudit]: cache-write multiplier corrected to 5×
  (12.5 $/MTok is the absolute rate); cost.md finding-3 internal error logged.
- **R1-31** — [^AlreadyShipped]: PR #18 verify-first instruction discharged (merged at
  4a3801c, pre-pin).
- **R1-32** — §4.2 + §6.4 item 1(a): "smallest" → "second-smallest (6 open, vs round-4's 5)";
  both sites.
- **R1-33** — §2.4 + §6.1 + [^CostAudit]: red-lens Σ scoped to rounds 1–5, killed r6 spawn
  +$0.61 stated.
- **R1-34** — §4.3(c): "54KB monolith" → measured ~107KB findings.md at pin; §6.4 item 5 added
  (stale figure in friction #15 / backlog 31(g)).
- **R1-35** — (a) §2.4: $12/run recomputed, $18 upper bound unbundled (cheaper-merge term
  stated separately, unsized); (b) §6.1 retitled (item 6 marked HOLD, not ratified).

Propagation sweep run (grep over $53 / 12.5× / 54KB / smallest board / two misattributions /
30–50 / ~34 / six conditions / $10.60–$13.56 / $12–18 / rounds 2–8 / paywalled / exactly one
round): every surviving hit is inside a correction record; §6.2 interlock qualification
propagated from §3.5. Claim count ≈148 footnoted claim sites (132 + round-1 additions).

## Round 2 — all 18 gaps addressed (13 lead-CARRIED + 5 un-adjudicated); zero rebutted, zero risk-accepted

New first-hand verification performed at the blue seat this round (per the lead's "no
unverified mechanism claims" bar): live workflow journal composition (43 lines, started/result
only, zero log() — the production persistence contract, determined, not inferred);
cost-audit.mjs read in full (glob `agent-*.jsonl` l.28, zero journal refs); run-3 transcripts
tarball EXTRACTED AND MEASURED (46 members; per-round red-merge context decomposition);
hooks.json matcher `Write|Edit`; tests/simulator/harness.mjs "stub world / no live agents";
blue-researcher.md l.14 vs debate.js l.263; loop exits ll.192/236/256; judge prompt full-read +
contested filter; citationPasses formula.

- **R2-1** (supersedes R1-10) — §2.5 item 1 rewritten: harness log() verified
  console-ephemeral FIRST-HAND (this run's own live journal + run-3's + cost-audit.mjs read);
  durable sink specified — merge-seat append of one JSON line/round to git-tracked,
  name-preflighted `trajectories/board-telemetry.jsonl` (proven ledger `cat` route); consumers
  named: retrospective/next-run docket (demonstrated class) + cost-audit.mjs ONLY WITH a
  stated one-read extension. New footnote [^JournalCheck]. Propagated: §0 rows 1+2
  (instrumentation RATIFY now explicitly conditional on the sink), §1.5(1) (rewritten;
  "zero tokens" honesty-downgraded to near-zero), §3.3(v) (telemetry line, not log line),
  §6.1 item 5, §6.2 evidence-supply bullet (conditional clause).
- **R2-2** (supersedes R1-1, R1-35) — run-4 baseline restated with red-lens rescaled to the
  citationPasses-implied 6 seats/round (~+$2/round); §2.4 throttle saving recomputed at the
  live shape (~$18/run, ~10%; direction noted as strengthening the throttle blue rejects);
  §4.6 item 2 swept (6–8 read turns, 6 files minimum); §5.5 cost-at-stake swept; §6.1 header
  line swept; claim_count echoed into tracked artifacts (report §7: ~160 declared; here:
  ceil(160/40)=4 citation instances + 2 fixed = 6).
- **R2-3** — §4.2 corrected ("untracked at the pin but PRESENT in the working tree") AND the
  measurement RUN rather than deferred: tarball extracted, all five red-merge transcripts
  decomposed (table added to §4.2 — findings share 0.1%→~29–32%, ≈$12/run
  findings-attributable, ~$7–10/run sharding-addressable, blue/report.md the largest merge
  component from round 2, debate.md minor at the merge). §4 confidence MEDIUM→MEDIUM-HIGH
  with honest caveats; §8 Q2 marked ANSWERED-for-run-3 with the run-4 residual stated. New
  footnote [^MergeDecomposition] (method documented for reproduction).
- **R2-4** (supersedes R1-6) — §4.5 condition 7 rewritten: executor named per check (script =
  shape check: unset/empty fails structurally; vacuity beyond in-script reach — no independent
  counterpart structure, unlike the lineage throw; named post-hoc auditor =
  retrospective/next-run red over git-tracked shards; hook of the sc-recall-index class as the
  optional in-run fs-capable executor); "fails structurally, not silently" overclaim removed;
  folded into the §6.2 attestation ceiling.
- **R2-5** — §4.5 condition 2 extended to the judge (demanded-read MUST in the lead-judge
  prompt/agent: rulings on supersedes-descended gaps MUST read named ancestors' archive
  records); §6.1 sentence added pricing the judge read-profile change under 4a in both
  directions (unpriced benefit + unpriced cost, ~$10–13 assumes unsharded profile).
- **R2-6** (supersedes R1-2) — §3.3(v): one-round contest window before accepted deltas enter
  mass under actuation (or above-magnitude deltas auto-docket); "absorber in both directions"
  restated honestly (contested branch only; accepted branch = window + post-hoc audit).
  §3.3(vi) restated exit-agnostically: pending/held disputes auto-docket at ANY exit (PASS
  l.236 / deadlock l.256 / maxRounds l.192, verified first-hand).
- **R2-7** (supersedes R1-9) — §2.5 item 2: independent re-derivation clause (actuation review
  MUST re-derive found_by from preserved lens files at a seat other than red-merge and cite
  it); cross-referenced to §6.2 ceiling.
- **R2-8** — §6.1 money map: carried-ruling persistence inserted as ranked item 3 (marginal
  docket growth pricing per R2-11; gate-erosion closure; engine-successor scope stated);
  items renumbered 1–7.
- **R2-9** — §6.2: attestation-ceiling paragraph added (shape / consistency in-run; vacuity
  post-hoc only; named enforcement tier); cross-referenced from §4.5 conditions 5/7 context,
  §2.5 item 2, §3.3(v).
- **R2-10** (supersedes R1-4) — §1.2 both sites: ~$68 net (~$78 gross minus ~$10 judge round,
  matching §1.5's netting); "four findings" → "all ~21 rounds-3–5 mints (10+5+6), four of
  consequence."
- **R2-11** (supersedes R1-1) — §6.4 item 6: repeat spend repriced as marginal docket growth
  (full $10–13 only as sole docket member); trigger restated grade-change-only (script-visible)
  with evidence-change routed via successor-id lineage; carried-dispute traffic class named.
- **R2-12** (supersedes R1-5) — §2.2: subject restored ("But THE THROTTLE INPUT was
  measured-robust...").
- **R2-13** (supersedes R1-20) — [^AdaptiveStability]: "typically 4–7, full range 2–8 (three
  JudgeAnything rows 2, 2, 8), ≈1% or less (max −1.03%)"; correction record amended ("matched
  neither" was half-false — the 2–8 half matched; only <1% failed).
- **R2-14** (supersedes R1-26) — §2.3 gloss: "for symmetric error bounds" restored as the
  derivation condition; [^Sprt] quotation restored in full (second "by" reinstated).
- **R2-15** (supersedes R1-27) — [^PropagationChains]: verbatim home = blue-researcher.md l.14
  only; debate.js l.263 cited as paraphrase ("5 regressions in 5 rounds"); both lines re-read
  first-hand.
- **R2-16** (supersedes R1-13) — §4.6 item 1: mechanism corrected to "swept into qmd's index
  at the next `qmd update`" (Bash redirect never fires the Write|Edit hook — hooks.json read
  first-hand).
- **R2-17** (supersedes R1-14) — §4.5 condition 6: preflight MUST issue real Write calls from
  a live seat in the production harness; simulator confirmed a stub world (harness.mjs read
  first-hand) — simulator-only preflight passes vacuously.
- **R2-18** — §7: enumeration rewritten to name all FOUR access-blocked sources; "genuinely
  paywalled" → "inaccessible from the verifying seats (403; mechanism unshown)" at §2.2 and
  §7; [^DalalMallows] via-secondary disclosure added (Höhle 2016, red ledger l.63).

Propagation sweep run (grep over: log() / journal.jsonl / $12/run / ~8% / 5 lenses / five
lenses / genuinely paywalled / $78 / four findings / gitignored and absent / double-indexing /
5–8 read turns / zero tokens / matched neither / absorber-in-both-directions / unavailable):
every surviving hit is historical run-3 fact or inside a correction record. Claim count ≈160
footnoted claim sites (148 + round-2 additions: journal determination, decomposition
measurement, sink spec, rescaled figures). citationPasses input echo: ceil(160/40) = 4.

## Round 3 (2026-07-14) — response to red round-3 verdict (FAIL, 14 open gaps, all carried by the lead)

All 14 addressed; zero rebuttals, zero risk-accepts (six gaps are certain-likelihood and every
fix is trivial-to-low complexity below blue's own absorb threshold — the lead's calculus,
accepted). New first-hand work this round: the [^MergeDecomposition] instrument REBUILT and
COMMITTED (`trajectories/decompose-merge.mjs`), re-run against the pinned tarball; pinned
findings.md archive/open byte split derived by awk; per-round per-gap mass means recomputed
from §2.1's table; §4.5 condition 7 writer-capability audit run end-to-end.

- **R3-1** (MEDIUM, supersedes R2-3) — the big one, and the fix found MORE than red asked:
  parser committed as `trajectories/decompose-merge.mjs` and re-run. (a) Raw table REPRODUCES
  exactly (±1pt all five rounds; r5 blue 145.7KB; 61 turns matches cost.md). (b) The round-2
  DOLLAR series does not reproduce under the stated 4-bytes/token conversion: it (and lens
  3's Σ$12.4 recompute) priced cache-weighted BYTES as tokens — ≈4× overstatement. Strict
  series ≈$0.26/0.53/0.89/1.16 = Σ≈$2.8/run; ceiling cross-check vs cost.md's measured r5
  cache-read $7.87 confirms ($4.10 would be 52% of the round's whole cache spend at 28.8% of
  ingest — impossible with overhead unbucketed). Honest band $3–6/run findings-attributable.
  (c) Archive fraction DERIVED: 72.6% of pinned findings.md body bytes are
  superseded-preserved blocks (l.340–1364; awk line in footnote). Sharding-addressable
  restated ≈$2–4/run (was "$7–10 measured"). Propagated to ALL sites: §4.2 (rewritten with
  three-way derivation-status split), §4 confidence (MEDIUM-HIGH→ split HIGH-table /
  MEDIUM-dollars), §6.1 item 1 (rank now rests on judge-read + quality + growth direction,
  not dollar dominance; "comparable to batching" stated honestly), §8 Q2,
  [^MergeDecomposition] (correction record + retention assumption + derivation commands).
- **R3-2** (supersedes R2-4/R2-5) — §4.5 condition 7: demanded reads split into TWO named
  homes (red-merge → archive_spot_checks; judge → chain-ruling rationale naming ancestor
  records, git-tracked in ### LEAD — zero schema change, JUDGE_ENVELOPE schema quoted);
  writer-capability audit of every condition-7 observable run end-to-end and recorded inline
  per the lead's instruction.
- **R3-3** (supersedes R2-6) — §3.3(v): window operator NAMED via read-surface change
  (pending-entry deltas listed in the round's ### RED entry; any seat may docket within the
  window; unlisted deltas never enter mass); "or, equivalently" DROPPED (branches not
  equivalent); auto-docket threshold made CUMULATIVE per round with overflow batch-docketed
  mirroring clause (vii) (kills 5×threshold×rounds salami-slicing).
- **R3-4** (supersedes R2-3) — "largest every round from round 2" restated from the table as
  printed at BOTH sites (§4.2, §8 Q2): largest in rounds 2, 3, 5; round 4: candidates 33% /
  findings 32% > blue 20%.
- **R3-5** (supersedes R2-3) — §4.2 lane-1 comparison restated like-with-like: lane 1's
  20–30K ARCHIVE-share estimate vs measured 23K WHOLE-FILE r5 ingest and ~16–17K archive
  sub-fraction — upper half excluded, below the band's floor; "lane 1 ran high on both axes;
  §4.2 validates direction, not magnitudes." "Landing INSIDE" deleted.
- **R3-6** (supersedes R1-12/R1-17) — §1.5 registered prediction restated to test the arm AS
  HARDENED: two consecutive zero-above-floor-mint rounds preceding the final round +
  all-≤-MEDIUM low/trivial pre-final board + red-health held → ~$10 judge round substitutes
  the ~$25–30 final round (~$15–20 net) PROVIDED the judge disposed rather than carried;
  single-round total-mint trigger explicitly disavowed as settling the prediction.
- **R3-7** (supersedes R2-1) — §2.5 item 1: presence-check honesty stated; mass/board columns
  get the same independent re-derivation clause as found_by (actuation review recomputes a
  sample from the git-tracked findings record at a non-red-merge seat and cites it; telemetry
  = convenience copy, never evidence of record).
- **R3-8** (supersedes R2-1) — §2.5 item 1: prevention branch only ("pinned before first
  logged round; changed mapping starts a NEW series; stamps mark, they do not prevent");
  Q6 DECIDED this round per the lead's preference: realized EXCLUDED from open-gap mass;
  pinned mapping enumerated at §8 Q6; §2.5 carried-design detail marked resolved; §2.1's
  historical series ring-fenced as non-comparable.
- **R3-9** (supersedes R2-11) — §3.3 default-to-docket bullet repriced per §6.4 item 6's
  marginal-docket-growth rule (cross-referenced); §6.4 item 6's enum pointer corrected to the
  resolution-enum bullet (clause (v) contains no enum); terminal-exclusion cross-link added.
- **R3-10** (supersedes R2-2) — §2.4: "× 3 throttled rounds (the low-mass rounds 3–5 of
  §2.1's series)" — basis stated.
- **R3-11** (supersedes R2-17) — §4.5 condition 6: preflight writer pinned to the SAME SEAT
  CLASS as the future shard writer (first sharded run's red-merge writes both skeleton shards
  as its opening act), or guard seat-independence verified first-hand and cited.
- **R3-12** (supersedes R2-6) — §3.3(vi): terminal resolution set excludes `carried`
  (accepted-with-delta / rejected-recorded-as-contested / unresolved); exit-time dispatch
  added as a priced firing class (marginal per §6.4 item 6; full $10–13 only as sole member);
  §3.3 cost line updated.
- **R3-13** (supersedes R2-3) — §4.2: residual disclosed (named columns sum 86/80/91/91/96%;
  "other" = citation-ledger, red-auditor memory files, git/wc output, scratchpad staging —
  parser prints 13.6/19.5/9.0/8.6/4.0%); "four-file decomposition plus disclosed residual,
  not a complete partition."
- **R3-14** (new raise) — §2.1 item 4: "~5" → flat 4.4–6.0 band (4.9/5.9/4.4/6.0/5.2,
  recomputed from the table); honest band strengthens the item (no downward trend).

Propagation sweep run (grep over: 7–10 / ≈$12 / every round from round 2 / clear majority /
$5–15 band / landing INSIDE / pinned or version-stamped / ~$10/firing / × 3 rounds / stays
~5 / mint list is empty): every surviving hit is inside a correction record. Claim count ≈166
(§7 echo; ceil(166/40)=5 → capped at 4 by shipped min(4,…), red-lens shape stays 6 seats).
Round-4-bar compliance: every table-summary sentence in §4.2/§2.1 recomputed from the printed
table this round; every new design clause names its executor and a channel verified to carry
it (condition 7 writer audit; §3.3(v) operator surface; (vi) terminal set).
