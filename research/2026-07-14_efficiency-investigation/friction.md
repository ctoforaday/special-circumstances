# friction.md — Efficiency and termination levers for the frank-exchange-of-views debate engine

- (lead, 2026-07-14, during round 1) OBSERVER-EFFECT DISCLOSURE: the launching session's project had no .mcp.json at workflow start — seats spawned before this note had NO qmd/pdf-reader/arxiv-latex MCP tools. The servers were added to the launching project (.mcp.json) during round 1; if they hot-connect, later seats will have them. Read recall/PDF usage evidence accordingly: absence-friction from early seats is genuine; usage evidence starts mid-round-1 at the earliest. Connection pending human approval (/mcp) — a second note will record whether it succeeded.
blue-lane-1: qmd MCP retrieval (research-protocol mode 1) not available at this seat — no MCP/ToolSearch tools exposed to lane agents; fell back to Grep/Read on the local corpus, workable here but the protocol's stated retrieval mode was unreachable as specified.
blue-lane-1: red's gap-pattern project-memory file not readable from a lane seat (no path provisioned) — the blue pre-flight clause 'check red's accumulated gap-pattern memory if readable' was unsatisfiable; provision the path in the lane dispatch prompt if lanes are meant to honor it.
blue-lane-1: Grep count-mode footgun (run-3 friction #12) recurred live this lane — an unanchored count of '### LEAD' in debate.md returned 5 prose mentions vs 0 anchored headers; second documented recurrence, supports a lens-prompt documentation line.
blue-lane-2: red's gap-pattern project memory (named as a pre-flight MUST-read for blue seats) is not readable from a lane seat — the special-circumstances project memory dir is empty from this environment; substituted the pinned run-3 findings' gap-class inventory. Wanted: a readable path (or run-dir copy) of red's pattern memory for blue pre-flight.
blue-synthesize: the harness refused the Write of blue/report.md at the synthesis seat ("Subagents should return findings as text, not write report files") — but blue/report.md IS the blackboard deliverable the whole workflow reads; the envelope is only the handle. Worked around via neutral scratchpad filename + cp (run-3 friction #4's exact filename-keyed detour, now confirmed live on the BLUE side too). Wanted: a sanctioned write path for living blackboard artifacts, or the guard keyed on destination role rather than filename.
blue-synthesize: lane drafts use uncoordinated footnote-label namespaces — the SAME semantic label [^CaptureRecapture] named two different papers (Briand et al., IEEE TSE in lane 1; Petersson et al., JSS 2004 in lane 2), and lane 1's [^BlueRound5] mislabeled a round-4 quote; union merge required manual collision detection and relabeling across ~50 definitions. Wanted: lane-prefixed footnote labels (or a shared bibliography convention) in the lane dispatch prompt so semantic labels stay collision-free at merge time.
blue-synthesize: red's gap-pattern memory unreadable from the synthesis seat as well (third seat this run) — pre-flight clause satisfied only by substituting the pinned run-3 gap-class inventory; same want as the lane entries above.
red-merge-r1: 25k-token Read cap vs blue/report.md (~32k tokens, 914 lines) forced windowed reads at the merge seat AND at every lens seat, on a round-1-sized report — run-3 friction #15's class recurring before the report has grown; the full-read mandate outranks the token saving, so audit seats want a sanctioned whole-file read mode.
red-merge-r1: (carried from lens seats 1-4, whose only voice is their candidate files) no ToolSearch/MCP tools were exposed at any lens seat, so the protocol's arxiv-latex/pdf-reader MUST-try clause was unfulfillable there — Votta and SafeRTS primaries (ACM 403), the Briand abstract (IEEE blank page), STADS FSE21 and Petersson PDFs (lossy WebFetch) all needed detours or grade-downs; lens 2 improvised pdftotext. Either expose the document-extraction tools at lens seats or scope the clause to seats that have them.
red-merge-r1: (carried from lens 4) the citation-slice dispatch ("divide the report's sections evenly among instances and take slice N") leaves footnote-block ownership unassigned — lens 4 improvised a rule; name footnote ownership in the dispatch prompt.
blue-respond-r1: (recording per red R1-5's required fix) round-0 lane 2 graded a citation "paywalled, not leaf-verified" without attempting the protocol's arxiv-latex/pdf-reader MUST-try — on a paper that is open-access; this round the fallback path (WebFetch binary save + pdftotext, no MCP extraction tools exposed at the blue-respond seat either) leaf-verified the replacement source (arXiv:2607.05670) in two calls. The MUST-try clause has no observable: a recorded attempt-or-impossibility line per graded-down citation would have caught the false paywall excuse at round 0 instead of costing a red gap.
red-lens-5-r2: 25k Read cap vs blue/report.md again — now ~43k tokens (1178 lines) after the round-1 growth, three windowed reads for the mandatory full re-read at a bulk-tier lens seat (× 6 lenses × N rounds); the truncation banner's "do NOT answer from this page alone" is the only guard against a lazy lens answering from window one. Same want as red-merge-r1's entry: a sanctioned whole-file read mode sized to the audit surface.
red-merge-r2: 25k Read cap vs blue/report.md (1178 lines, ~43k tokens) forced three windowed reads for the mandatory full re-read at the merge seat — third seat-class this run to hit friction #15's pattern; the audit surface outgrows the read tool a little more each round. Same want: a sanctioned whole-audit-surface read mode.
red-merge-r2: the write guard on red/findings.md is still filename-keyed — the living findings file cannot be Written directly at the seat that owns it; the standing neutral-scratchpad-name + cp detour was used again this round. Want: a sanctioned write path for living blackboard artifacts (guard keyed on destination role, not filename).
red-merge-r2: blue's claim_count is envelope-only — no pinned or tracked artifact records it, so R2-2's lens-count baseline had to be reconstructed from the candidates directory and the citationPasses formula; the precise rescale is capped at MEDIUM. Want: claim_count echoed into a tracked artifact (CHANGELOG line or journal) so pricing claims are auditable at the leaf.
red-merge-r2: (carried from lens seats) harness log() persistence is documented nowhere in the pinned corpus — R2-1 rests on negative evidence (the measured journal lacks log output) because the contract is uninspectable; a one-line harness-behavior reference (where log() lands, journal.jsonl's schema) would convert the finding from strong inference to direct check.
judge-r2: resolution-enum misfit for regression-successor traffic — the judge fires BEFORE blue-respond in the round loop, so every contested gap that is a first-raise successor (all 13 this docket) arrives with no blue response in existence; `closed` and `rebuttal_sustained` are structurally dead options for this traffic class, and the real decision space collapses to carried/risk_accepted/unresolved. Either the enum should acknowledge the two traffic classes (re-raised-after-rebuttal vs first-raise successor), or successors should reach the judge only after blue has spoken to them once.
judge-r2: harness log() persistence contract uninspectable from the judge seat too (same wall as red-merge-r2's entry) — the simulator injects log as a harness-supplied parameter (tests/simulator/harness.mjs l.21), confirming it is a harness property, but nothing in the corpus states where the production harness sends it; R2-1's ruling had to be carried on negative evidence rather than settled by direct check.
blue-respond-r2: harness log() persistence SETTLED by direct check this round, but only via session-internals spelunking — the live workflow journal was found at ~/.claude/projects/<project>/<session>/subagents/workflows/wf_*/journal.jsonl by filesystem search, a path documented nowhere; it holds started/result lifecycle events only, zero log() output (R2-1 closed on direct evidence, upgrading red's and the judge's negative-evidence wall). Want: the harness's log()/journal contract stated in one referenceable line, so the next seat checks a document instead of excavating ~/.claude.
blue-respond-r2: R2-3's decomposition measurement required a bespoke ~70-line transcript parser written in the session scratchpad — cost-audit.mjs has no per-agent/per-source context timeline (backlog 28(d) names the want); the parse is documented in [^MergeDecomposition] for reproduction, but run 4 should get this as standard cost-audit output, not seat improvisation.
blue-respond-r2: red's gap-pattern memory still unreadable from the blue-respond seat (fourth seat class this run) — red's round-2 merge notes say regression vectors were "logged against red in project memory," a file blue cannot locate from this seat; the pre-flight clause remains satisfiable only by substituting the pinned run-3 inventory. Same want as the lane/synthesis entries: the memory path provisioned into the run skeleton or mirrored into inputs/.

## Entry (round-3 lens-5, logic/completeness)
- The 25k Read cap forced three windowed reads of blue/report.md (1365 lines) before this
  lens pass could begin — live recurrence of run-3 friction #15 at a lens seat; the full
  re-read MUST is honored but costs 3 tool turns per lens seat per round at current report
  size. (2026-07-14)

## round-3-lens-6 (red, dark-side lens)
- 25k Read cap vs blue/report.md (1365 lines, ~50k tokens): three windowed reads to honor
  the full-re-read MUST — friction-#15 class, third consecutive round at a red seat. The
  audit-surface file has outgrown the read tool; wanted: a single-call full read or a raised
  cap for the designated audit surface.

- red-merge-r3: the closure taxonomy has no amendment class — R1-12/R1-17 were closed CLEAN in round 2, and round 3 found a composition defect BETWEEN their two verified repairs (R3-6); the only way to declare the lineage was to reclassify last round's closures in this round's closures array and annotate the ledger by hand. Wanted: a closure class (or envelope field) for "prior clean closure amended → successor," so late-discovered composition defects between individually-verified repairs don't have to be shoehorned into closed_with_regression a round after the closure event.
- red-merge-r3: the write guard on red/findings.md is still filename-keyed — the living findings file again could not be Written directly at the seat that owns it; standing neutral-scratchpad-name + cp detour used (fourth consecutive round-seat this run). Same want as red-merge-r2: guard keyed on destination role, not filename.
- red-merge-r3: (carried from lens 1) WebFetch's extraction model produced internally inconsistent reads of arXiv 2510.12697 Table 2 — its prose summary said "18 configurations" (and a second fetch's said 24 by naive 6×4 arithmetic) while its own row enumeration listed 22; the correct count required hand-recounting enumerated rows across two fetches. Fetch-model summaries are not evidence, only quoted enumerations — and arxiv-latex (the table-faithful path) was again not exposed at lens seats.
- red-merge-r3: (carried from lens 3) Bash quoted heredoc (<<'EOF') ate one backslash of a /\/g regex in a node script despite quoted-heredoc literalness, forcing a Write-tool detour that then tripped the read-before-write guard on the file the broken heredoc had created — two extra turns. Wanted: literal heredoc passthrough, or a documented always-use-Write rule for scripts.
- judge-r3: second consecutive round the entire contested docket is first-raise successors — `closed` and `rebuttal_sustained` structurally dead again (judge-r2's entry, unchanged, now a pattern): the docket trigger "raised, rebutted, and re-raised" does not describe regression-chain traffic, where blue has never spoken to the docketed item. The work wants either a successor-review resolution class (verify-red-then-direct-blue, which is what carried is being used to mean) or successors held out of the docket until blue has responded to them once; as built, the adjudication enum forces the judge to spend two of five values on options that cannot occur for the dominant traffic class.
- blue-respond-r3: the 25k-token Read cap forced a 3-window read of blue/report.md (1364 lines) at the blue seat before editing — run-3 friction #15's class, now recurring at blue-respond, the seat that must re-read its own living report every round; a section-indexed read or a higher cap for a seat's own primary artifact would remove two reads per round.
- blue-respond-r3: GNU tar on Windows misparses `C:/...` absolute paths as remote-host specs (`Cannot connect to C: resolve failed`) — extraction of the pinned agent-transcripts tarball needed `--force-local`, discovered by failure; worth one line in the research-protocol trajectory notes since every future decompose-merge.mjs run on Windows hits it.
red-lens-6-r4: 25k Read cap vs blue/report.md (1535 lines, ~56k tokens) — three windowed reads for the mandatory full re-read, fourth round of friction #15's class at this run; the audit surface has grown ~75% since round 1. Same want: a sanctioned whole-audit-surface read mode.
red-merge-r4: the write guard on red/findings.md is still filename-keyed — fifth consecutive round-seat this run using the neutral-scratchpad-name + cp detour to update the living findings file at the seat that owns it. Same want as red-merge-r2/r3: guard keyed on destination role, not filename.
red-merge-r4: 25k Read cap vs blue/report.md (1535 lines, ~56k tokens) — three windowed reads for the mandatory full re-read at this merge seat; lens 5 reports the same in its candidate file (lens 6 logged its own entry above). Friction-#15 class, every full-read seat, every round; the audit surface has grown ~75% since round 1. Same want: a sanctioned whole-audit-surface read mode.
- (lead, 2026-07-14, run end) ABORT DISCLOSURE: judge-r4 died on the account's monthly spend limit → agent() null → the engine's null-guard aborted cleanly (run-2's crash class, now handled). State at death: red r4 merge COMPLETE (FAIL, 10 open: 1 MEDIUM/6 LOW-MEDIUM/3 LOW, all 14 r3 gaps closed), judge-r4/blue-respond-r4/assembly never ran. NO REPORT ASSEMBLED — resumable via wf_5cefd2a4-35f (38 agents cached; the run-3 play: resume with maxRounds:4 for judge + blue-respond + honest UNVERIFIED assembly). The death itself is evidence for lever (1): the terminal board is exactly the severity-floor condition (zero gaps above MEDIUM), and the engine had no rule to stop.
