# round 5 — lens 2 (leaf-node citation verification; slice 2: §2 + §3 + referenced footnotes)

Full report re-read: 5 consecutive Read windows, lines 1–2159 (whole document, no diff shortcut).
Round parity: CHANGELOG Round 4 is the latest revision; report header carries the round-4 delta block — round 5 audits the round-4 revision. In-slice round-4 edits: §2.3 (R4-7/R4-9), §3.4 (R4-1/R4-10/R4-14/R4-7).

## Re-fetch dues executed (>2 rounds since last leaf verification, living sources)

| Claim (section) | Reference | Result | Confidence |
|---|---|---|---|
| /loop session-scoped; fires only while session open+idle; recurring 7-day expiry; "No catch-up for missed fires" (§3.3.3) | [^ScheduledTasks] live WebFetch | all verbatim on page ("Tasks are session-scoped…"; "Recurring tasks automatically expire 7 days after creation"; "No catch-up for missed fires") | HIGH (zero drift) |
| disable-model-invocation scheduled fires reach Claude as plain text (§2.4, §0 trampoline premise) | [^ScheduledTasks] live | "a scheduled fire only runs skills that Claude is allowed to invoke on its own… skills marked `disable-model-invocation: true` … reach Claude as plain text instead of executing" (v2.1.196 marker) | HIGH |
| Desktop task: per-task permission config, machine on, local files, 1-min minimum (§3.4 rung 2) | [^ScheduledTasks] live compare table | "Permission prompts: Configurable per task"; machine on Yes; local files Yes | HIGH |
| Rung-3 set: autonomous no-prompts; claude/-branch push default; fresh clone from default branch; ≥1h minimum; connectors default-included; identity attribution; claude.ai-login-only (API accounts unsupported); daily cap + "rejected until the window resets"; green-status warning; research-preview volatility (§3.4 rung 3 + caveats, §5.1 auth leg) | [^RoutinesDocs] live WebFetch | every claim verbatim on page, incl. "there is no permission-mode picker and no approval prompts during a run"; "By default, Claude can only push to branches prefixed with `claude/`"; "The minimum interval is one hour"; "all of your currently connected connectors are included by default"; "API accounts aren't supported for routines"; "rejected until the window resets"; "A green status … does not mean the task in your prompt succeeded"; "Behavior, limits, and the API surface may change" | HIGH (zero drift) |
| systemd `Persistent=` catch-up semantics (§3.4 missed-run) | [^MissedRun] man7.org systemd.timer live | "the time when the service unit was last triggered is stored on disk … triggered immediately if it would have been triggered at least once during the time when the timer was inactive" | HIGH |
| anacron interval-since-last-run; no continuous-running assumption (§3.4) | [^MissedRun] man7.org anacron(8) live | "checks whether this job has been executed in the last n days"; "does not assume that the machine is running continuously" | HIGH |
| Task Scheduler missed-start catch-up (§3.4) | [^MissedRun] learn.microsoft.com | r1-cited settings URL now 404 (attempt 1: task-scheduler-2-settings → 404); rerouted to TaskSettings.StartWhenAvailable property page: "the Task Scheduler can start the task at any time after its scheduled time has passed" — semantics exact; the report's quoted UI string ("Run task as soon as possible after a scheduled start is missed") is the MMC checkbox label, not on this page | HIGH on mechanism / MEDIUM on exact-string leaf (source page moved — live-source-drift nit, no gap: design conclusion untouched) |
| GHA schedule delayed at high load / dropped under saturation / 5-min minimum (§3.4 rung 4) | [^GhaSchedule] docs.github.com live | "can be delayed during periods of high loads … If the load is sufficiently high enough, some queued jobs may be dropped"; "shortest interval … once every 5 minutes" | HIGH |
| Web sessions: isolated Anthropic-managed VM per session; credentials outside the sandbox (§3.4) | [^WebSandbox] code.claude.com/docs/en/sandbox-environments live | "runs each session in an isolated, Anthropic-managed virtual machine. A network proxy enforces a default allowlist, and a separate proxy holds your GitHub token outside the sandbox" | HIGH (zero drift) |
| #837 status (§3.3.1) | [^SlashHeadlessIssues] WebFetch issue page | CLOSED confirmed live; close REASON not rendered on fetched page (attempt: WebFetch shows status only; gh CLI classifier-blocked from lens seats ×3 per r3 record) — COMPLETED reason carried from r1 gh leaf; closed issues low-volatility | HIGH (status live + reason r1-leaf) |
| #14246 status (§3.3.1) | [^SlashHeadlessIssues] WebFetch issue page | "Closed as Duplicate" — supersession story holds | HIGH |
| IdeaStudy: more novel p<0.05; feasibility slightly weaker; self-eval + diversity failures (§2.1) | [^IdeaStudy] arXiv abs 2409.04109 live | all three verbatim in abstract; "human re-ranking improved outcomes" sub-claim not in abstract — carried on the r1 /html body leaf (immutable arXiv version) | HIGH (rerank sub-claim HIGH-via-r1-html) |
| qmd ladder figures: 973MB daemon-only; bare-CLI hybrid 36.3s vs daemon lex 2.9s; :8181/mcp, /health, PID file (§3.3.2b) | [^QmdDaemon] git show 7bc501e:ideas/backlog.md line 34 (pin spot-check) | all verbatim at the pin | HIGH |
| PortPlan quotes: "human approves each step" (line 287); "always available, scheduling always human-opt-in" (line 357); Phase-4 verify "touches only research/+ideas/" (line 331) (§2.4, §3.4 rung 0) | [^PortPlan] AgentOrange working tree | HEAD still 6df52af, file clean in git status, all three lines present | HIGH-content / snapshot-grade (adjudicated R1-7 pin defect standing) |

## Internal recomputes and repair verification (round-4 edits in-slice)

- **R4-10 cap/HALT arithmetic (§3.4) — RECOMPUTED, CORRECT**: k=3 per dir ⇒ deaths at nights 4 and 8; dir 3 runs nights 9–10; $50 cap after 10×$5 ⇒ skip from ~night 11; dir 3's remaining 2 resumes land month-2 nights 1–2 ⇒ third death + HALT ≈ $10 into month 2; worst case $60 across two months. Blue's "deaths nights 4 and 8 … ~$5–10 into month 2 … ~$55–60" all check. HIGH.
- **R4-7 repair (qmd-degrade readers) — FAITHFUL, closes r4 L2-F1**: §2.3 confidence field now carries the "recall-degraded (qmd down…)" labeling clause; §3.4 doctor line carries the degrade-streak term; §2.2 step 0's "named readers" sentence now has both readers specified. HIGH.
- **R4-14 flag custody (§3.4) — CONSISTENT**: four states all print (enabled-healthy → last-check timestamp per R3-9; enabled-stale → warning; flag-off → disabled token; flag-absent → anomaly token). Spec-level, internally complete. HIGH (internal).
- **R4-1 §3.4 containment-polarity paragraph — CONSISTENT** with §1.5 R2-6 corroboration gate and OQ24; trampoline premise (`disable-model-invocation` plain-text fires) leaf-re-verified today at [^ScheduledTasks]. HIGH. BUT see L2-F1 below.
- **§2.4 DGM honesty clause** (r3-verified repair) and **§2.2 step 4 execution locus** (r3-verified at plugin cache + pin): carried, sections unchanged r4.

## Findings

### L2-F1 (LOW-MEDIUM) — §3.4 ladder table rung-0 cell not updated for R4-1: stale "command markdown IS the payload" claim
- **Location:** §3.4 "The scheduling ladder", table row 0: "the /self-improve command markdown is the wrapper's phase-1 prompt payload in EVERY mode, not a standalone entry point (R3-2)".
- **Problem:** R4-1 moved the loop payload OUT of the command markdown: §0 tree now says "the full loop PAYLOAD lives in the wrapper's phase-1 prompt (skills/continuous-learning), NOT in commands/ … `self-improve.md` is a thin trampoline", and §3.4's own R4-1 paragraph ~150 lines below the table repeats it ("the payload no longer lives in `commands/`"). The rung-0 cell still asserts the round-3 shape — the command markdown as payload — which is now false as written and contradicts the same section.
- **Why it matters:** the ladder table is the artifact scheduling.md ships; a builder reading row 0 re-creates a payload-carrying invocable command — the exact scheduled-fire/paste vector R4-1 closed. CHANGELOG R4-1's edit list (§0 tree, §3.4 containment paragraph, §6 row 10, OQ24) never included the ladder cell, and the round-4 propagation greps carry no token covering "phase-1 prompt payload". Incomplete-repair/body-lags-repair class (known pattern, new instance).
- **Grading:** likelihood Medium (the table is the high-traffic summary artifact); impact Low-Medium (contradiction is self-correcting for a careful reader, vector-reopening for a skimming one); complexity Trivial (rewrite one cell: "the command is a thin trampoline (R4-1); the payload is the wrapper's phase-1 prompt sourced from the skill file").
- **Required fix:** update the rung-0 cell to the R4-1 shape; add "phase-1 prompt payload" to the propagation-grep list.

### L2-F2 (LOW) — §2.3 status enum's flag/status domain not total against the R4-9 root invariant
- **Location:** §2.3 idea-stub contract, `status:` field: "open | stale | graduation-queued | queued-stale | graduated | rejected | rejected-recurring".
- **Problem:** of the four R4-9 re-surface flags, three (`stale`, `queued-stale`, `rejected-recurring`) are enum values, but `regression` — the graduated state's re-surface flag ("graduated re-enters flagged `regression` on class recurrence", §1.4/§2.3/§6 row 3) — is not, and no rule states what the graduated stub's status becomes (stays `graduated`? converts?) or who sets `rejected-recurring` on the stub vs the docket row. Pinned-mapping-not-total class: a builder implementing the enum has an unmapped token on exactly the terminal state R4-9 was minted to govern.
- **Grading:** likelihood Low-Medium (bites at build time, not at read time); impact Low (behavior recoverable from §1.4 prose); complexity Trivial (one clause: e.g. "regression is a docket flag only; the stub stays `graduated` and the regression entry may mint a NEW stub" — or add `regression` to the enum).
- **Required fix:** one sentence pinning `regression`'s domain (docket-flag vs status) and the status-setter for `rejected-recurring`.

## Carried without re-fetch (within the ledger's carry rules)
[^HeadlessDocs] §3.2 set (r3 live, zero drift, 2 rounds); [^CliReference] flags + --input-format (r3 --help + r4 lens-3 live); [^PermissionsDoc] §3.2 carve-out sentence (r4 three-way live concurrence); [^McpHeadlessBugs] #76239/#68375 OPEN + #32191 CLOSED-dup (r4 live gh, same-day); [^WindowsHang] #66395 (r3 live); [^ResearchCommand]/[^Backlog]/[^SmokeRecord]/[^EfficiencyPlan]/[^QmdFallback]/[^IdeasCorpus] (pin-immutable @7bc501e); [^Reflexion] (r4 live); [^DGM] html (r3 live); [^DGMSakana] (r4 live); [^HeadlessProbe] (MEDIUM, disposition-of-record: re-run at build).

## Friction
- learn.microsoft.com: the r1-era Task Scheduler settings URL 404s; the quoted MMC UI string is no longer findable at a stable doc leaf (semantics confirmed at the StartWhenAvailable API property page). Suggest the footnote cite the API property page.
- GitHub issue close REASONS are not rendered in WebFetch's markdown conversion (status yes, reason no); gh CLI remains classifier-blocked from lens seats — close-reason verification still leans on prior-round gh leaves for #837.

## Lens verdict input
Slice citations: zero refuted leaves this round; all re-fetch dues executed with zero drift on ten living-source claim sets. Two findings, both repair-lag/spec-totality class (LOW-MEDIUM + LOW), both trivial fixes. Nothing in-slice blocks.
