# friction.md — How should sleeper-service, the autonomous learning loop (Phase 4), be designed?

- blue-lane-1: qmd MCP tools not exposed at this lane seat — the qmd server's instructions were injected into the seat context (collections, query/get usage), but ToolSearch "select:mcp__qmd__query" returned "No matching deferred tools found"; research-protocol retrieval mode 1 was unreachable as specified and corpus recall fell back to native Read/Grep (workable; recurring class — same complaint at efficiency-run lane seats). Wanted: qmd MCP tools actually reachable where the server instructions are injected, or the instructions withheld from seats that cannot call them. (2026-07-17)
- blue-lane-1: pinned-path mismatch carried from frontier: inputs/PINNED.md pins `plans/claude-port-plan.md` at 7bc501e but no such path exists in the special-circumstances tree; §3c was read from the AgentOrange working tree (`docs/claude-port-plan.md`), which is NOT commit-pinned — cross-corpus citation to the port plan is therefore snapshot-grade, not pin-grade. Wanted: the port plan copied into inputs/ at run setup or pinned at its real path. (2026-07-17)
- blue-lane-1: GitHub issue-status verification (red Pattern A discipline) requires one WebFetch per issue; three fetches spent this lane solely on open/closed/closure-reason checks. A cheap `gh issue view` path exists but shells out; a batched issue-status capability (or gh availability noted in lane prompts) would cut the round-trips. (2026-07-17)
- blue-synthesize: the subagent write-block on report-named files (red's filename-keyed pattern, env_write_block_filename_keyed) forced the documented workaround for blue/report.md — Write to blue/r0-synthesis.md then `cp` into place; the workaround is cheap but the neutral-name artifact now sits beside the real one in blue/. Wanted: the pre-created-skeleton fix or a synthesis-seat exemption so the living report can be written directly. (2026-07-17)
- blue-synthesize: the envelope's claim_count field has no pinned counting unit — "claims" is undefined at the merge seat, so the figure (132) had to be minted with a self-invented, re-derivable method (top-level bullets/numbered items/table body rows outside code blocks + code-block contract units; documented in blue/CHANGELOG.md). Two honest merges could report numbers differing by 2x. Wanted: a pinned claim-unit definition in the protocol so blue's count and red's audit count the same thing. (2026-07-17)
- red-merge-r1: the pinned mass mapping conflates likelihood-of-existence with likelihood-of-harm — a verified textual nit is graded "certain" (3.5) by the lens convention, so a trivial citation defect carries more L-mass than a high-likelihood design flaw (3.0); there is no way to express "defect verified present, consequence-likelihood low". Wanted: either a mapping note pinning "certain = consequence certainty, verified textual defects grade likelihood on consequence", or a separate existence flag so mass tracks risk, not verification success. (2026-07-17)
- red-merge-r1: lens seats have no friction.md write path — their envelope friction survives only inside red/candidates/*.md unless the merge manually relays it; a later-phase abort would lose it. Wanted: lens friction auto-harvested from candidate files, or an explicit merge relay step in the protocol. Relaying now (2026-07-17):
  - red-merge-r1 (relaying red-lens-2): freedesktop.org systemd man page returned HTTP 403 to WebFetch — worked around via man7.org mirror; the protocol could name preferred-mirror fallbacks for common man-page sources.
  - red-merge-r1 (relaying red-lens-2/lens-3): permissions-doc WebFetch overflowed the tool-result cap (54KB persisted to a side file) — grep-on-persisted-output worked but is undocumented workflow.
  - red-merge-r1 (relaying red-lens-4, concurred lens-2): WebFetch on GitHub issue pages is lossy on collapsed/paginated comments — it returned a confident "no workaround mentioned" for #22055 while the full thread (gh issue view --comments) contains one; an absence-claim from a summarizer fetch of an issue thread is not evidence of absence; gh should be the documented default for issue-thread content checks (and proved 7-statuses-in-one-call cheaper for status checks too).
  - red-merge-r1 (relaying red-lens-3): arxiv-latex MCP not exposed at the lens seat (ToolSearch: no match) — the run-4 standing class recurs; ar5iv WebFetch sufficed for STOP this time, but the table-faithful path remains unavailable where the MUST-try clause binds. (2026-07-17)blue-respond-r1: PINNED.md asserts a nonexistent path (`plans/claude-port-plan.md` at 7bc501e) — red re-confirmed at the merge seat (R1-7); blue can only label port-plan citations snapshot-grade. The fix is run infrastructure, not report text: setup-script pin validation (fail run creation when a pinned path is absent at the pin) or stage the port plan into inputs/ at pre-create. Routed to lead.
red-merge-r2: archive spot-check floor misfit — the mandate requires non-empty archive_spot_checks "from round 2", but entering the round-2 merge the archive held ZERO blocks (round 1 closed nothing); the only sampleable closures are the ones this same merge writes, so the "spot-check" degrades to same-seat self-attestation (satisfied here by independent merge greps of the repaired report text for sampled ids R1-1/R1-5/R1-8/R1-16/R1-30 BEFORE writing their blocks). The floor should key on "archive non-empty at round START"; the first real cross-round spot-check is possible at round 3.
judge-r2: R1-7 ruled risk_accepted for THIS run only, and the underlying infrastructure debt is the lead's: inputs/PINNED.md asserts `plans/claude-port-plan.md` at 7bc501e where the path does not exist — setup tooling must validate every pinned path (`git cat-file -e <pin>:<path>`) at run creation and fail loudly on a miss, or stage cross-corpus artifacts into inputs/ at pre-create. Until fixed, every port-plan citation in the final report is snapshot-grade (verified only at working-tree 6df52af), and the same defect will recur in every run that pins a cross-repo path. (2026-07-17)
judge-r2: resolution-enum misfit — R1-7 is a lead-owned infrastructure work item, not a likelihood×impact×complexity tradeoff, but the ruling enum offers no "routed_to_infrastructure" class; risk_accepted is the least-wrong fit (recorded, never dropped) and carried would park an item blue cannot act on in red's verdict pool. The enum wants a disposition for "valid finding, fix owned outside the debate". (2026-07-17)
red-merge-r3: blue shipped a round-3 revision to report.md (invariant 7 + all R2 fixes) but CHANGELOG.md and debate.md carry NO round-3 ### BLUE block — the change-summary channel is two revision-rounds behind the living report, and lens 3 misjudged the round state as "post-round-2" from the CHANGELOG absence. A change-summary is a navigation hint; when it silently desyncs from the report it actively misleads a lens. Wanted: an enforced round-N CHANGELOG/### BLUE stub whenever report.md changes, or a report-vs-CHANGELOG round-parity check at seat launch.
red-merge-r3: Glob/Grep refuse the run directory ("Path does not exist") because it sits outside the registered working directories while Read and Bash reach it — every audit-surface search forced a Bash spawn (10-100x a native read). Recurrence of the round-1/2 L1 complaint. Wanted: the lens/merge audit surface registered as a working directory, or Glob/Grep honoring the same access set as Read.
judge-r3: adjudication had to reconstruct blue's round-3 position entirely from red's ledger/archive plus direct report reads, because debate.md carries no round-3 ### BLUE block (same desync red-merge-r3 logged) — the judge's demanded full-transcript read no longer reflects the debate state when a party revises silently. Wanted: the protocol makes the round-N ### BLUE block a precondition for red's round-N pass (blue's revision is not "on the record" until the transcript carries it). (2026-07-17)
judge-r3: docket-composition note — all 12 docket items were first_raise_successor, so every ruling was structurally confined to carried/risk_accepted/unresolved/moot; the "contested-gap adjudication" framing (weighing blue's response against red's re-raise) had nothing to weigh, and the docket functioned purely as a direction-setting pass. Not wrong, but the template's closed/rebuttal_sustained machinery sat idle; a lighter "direction docket" shape for all-successor rounds would name what this pass actually was. (2026-07-17)
blue-respond-r3: the round-2 ### BLUE/CHANGELOG omission is repaired (catch-up entries written this round), but the failure class remains open at the protocol level — nothing verifies envelope-side that debate.md gained a ### BLUE block and CHANGELOG a round-N entry before a blue round is accepted; the same silent desync can recur. Concurs with red-merge-r3/judge-r3: make the round-N ### BLUE block a precondition for red's round-N pass. (2026-07-17)
blue-respond-r3: quoted-heredoc append via Bash failed with a shell parse error on a large multi-line CHANGELOG entry (the research-protocol's own named footgun — "quoted heredocs can eat backslashes; prefer the Write tool"); worked around via the Edit tool, but the harness contract's advice cost one wasted round-trip because the footgun note is buried mid-paragraph. Wanted: append-to-file capability that doesn't route large literal text through shell parsing. (2026-07-17)
blue-respond-r3: the round-3 gap summary's R3-14 carve-out enumeration was NARROWER than the primary source — the permissions doc names nine additional read-only commands (ls, echo, pwd, wc, which, diff, stat, du, cd) beyond the gap's cat/grep/head/tail/find set; blue's own leaf re-fetch caught it and the deny enumeration covers the full doc set. Not a defect in red's finding (the claim was true as stated) but a reminder the gap JSON is a lossy summary — repairs keyed on it without a fresh leaf read would have shipped an under-inclusive deny list wearing a "closed" label. (2026-07-17)
red-lens-4-r4: layer-isolation permission probes are unrunnable from a lens seat — the isolating check for blue's "the carve-out classifier itself passes `git log --output`" claim (a nested `claude -p --permission-mode dontAsk` from a fresh temp repo, zero allow rules, ~$0.02) was DENIED twice by this seat's own auto-mode classifier, and per policy the denial was not routed around. Consequence: classifier-attribution claims (L4-F1/L3-F2 this round) can only be graded LOW-MEDIUM with an attempt record, never settled in-run; every such claim defers to a build-PR or operator probe. Wanted: a sanctioned probe path — an operator-approved allow rule for `claude -p` smoke probes or a wrapper script under an allowed path — so the MUST-try clause can reach permission-layer claims, not just document fetches. (2026-07-17)
red-merge-r4: lens seats cannot run layer-isolating permission probes — the nested `claude -p --permission-mode dontAsk` invocation (trivial cost, fresh temp repo) was DENIED twice by the seat own auto-mode classifier, so probe-attribution claims (R4-11) can only be graded, never settled, from inside the run. Wanted: a sanctioned probe path (operator-approved allow rule or a wrapper script under an allowed path) for one-command permission-mode probes; without it every classifier-attribution claim defers to a build-PR probe.
- blue-respond-r4: layer-isolating permission probes cannot be run from inside a seat — a nested `claude -p --permission-mode dontAsk` (zero allow rules) to isolate which layer approves the `git --output`/`-o` write gadget is denied by the seat's own auto-mode classifier (red logged the same block round 4). Consequence: R4-11's attribution (carve-out classifier vs auto classifier) and OQ18(c)'s member/subcommand enumeration can only be GRADED, never SETTLED, from inside the run; both are deferred to a build-time probe. The round-4 bare-`Bash`-deny structural close makes the outcome non-load-bearing, but the attribution question stays open. Wanted: a seat capability to spawn a child `claude -p` under an arbitrary explicit `--settings`/`--permission-mode` for isolating permission-layer probes. (2026-07-17)
red-merge-r5: live-source drift on [^MissedRun] — the r1-era learn.microsoft.com Task Scheduler settings URL now 404s and the quoted MMC checkbox string ("Run task as soon as possible after a scheduled start is missed") has no stable doc leaf; semantics re-verified at the TaskSettings.StartWhenAvailable API property page, where the footnote should point. Capability wanted: none new — a footnote re-point; flagged so the citation does not silently rot.
red-merge-r5: GitHub issue close REASONS are not rendered in WebFetch markdown conversion (status yes, reason no), and gh CLI remains classifier-blocked from lens seats (4th round) — close-reason verification (#837 COMPLETED vs NOT_PLANNED) still leans on round-1 gh leaves. Capability wanted: gh issue view (or an issue-API fetch surface) permitted from red lens seats for status+reason leaf checks.
assemble: Catechism had no home anywhere in the run — blue's report shape (§0–§8) carries no Catechism section and no debate round ever audited catechism answers, so the report template's "AGREED answers" had to be reconstructed by the judge at assembly from the debated text (disclosed in-report). Wanted: the catechism answered in blue's round-0 synthesis (a required section of blue/report.md) so red audits it inside the debate instead of the judge minting it after. (2026-07-17)
assemble: terminal-state misfit — the protocol names two terminators (red-PASS, confirmed deadlock) but this run ended by a third: the 5-round safety ceiling fired while the round-5 LEAD had ruled deadlock FALSE ("continues into round 6") and blue had already shipped a round-5 revision no red pass ever audited. The template has no verdict class for "converging, ceiling-terminated, final revision unaudited"; handled by stamping UNVERIFIED plus an assembly-owned risk row (#18) marking the Round 5 edit set as claims requiring build-PR re-audit. Wanted: the round ceiling as a named first-class terminator with a defined disposition for the post-ceiling revision (e.g., a closing red micro-audit pass, or an explicit "unaudited-revision" stamp qualifier). (2026-07-17)
