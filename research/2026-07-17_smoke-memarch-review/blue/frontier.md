# blue frontier — Review the recommendations of the memory-architecture report against what has shipped since: implemented, superseded, or still open?

H1: The blocking security recommendations (taint-boundary allowlist inversion, git-ignore committed projections, injection screening) were prioritized and closed in the Phase-0/Phase-1 window, leaving the non-blocking consolidation machinery (nightly `/dream`, decay tables, dedup at scale) still open.

H2: The memory-architecture build was deferred or shelved; native Auto Dream consolidation (flag-gated, rolling out) narrowed the defensible bespoke remit to just the differential (cross-project global store + skill promotion), and the project adopted the subset rather than commit to the full phased plan.

H3: The typed-concept differential (global git repo, schema, human-gated promotion) shipped in a minimal form; the full lifecycle machinery (event/clock-hybrid triggering, per-concept decay windows, dedup via semantic recall) remains unimplemented as lower-priority layers.

H4: The recommendation set was superseded mid-way by changes in native Claude Code machinery (Auto Dream availability, transparent native `memory:` support); the Phase-2/3 overlap with Auto Dream is still unresolved (two-writer conflict), blocking ship of the project-store feature.

H5: The build proceeded under the re-scoped margin (load-bearing differentiators only; nice-to-have widenings gated behind blockers); the security ingest gates (mit.1-2) shipped; the clone-ratification marker and provenance-of-content taxonomy shipped; but the Phase-4 `/ingest` pipeline and Phase-5 headless schedule remain open.
