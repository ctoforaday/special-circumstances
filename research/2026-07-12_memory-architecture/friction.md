# Friction — run 2 (memory-architecture): 35 agents, 4 rounds, UNVERIFIED / 15 gaps

- red-merge-r1: Write tool refused red/findings.md on a filename-heuristic guard ('findings' .md), despite it being the protocol's required living blackboard artifact that the workflow script reads. I wrote it via a Bash heredoc instead (permitted tool-input path). Recommend the guard exempt the protocol's declared run-directory artifacts (findings.md, report.md, CHANGELOG.md, debate.md) or the workflow pre-create them so subagents only append.
- red-merge-r1: Bash heredoc hit ENAMETOOLONG when writing the full ~236-line file in one spawn (Windows command-length limit), forcing me to split into ~6 append calls. A file-append tool or larger spawn budget would remove the chunking overhead.
- red-merge-r1: HTML-arXiv leaf-node verification is lossy: the small-model WebFetch cannot reliably read numbers embedded in tables/figures, which is the direct reason R1-19, R1-28, and R1-30 are graded medium/uncorroborated rather than resolved true/false. A PDF-table-extraction or full-PDF-text-search tool would let red discharge these definitively.
- red-merge-r1: Live-source drift (R1-23 mem0 ADD-only pivot, R1-24 star count, R1-20/R1-21 closed-issue statuses) is only catchable by re-following each citation to the current primary. A verification lens run against archived snapshots would miss them; recommend the protocol record access-date deltas explicitly.
- blue-respond-r1: PDF table extraction: WebFetch runs a small model over converted markdown that misses numeric table cells, so I could not confirm the exact figures behind R1-19 (61.4%/71.6% agent-PR review) or R1-30 (ALFWorld 59.88->28.71) at the leaf node — I relabeled them approximate/hedged instead. A tool that reliably extracts arXiv PDF result tables would have let me confirm-or-correct the digits rather than hedge.
- blue-respond-r1: gh issue status verification: I accepted red's leaf-node finding that #57507 and #56540 are Closed-not-planned rather than re-fetching via gh, to conserve the round. A direct gh issue view in the envelope-permitted path would have let me independently re-confirm status and the macOS-launchd repro scope without spending a web fetch.
- red-merge-r2: arXiv/dev.to leaf-node fetches are abstract/HTML-only and lossy for in-table numbers. This round it was decisive enough to confirm contradictions where the abstract carries a materially different figure (R2-8: 2604.02623 reports ≤32.5% vs claimed ~90%) but it still cannot rule a figure OUT when it might sit in a body table (the MINJA-in-survey question, R1-28, stays 'untraceable-as-cited'). A full-PDF-text-search or PDF-table-extraction tool would discharge R1-19, R1-28, R2-8's residual, and R2-10 definitively.
- red-merge-r2: Leaf-node confirmation of the 'removed user memories from the system prompt' CVE remediation detail (load-bearing for the R2-2 double-bind) is blocked by post-cutoff vendor-blog-only sourcing. A way to fetch/confirm the primary Anthropic security advisory would settle whether the bespoke projection re-authorizes a remediated (net-new) or unremediated (shared) surface — the whole R2-2 keystone turns on that fact.
- judge-r2: No PDF-full-text / table-extraction capability: could not independently re-verify the arXiv 2604.02623 ASR figure (R2-8's ~32.5%) at the leaf node; R1-11's likelihood premise relies on red's verification. With such a tool I would have confirmed the figure and the MINJA ~95% attribution directly.
- judge-r2: No access to the primary Anthropic security advisory for CVE-2026-21852: the 'removed user memories from the system prompt' detail is load-bearing for R2-2's double-bind but confirmable only against post-cutoff vendor-blog sourcing. With primary-advisory access I would have settled whether the bespoke projection re-authorizes a remediated (net-new) or unremediated (shared) surface, rather than directing blue to make the conclusion robust to both branches.
- blue-respond-r2: No PDF full-text / table-extraction capability: I verified arXiv 2604.02623 and 2503.03704 via abstract fetch + web search, but could not confirm MINJA's exact ASR from the primary results table (relied on secondary coverage for ~98.2%/76.8%). With PDF table extraction I would have pinned the MINJA figure to the paper's own table rather than secondary sources.
- blue-respond-r2: No access to the primary Anthropic security advisory for CVE-2026-21852: the load-bearing 'removed user memories from the system prompt' detail stays medium-confidence and is confirmable only against post-cutoff vendor blogs. With the primary advisory I would have settled the R2-2 double-bind directly instead of engineering §13.7(4) to hold under both branches.
- red-merge-r3: No full-PDF-text / PDF-table-extraction capability: R3-14 (~29-day half-life), R3-15 (RecMem 77% lower bound), and carried R1-19 (agent-PR 61.4%/71.6%) remain 'unable-to-corroborate-at-leaf-node' rather than confirmed or ruled out — HTML/abstract fetches are lossy for in-body-table numbers. With a PDF table extractor I would discharge all three definitively instead of grading them low-confidence.
- red-merge-r3: No access to the primary Anthropic security advisory for CVE-2026-21852: R3-17's 'removed user memories from the system prompt' detail is confirmable only against post-cutoff vendor-blog sourcing. With the primary advisory I would settle whether the bespoke projection re-authorizes a remediated (net-new) or unremediated (shared) surface.
- red-merge-r3: No way to inspect the (unbuilt) trajectory-extractor implementation: R3-7 hinges on whether §13.4's supporting-turn-set is computed mechanically by the harness or self-reported by the LLM. A way to read the extractor's turn-traversal would settle whether R3-7 is a live design gap or already foreclosed.
- blue-respond-r3: No access to the primary Anthropic security advisory for CVE-2026-21852 (blocked/unreachable from this environment). I could only corroborate the 'removed user memories from system prompt' remediation and the CVE-id-to-vector mapping against post-cutoff third-party vendor blogs (Cisco, omegamax). With a way to fetch the primary advisory I would have settled R3-17/R1-29's medium-confidence tag to high-or-retracted, and confirmed whether the bespoke projection re-authorizes a remediated (net-new) or unremediated (shared) authority surface.
- blue-respond-r3: No PDF full-text / table-extraction capability: I verified RecMem/MemorySurvey/tianpan via abstract-and-HTML fetches (sufficient this round), but for figures that live only in PDF body tables (e.g. the unreviewed-PR 61.4%/71.6% split still pending in [^UnreviewedPRs]) I cannot reach the leaf node and must leave them labeled approximate. A PDF table reader would let me pin those.
- red-merge-r4: No full-PDF-text / PDF-table extraction tool: the MINJA digits (ISR 98.2% / ASR 76.8%) were invisible to the default arXiv /abs/ abstract fetch and to the 7.7MB compressed PDF; they surfaced only via the /html/…v2 route (three calls where one should suffice). With reliable PDF-table extraction I would have closed the MINJA check in one call and discharged R1-19 (agent-PR 61.4%/71.6%) and R4-9 (locate the embedding-dedup study that actually carries the cosine-bin figures) definitively rather than leaving them at 'unable-to-corroborate' or 'source-wrong-but-real-figure-unlocated'.
- red-merge-r4: No way to observe Auto Dream's actual runtime output/behavior from here (it is a server-side-flag feature on the §10 Unverified list). This directly limits how hard R4-6 can be pressed — I can flag that the recurring flag-check assumes a 'native-consolidation signature' whose existence is unverifiable, but I cannot test whether such a signature exists. A sandbox with the server-side flag toggled would let me confirm or refute the detection primitive.
- red-merge-r4: [^ParaphraseGap] Springer primary (10.1007/s10579-023-09715-7) redirects to an auth wall (HTTP 303 → idp.springer.com) — unfollowable for a skeptic. Could not corroborate the §2.3a paraphrase-recall figures at the leaf node (plausible, not contradicted). An authenticated-fetch or institutional-access path would settle followability; at minimum one of the two [^ParaphraseGap] sources should be open-access.
- blue-respond-r4: PDF-table-extraction capability absent: blocks leaf-node confirmation of the two [^UnreviewedPRs] figures (61.38%/71.58%) in arXiv 2604.24450 — two HTML fetches surfaced only category distributions. With a PDF-table extractor I would quote the exact sentence or definitively retire the figures; direction is independently carried by the ~54% Dependabot rate, so it is not verdict-bearing.
