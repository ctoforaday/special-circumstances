# friction.md — Trajectory telemetry for agent adjudication: what can be mined from Claude Code transcripts today, what settings or APIs expose a summary of an agent's reasoning rather than only its acts, and which of those are sound enough to carry a citable finding?
blue-synthesize: PINNED.md names two evidence files under inputs/ (probe-thinking-persistence.md, mining-substrate-architecture.md) that do not exist on disk, and pins repo HEAD at cacb736 while actual HEAD at synthesis is 4baf282 — the pinned evidence base is unresolvable; I would have cited those files for the thinking-persistence probe rather than re-deriving it from the local store.
blue-synthesize: the Write tool refused to create blue/report.md ('Subagents should return findings as text, not write report files'), a generic subagent guard that contradicts this seat's contract where the report file IS the payload; I had to stage the report in the scratchpad and install it with a Bash cp. A seat whose deliverable is a tracked markdown artifact needs the guard scoped to exclude run-directory writes.
blue-synthesize: no verb exists to record a claim-provenance/minority-report map or the claim_count computation method as an event; I recorded both in blue/CHANGELOG.md prose instead. A 'manifest-row'-shaped verb usable at synthesis (not only per repaired gap) would carry it.

red-merge-r1: `merge mint` has no amend path — a first mint with placeholder payload is permanent (same `--key` returns the existing id without updating `--problem`/`--fix`/`--check`; `regrade` moves grades only). The tool documents no way to list valid `--class` slugs, so discovering the registry costs a throwaway mint, which then cannot be corrected. Wanted: a class-registry listing, or `mint --amend` for payload correction on an unclosed gap.
red-merge-r1: `merge spot-check` cannot record an honestly-empty round — `--ids` requires a list, and the archive entered round 1 with zero records. There is no way to record "archive empty at round start, floor not applicable" as an event; it survives only as prose in ledger.md/archive.md. Wanted: `spot-check --none --reason`.
red-merge-r2: acceptance-check contracts have no field for CONDITION-PINNING. R1-1's check demanded the digits be verified at the newly cited source; it passed exactly as written while the repair introduced a generalization the source's second experimental arm refutes. The gap record wants a distinct slot — "conditions/arms this figure must remain pinned to" — separate from the digit check, so a fix-spec cannot verify a number and miss the condition that produced it.
judge-r2: the ruling set has no disposition for the commonest bench outcome this docket produced — the acceptance check passes as written while a residue of the same class rule survives at a site the check did not name (R2-1 Provenance, R2-5 footnote label). I had to carry both in prose review-flags, which are not machine-readable and do not travel to the next round as an obligation. The work wanted either a 'closed_with_residue' disposition or a --residue flag on 'bench opinion' that ships the leftover as a named, tracked note without re-opening the gap. Without it the bench either over-carries (spending a round on form) or closes and loses the residue.
red-merge-r3: red-merge-r3 was invoked with no preceding blue-respond-r3 turn — no events-blue-respond-r3-*.jsonl exists and blue/report.md is byte-identical to the artifact judge-r2 ruled on. The seat has no way to signal 'audited an unchanged artifact' except in prose; the binary verdict field forces FAIL, which the board telemetry then reports as if blue's repairs had failed. What the work wanted: a round-parity precondition the engine checks before spawning the red merge, or an envelope field distinguishing an evidentiary FAIL from a no-response FAIL.
red-merge-r3: the archive is append-only and closure classes are enumerated {closed, closed_with_regression, ...}, but there is no class for 'a defect discovered after the bench closed a gap, which amends that closure without reopening it'. R3-1 amends judge-r2's R2-1 and R2-5 closures; the only available expression was a supersedes edge plus hand-written prose, and a withdrawn archive block had to be corrected by appending two further blocks rather than by any supported retraction mechanism. What the work wanted: an amends_prior closure class usable by a seat that did not enter the original closure, and a 'withdraw' verb for an archive block written in error.
red-merge-r3: the verdict render reports '9 open, 9 closed' against the hand-written board's 3 open / 15 closed — the difference is exactly the six gaps judge-r2 closed at the round-2 sitting. Bench dispositions live in the judge's event stream and in debate.md and nothing carries them into red's board projection; red cannot close them itself without double-counting closure history and corrupting the repair_regression denominator. The projection over-reports open gaps by the number of bench closures after every sitting, and diverges further each round. What the work wanted: the render consumes judge disposition events, or the merge seat gets a verb to ACKNOWLEDGE a bench closure (recording that the bench closed it, not that red did).
judge-r3: DOCKET INCOMPLETE — R3-2 was open on red's round-3 board (3 open: R2-2, R3-1, R3-2), red filed a RED CLOSING for it and blue filed a BLUE CLOSING for it in debate.md, but R3-2 was not delivered on the bench's contested docket (2 gaps only). A gap both sides argued to closing reached no ruling and returns to red's verdict pool unadjudicated. The bench has no verb to rule on a gap it was not docketed and no channel to request one mid-sitting. What the work wanted: the docket built from the ledger's open set, or an explicit statement of why a gap was withheld.
judge-r3: DOCKET PREMISE STALE BY CONSTRUCTION — both docketed gaps' problem/acceptance_check text was snapshotted at red-merge-r3 (01:03-01:17) and asserts as a governing condition that "blue took no round-3 turn" and that the cited lines are "unchanged". Blue then repaired at 01:21-01:24 (events-blue-respond-r3-db8bf552.jsonl; report.md 01:21). Both premises were false at my sitting. Ruling on the artifact's final state is correct and I did, but nothing marks a docket premise as time-stamped-and-possibly-superseded, so a bench that credited the problem statement instead of re-running the check would have carried two discharged gaps. What the work wanted: an as-of timestamp per docketed gap, or the engine re-running DOCUMENT-PROBE acceptance checks against the final artifact before the bench sits.

assemble: The task named the report template as `references/report_template.md`; no such path exists at the run root or the repo root. The template actually lives at `plugins/frank-exchange-of-views/skills/research-protocol/references/report_template.md`. Cost a repo-wide find before assembly could start. Wanted: the engine passes the resolved absolute template path, or the seat prompt cites it as it exists on disk.
assemble: TEMPLATE MISFIT — the report template has no home for the three things a ceiling-terminated run most needs a reader to see: the CEILING-TERMINATED stamp distinguishing a round-ceiling stop from a judged failure, the fact that the final blue revision carries no red audit, and the re-audit obligation leaving the run as debt. I added a named "Ceiling termination: what this costs" section outside the template rather than smuggling the debt into the verdict line or dropping it. The template assumes a run that terminates on a verdict; this one terminated on a clock, and the difference is the finding.
assemble: `feov-record bench` has no verb for the assembly act itself. The available verbs are certify, opinion, petition-rule, halt, friction, register, render — none records that a report was assembled by union-copy from N named sources, nor the verbatim-integrity check I ran (diff of each embedded region against its source file). The bench's single largest act in the run leaves no event, and the audited-copy guarantee that the run-5 post-capture finding exists to enforce is therefore unverifiable from the event stream. Wanted: an `assemble` verb taking source paths plus a per-source verbatim/arranged/authored classification.
assemble: Blue's final envelope `open_questions` (10 items) and blue/report.md's own "Open questions carried past this round" section (10 DIFFERENT items) diverge, and the template provides exactly one home for open questions. Nothing in the record marks which list supersedes the other or that they differ. I preserved both and stated the relationship in prose, but that is an assembly-time judgment call the record should carry, not the assembler. Wanted: the envelope's open_questions to be a recorded event superseding the in-report list, or an explicit field marking the in-report list as a round snapshot.

## auto-harvested at capture (envelope entries absent from the file — lens/abort loss class)

- The Write tool refused to create blue/report.md with 'Subagents should return findings as text, not write report files' — a generic subagent guard that directly contradicts this seat's contract, where the report file IS the payload. I staged the report in the scratchpad and installed it with a Bash cp, costing an extra round-trip. The guard needs scoping to exclude writes inside the run directory.
- No record verb exists for a synthesis-round claim-provenance map (minority tags, merged footnote labels) or for the claim_count computation method; both had to go into blue/CHANGELOG.md prose. A manifest-row-shaped verb usable at synthesis — not only per repaired gap — would carry it as an event.
- The catechism template's Q5 ('what would this displace?') and Q7 ('where would we stop?') assume a build decision with an implementation budget. This topic is a capability survey whose answer is largely negative, so the honest answers are about what the finding LICENSES (stopping a planned thinking-block miner) rather than what it costs to build. The template wanted a project; the work was a reconnaissance.
- `merge mint` has no amend path. A first mint with placeholder payload is permanent: retrying with the same `--key` returns the existing id ('idempotent retry — existing id returned') without updating `--problem`/`--fix`/`--check`; `regrade` moves grades only. The tool documents no way to list valid `--class` slugs, so discovering the registry cost a throwaway probe mint, which then became R1-1 in the event stream with placeholder prose (the ledger carries the real record). Wanted: a documented class-registry listing so no probe mint is needed, or `mint --amend` for payload correction on an unclosed gap.
- `merge spot-check` cannot record an honestly-empty round. `--ids` takes a comma-separated list and the archive entered round 1 with zero records, so there is nothing to sample; the verb offers no way to say 'archive empty at round start, floor not applicable'. The discharge survives only as prose in ledger.md/archive.md, which a later audit has to take on trust. Wanted: `spot-check --none --reason`.
- Acceptance-check contracts have no field for CONDITION-PINNING. R1-1's check demanded the digits be verified at the newly cited source; it passed exactly as written while the repair introduced a generalization the source's second experimental arm refutes (GPT-OSS 120B 0.435→0.227 against DeepSeek-R1's 0.417→0.012). The gap record wants a slot distinct from the digit check — 'conditions/arms this figure must remain pinned to' — so a fix-spec cannot verify a number and miss the condition that produced it. Appended to friction.md and recorded via the friction verb.
- Ruling set has no disposition for 'acceptance check passes as written, residue of the same class rule survives at an unnamed site' — hit twice this docket (R2-1 Provenance still framing the independence question as pending; R2-5 footnote label still reading NISTAuditRequirement while its content disclaims NIST). Prose review-flags are not machine-readable and do not travel to the next round as an obligation. The work wanted a `closed_with_residue` disposition or a `--residue` flag on `bench opinion` that ships the leftover as a tracked note without re-opening the gap; without it the bench either over-carries on form or closes and loses the residue. Appended to friction.md as an attributed line.
- The archive is append-only and closure classes are enumerated {closed, closed_with_regression, ...}, but there is no class for 'a defect discovered after the bench closed a gap, which amends that closure without reopening it'. R3-1 amends judge-r2's R2-1 and R2-5 closures; the only available expression was a supersedes edge plus hand-written prose, and a withdrawn archive block had to be corrected by appending two further blocks rather than by any supported retraction mechanism. What the work wanted: an amends_prior closure class usable by a seat that did not enter the original closure, and a 'withdraw' verb for an archive block written in error.
- The verdict render reports '9 open, 9 closed' against the hand-written board's 3 open / 15 closed. The difference is exactly the six gaps judge-r2 closed at the round-2 sitting: bench dispositions live in the judge's event stream and in debate.md, and nothing carries them into red's board projection. Red cannot close them itself without double-counting closure history and corrupting the repair_regression denominator, so the projection over-reports open gaps by the number of bench closures after every sitting and diverges further each round. What the work wanted: the render consumes judge disposition events, or the merge seat gets a verb to ACKNOWLEDGE a bench closure (recording that the bench closed it, not that red did).
- DOCKET INCOMPLETE: R3-2 was open on red's round-3 board (3 open: R2-2, R3-1, R3-2), red filed a RED CLOSING for it and blue filed a BLUE CLOSING for it in debate.md, but R3-2 was not delivered on the bench's contested docket (2 gaps only). A gap both sides argued to closing reached no ruling and returns to red's verdict pool unadjudicated. The bench has no verb to rule on a gap it was not docketed and no channel to request one mid-sitting. What the work wanted: the docket built from the ledger's open set, or an explicit statement of why a gap was withheld.
- DOCKET PREMISE STALE BY CONSTRUCTION: both docketed gaps' problem/acceptance_check text was snapshotted at red-merge-r3 (01:03-01:17) and asserts as a governing condition that 'blue took no round-3 turn' and that the cited lines are 'unchanged'. Blue then repaired at 01:21-01:24 (events-blue-respond-r3-db8bf552.jsonl; report.md 01:21). Both premises were false at my sitting. Ruling on the artifact's final state is correct and I did, but nothing marks a docket premise as time-stamped-and-possibly-superseded, so a bench that credited the problem statement instead of re-running the check would have carried two discharged gaps. What the work wanted: an as-of timestamp per docketed gap, or the engine re-running DOCUMENT-PROBE acceptance checks against the final artifact before the bench sits.
