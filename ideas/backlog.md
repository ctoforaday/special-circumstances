# Backlog — small findings awaiting a PR

Working list of paper cuts and improvements found while dogfooding. Each item graduates into
whichever PR next touches its plugin; bigger items get promoted to `plans/` proposals instead.

- [x] **frank-exchange-of-views**: rename `skills/research-protocol/scripts/workflow.js` → `debate.js` (name by function); update the `scriptPath` reference in `commands/research.md`.
- [x] **prosthetic-conscience**: the doctor command's bootstrap snippet must not clobber Windows `%TMP%` — a bash `TMP=$(mktemp -d)` overwrote the exported variable and broke child processes (`gh`, `go`). Use a non-reserved shell variable name in `commands/doctor.md`.
- [x] **frank-exchange-of-views**: the workflow-subagent write-block is CONFIRMED as a hard, report.md-specific tool error (forensics: `is_error: True`, "Subagents should return findings as text, not write report files"; CHANGELOG/debate writes succeeded). Run 2 is the live confirmation. Fix landed in PR #14 as the PRE-CREATED blackboard (red's recommendation) rather than renaming artifacts — agents only append to existing files.
- [ ] **frank-exchange-of-views**: run-1 staged inputs vanished — forensics EXONERATES the agents (zero writes/deletes outside undefined/ + agent-memory; all shell read-only). Caller-side cause still unidentified; watch run 2's staged inputs.
- [ ] **frank-exchange-of-views**: formalize trajectory capture in `/research` — after each run, copy the workflow `journal.jsonl` into `<run>/trajectories/` (git-tracked; compact structured returns) and gzip the per-agent transcripts alongside (gitignored; durable home decided by the memory-architecture proposal, PR #2). Raw trajectories are the primary self-learning input and currently evaporate with the session.
- [x] **frank-exchange-of-views**: build the **workflow simulator** — DONE on the PR #14 branch (`tests/simulator/`, 11 tests, ~200ms, zero tokens, CI job `debate-sim`): founding regressions (stringified args, null returns) + PASS/docket/deadlock/ceiling/citation-scaling/per-role routing/friction attribution. Same doctrine as the Go hooks: separate logic from model calls, then test the logic.
- [x] **repo**: `.gitattributes` forcing LF on js/json/md/yml/go — prevents the CRLF control-character rejection that blocked the run-2 launch.
- [x] **frank-exchange-of-views**: null-guard every schema'd `agent()` return in the debate script — `agent()` returns null on terminal failure; run 2 crashed with `null is not an object (evaluating 'redEnv.verdict')` when the quota wall killed red. Second founding test case for the workflow simulator.
- [x] **frank-exchange-of-views**: optional `model` arg on the debate script (dev knob — run swarms on a cheaper model during development; default inherits the session model).
- [ ] **frank-exchange-of-views — the cheap-testing stack** (from run-2's quota crashes; 3M tokens of evidence):
  (a) workflow SIMULATOR: Node harness stubbing agent() with canned envelopes — tests arg parsing, round loop, contested docket, deadlock, ceiling, null returns, cache-prefix stability, for zero tokens. Founding regressions: the stringified-args bug, the redEnv null crash.
  (b) SMOKE MODE: /research --smoke = 1 lane, 1 round, 1 citation pass, model=haiku, trivial topic — full-pipeline integration for ~50k tokens; pre-merge check for debate-script changes.
  (c) ASSEMBLE-ON-FAILURE: a dead round (agent null / quota wall) should break the loop and assemble UNVERIFIED from existing material, never throw away paid-for rounds (run 2 has 3 real debate rounds and no report).
  (d) COST LEVER: red's full-report re-read × scaled lens instances × round is the dominant burn; consider narrowing later-round audit scope (changed sections + contested gaps + spot checks) as a graded-integrity middle path — needs the human's call, it trades against the full-re-read principle.
- [ ] **frank-exchange-of-views — efficiency levers** (approved direction: cheapen redundancy, never judgment):
  (1) ~~PER-ROLE MODELS~~ DONE (PR #14): `model` (bulk) + `judgmentModel` (judgment seats, defaults to inherit-session — never falls back to the bulk model). Run 3 is the live trial: bulk=sonnet, judgment=session.
  (2) ~~CITATION LEDGER~~ DONE (PR #14): `red/citation-ledger.md` — high-confidence verifications persist across rounds unless blue's CHANGELOG shows the section changed.
  (3) ROUND-SCOPED AUDIT (human-gated — trades against the full-re-read principle): round 1 full leaf-node; rounds 2+ = changed sections in full context + contested gaps + random spot-checks. Hold unless 1+2 insufficient.
- [ ] **frank-exchange-of-views — make red cheaper by improving blue** (quality at the source beats inspection):
  (4) BLUE PRE-FLIGHT SELF-AUDIT: before submitting, blue checks a cheap checklist (claims footnoted, footnotes complete, confidence self-graded, open questions declared) AND red's accumulated gap-pattern project memory — yesterday's expensive red discovery becomes tomorrow's free checklist line. Limit: kills shallow gaps only; red stays load-bearing for blind spots.
  (5) CLAIM MANIFEST: blue emits a machine-readable ledger (claim -> citation -> self-graded confidence -> lane provenance). One artifact, five wins: red walks the manifest instead of parsing 53KB of prose; the citation ledger gets increments for free; consensus-vs-minority doubt answered via provenance; blue gains drafting discipline; red grading gets provenance input.
- [ ] **run-2 friction harvest (the self-evolution loop's first real meal)**: (a) ~~write-block~~ FIXED (PR #14): `/research` pre-creates the full blackboard skeleton (stub headers) so agents only append/edit — first live trial is run 3. (b) Windows ENAMETOOLONG on big heredocs — agents need an append-friendly write path. (c) TOP TOOL GAP, requested by red, blue, AND judge across all 4 rounds: **PDF full-text/table extraction** — citations stayed low-confidence without it; candidate: a `sc-pdf-extract` Go tool or an MCP. (d) primary-security-advisory access gap. (e) live-source drift: protocol should record access-date deltas.
- [ ] **run cost audit — a tool, not a diet** — (a) ~~DONE (PR #16)~~: `scripts/cost-audit.mjs` + fixture tests in CI; `/research` step 6 emits `cost.md` per run. REMAINING: the (c)/(d) efficiency investigation below. (from run 3's live measurement; explicitly NOT a call to cheapen judgment): (a) build a cost-audit tool (Node, like the simulator) that parses the workflow journal + agent transcripts and reports per-seat tokens (input / output / cache-read / cache-write) and dollars; `/research` runs it at assembly and drops `cost.md` into the run directory next to friction.md. (b) FINDINGS so far (run 3, round 1): the panel token counter excludes cache traffic = 92% of real flow (panel said 610K; transcripts showed 47.7M); blue-synthesize on the session model was the single priciest agent ($10.58 — cache RATES, not output volume); red ≈ $20/round at this corpus size; full run projects $80-120 at list rates. (c) INVESTIGATE in its own PR: efficiency that preserves judgment quality — claim manifest and round-scoped audit attack the cache-read volume structurally; per-seat model choices are a measurement question (needs A/B evidence), not a default. (d) MERGE-SEAT ANALYSIS (run-3 transcripts): the driver is TURNS x CONTEXT, not file size — an agent re-reads its whole context every tool call (red-merge-r1: ~100-150K of material, 2.7M+ cache reads). Levers, cheapest first: (1) SHARD THE FINDINGS — split open-items ledger from closed archive (or per-gap files with status headers); merge reads open + this round only; full-re-read principle covers red-vs-blue, NOT red re-reading its own closed cases; same pattern as the citation ledger. (2) COLLATOR STAGE — bulk-model agent concatenates/normalizes the round's lens passes into one digest before the judgment merge reads anything (one big read, fewer turns; cheapens mechanics never judgment). (3) prompt-level read batching. (4) TOOLING step-up if gap volume grows: evaluate beads (bd, git-backed agent issue tracker, JSONL+SQLite) vs a tiny sc-gaps Go tool; full SQLite/sqld = overkill at ~30 gaps and costs git-diffability. Burn is SPIKY and the spikes are judgment-seat full re-reads — cost.md should show a per-agent timeline.
- [x] **frank-exchange-of-views: the docket detector tracks IDs, not lineages** — FIXED (PR #15, FEOV 0.4.0): `supersedes` + `closures` on the red envelope, whole-debate docket window, and the STRUCTURAL enforcement throw red's R5-5 demanded (a closed_with_regression closure with no successor naming it aborts); 3 lineage regression tests in the simulator. First live trial: run 4. (discovered live in run 3, rounds 1-3): red closes gaps "WITH REGRESSION" and mints successor gaps under fresh IDs (R1-5 → R2-4 → R3-4/R3-9), so `prevGapIds.has(g.id)` never matches, the contested docket never arms, and the judge never sees a dispute lineage no matter how long it persists — the only remaining brake is the maxRounds cost ceiling. Observed behavior was legitimate deepening (red even conceded an error after independently re-verifying blue's rebuttal twice — adversarial self-correction working), but legitimate-vs-spinning is exactly the call the judge exists to make and the detector can't route it there. FIX for run 4: gap envelope gains `supersedes: [prior-ids]`; contested-detection follows lineage chains (depth >= 2 → docket); simulator gets a lineage regression test. AMENDED by red itself (run 3, gap R5-5, HIGH): a self-declared `supersedes` field relies on unenforced good-faith compliance — a red that omits the declaration evades the docket exactly as before; detection must be structural (judge samples closed-with-regression chains, or lineage inferred from location/quote overlap), not self-reported. The run-3 report's §3 docket carries the graded proposal.
- [ ] **frank-exchange-of-views — run-3 termination & fairness levers** (all born from watching run 3 grind rounds 3-5; the report's §3 grades the engine's own versions): (1) SEVERITY-FLOOR TERMINATION — when every open gap is <= MEDIUM with trivial fix cost, route the whole board to the judge for disposition instead of another $25-30 round (CLAIM CORRECTED per run-4 report §6.4 item 2: the "would have ended run 3 at round 3 for ~$10" savings claim FAILS audit — round 3 held two MEDIUM-HIGH open gaps and NO threshold setting reproduces the claimed stop; lever REJECTED as automatic by run 4 §1, telemetry+stop-and-resume ratified instead, hardened minority variant registered as a prediction). (2) RISK-MASS-PROPORTIONAL SPEND (umbrella): after each merge the script computes sum(likelihood x impact) over open gaps; that drives next round's lens count and audit scope (full sweep at high mass, scoped + spot-check floor as mass falls, judge disposition below threshold). Caveat: grades are red's own estimates — the spot-check floor never reaches zero. (3) GRADE-DISPUTE CHANNEL — blue envelope gains `grade_disputes: [{gap_id, dimension, proposed, evidence}]`; a dispute red rejects next round auto-dockets to the judge. Adversarial grading first; best-of-N panel grading only if runs 4-5 show lone-voice bias survives (needs per-gap records to grade blind from). (4) CORPUS PINNING — pin the evidence base at a commit for the run's duration; red audited OUR mid-run commits in run 3 (R3-8 cites a backlog commit from an hour earlier) and cross-corpus citation drift (R5-2 class) recurs whenever a living document is cited unpinned.
- [ ] **frank-exchange-of-views — run-3 friction paper cuts** — MOSTLY GRADUATED (PRs #15/#16): (b) friction-to-file ✓, (c) lens-scoped labels ✓, (d) blue reads transcript ✓, (e) compound grade enums ✓; friction.md added to the pre-created skeleton (partial (a)). STILL OPEN: (a) a true sanctioned Write path for red's living artifacts (skeleton covers append, not Write); (f) Grep line-vs-occurrence harness note; (g) Read-cap/section-indexed reads for the audit surface; (h) PDF MCP adoption (awaiting install consent — vetted candidates in report §3 row 13); log()-per-transition heartbeats (below). (17-entry harvest in the run's friction.md): (a) WRITE-BLOCK MECHANISM ISOLATED by red's own controlled experiment (r2 merge): the guard is FILENAME-KEYED and path-independent (findings.md refused even in scratchpad; neutral name + cp succeeded) — every red merge pays a copy detour; need a sanctioned write path for red's living artifacts (pre-created skeleton did not cover the merge seat's Write path). (b) friction array is script-local until the terminal return — a thrown abort loses the whole harvest (R5-6); agents should append friction to a blackboard file as they go. (c) per-lens finding labels collide (four different "R5-1"s in one round) — lens-scoped labels (L3-F1) in the lens template. (d) gap-summary JSON handed to blue is lossy vs debate.md's RED section — prompt blue to read the transcript first (cost one wasted iteration in r4). (e) envelope grade enums (low/medium/high) round off red's finer protocol vocabulary (medium-high, certain, realized) — widen enums or free-text. (f) Grep count mode counts LINES not occurrences (caused a false miscount alarm) — harness note in lens prompts. (g) Read-tool 25k cap vs the living report (size figure corrected per run-4 §6.4 item 5: "54KB" was stale — findings.md was 106,772 bytes and blue/report.md 159,394 at the run-4 pin) forces windowed reads at the merge seat — section-indexed reads for the designated audit surface. (h) PDF extraction remains gap #1, reported by every red merge in two consecutive runs. — panel anatomy established from run 3's live view: per-agent rows show only the static `agent()` label (never updated mid-life; the slug suffix is why rows are readable); the top line = phase + most-recently-active agent label; `log()` is the narrator line and the ONLY thing the script can update at will, zero tokens. NEXT PR: emit `log()` at every state transition the script already knows (round start w/ lens count, red verdict w/ gap+contested counts, docket rulings, blue response w/ open-gap count) + simulator assertion. RESIDUAL gap: mid-agent liveness (a 16-minute synthesize sits silent) — no harness API for an agent to update its own row; keep researching.
- [ ] **doctor cross-plugin aggregation**: sc-doctor reads only its own plugin root's requirements.json — FEOV's deps (node/uv for MCP runtimes) currently squat in prosthetic-conscience's manifest with FEOV-naming purposes (PR #17). Right shape: each plugin ships its own requirements.json; the doctor walks every installed SC plugin's cache dir and aggregates. Go change + tests; tag on next PC bump.
- [ ] **plugin LSP servers — evaluate when the code surface grows**: Claude Code supports plugin-bundled LSPs (/reload-plugins reports the count). Live diagnostics (gopls, typescript-language-server) pay off for agents iterating on substantial code; our Go/JS surface is small and CI/simulator-tested today. Revisit at Phase 4 (sleeper-service is code-heavier) or if hook-tool development accelerates. Same graduation ladder as MCPs: project-scope first, plugin-bundled on evidence.
- [ ] **qmd adoption — the recall layer** (tobi/qmd 2.5.3, probed live 2026-07-14; installed globally on this box for the probe): MEASURED — index build 77 files/1.3s; `qmd update` 0.7s flat (changed or not); BM25 `search` 0.6s cold with ZERO models and excellent precision on our corpus (line-anchored citable URIs `qmd://collection/path:line #docid`); embed = 333MB model + ~2m45s full corpus, incremental thereafter (~10s/changed report); semantic layer's true footprint ~1.6GB+ (vsearch pulls the 1.28GB query-expansion model too; hybrid `query` adds a 640MB reranker). LAYERED DESIGN (each degrades to the layer below): (1) Bash BM25 for swarm agents — model-free, sub-second, no MCP needed (qmd ships its own MIT agent skill teaching search→get); (2) MCP stdio hybrid `query` for session-level recall + /self-improve; (3) PostToolUse hook runs fast FTS `update` on writes under indexed roots — deterministic freshness, can't forget (capability-gated, no-op without qmd); (4) `embed` refresh at blue/red phase tops (semantic at-worst one-phase stale; BM25 never stale). DOCTRINE: retrieval for evidence/context; FULL READ for the document under audit; leaf-node fetch for verification — three access modes, never confused. PREDICTED HARD PROBLEM (gb): not write latency (confirmed ~0.7s) but COLLECTION LIFECYCLE — ballooning across many research passes and expensive tombstones in the vector store. Mitigants to design: pinned/archived run dirs never change (no re-embed churn by construction — the pinning convention IS the lifecycle policy); `qmd cleanup` vacuums; consider one collection per corpus root (research/ideas/plans) not per run; measure index+model disk over runs 4-6 before promoting beyond dogfood. HTTP DAEMON VERIFIED LIVE (2026-07-14, this box, CPU-only): `qmd mcp --http --daemon` works as README documents (PID file, `qmd mcp stop`, `/health`, MCP Streamable HTTP at :8181/mcp) — Phase 4 can depend on it. MEASURED LADDER: bare-CLI hybrid `query` = 36.3s per invocation even fully model-warm (loads all 3 models from disk every time — never use bare CLI for model-backed search); daemon first-touch 27.3s (lazy load), steady-state hybrid 19.6s (CPU rerank of 40 chunks dominates; GPU box would collapse this), lex-only via daemon 2.9s, bare-CLI BM25 0.6s (swarm layer unchanged). KEY DISCOVERY: the MCP `query` tool takes a REQUIRED client-authored `searches:[{type:lex|vec|hyde, query}]` array — over MCP the CALLING AGENT does the expansion, so the 1.7B expansion model is never loaded (CLI-only dependency, 11.3s saved) AND quality improves: the generic expander mangles domain jargon (expanded "can blue argue…" to "how to use blue to reduce risk levels"); the agent knows blue is a team. Total model footprint if daemon-only: 973MB (embed+rerank), not 2.2GB. Retrieval quality: 93%-scored exactly-right top hits on both probe questions (R5-5's own lens doc for the lineage question). OPEN NIGGLE: `collection` param may not filter (sc-ideas hit returned despite collection:"sc-research") — verify param semantics before the PR ships. SHIP SHAPE (PR #18, revised twice per review): seats MCP-only (lex-only sub-queries ARE BM25 — no CLI in seats); ONE installed qmd everywhere (doctor installs from requirements.json = version authority; .mcp.json runs `qmd mcp`; hook runs installed binary, NO npx fallback — a second resolution path is a second version, and qmd versions can disagree on index schema). STORE COORDINATION (verified): a single per-user SQLite at ~/.cache/qmd/index.sqlite is the rendezvous — collections are rows mapping name → ABSOLUTE path + glob, so hook/MCP/CLI need no config and cwd never matters; collection-per-corpus-root means new run dirs are covered with zero registration. CAVEATS: `qmd update` (no -c flag) refreshes ALL collections the user registered across ALL projects — cheap (hash-skip) but a global side effect; SQLite/WAL is the hook-write-vs-MCP-read concurrency story (fine at our rates); `qmd init` project-local .qmd store exists as the bounded alternative — evaluate during the lifecycle measurement (runs 4-6) if cross-project mingle or index growth bites.
- [ ] **qmd tombstone / index-lifecycle problem** (gb's prediction, tracked for after PR #18 lands): the hard part of the recall layer is not write latency (0.7s measured) but what accumulates — vector-store tombstones from deleted/renamed files, index growth across many research passes, and `~/.cache/qmd/index.sqlite` ballooning as a per-user GLOBAL store shared by every project on the box. Investigate after runs 4–6 give real churn data: (a) measure index.sqlite + models disk over the runs; (b) does `qmd cleanup` actually reclaim tombstoned vectors or just vacuum FTS? (read the source, then test on a copy); (c) rename/delete behavior — a moved run dir re-embeds whole (cost) and does the old row die or linger?; (d) whether pinned-immutable run dirs (our convention) make churn zero in practice, making the whole problem moot for research/ and acute only for ideas/ (living docs); (e) the bounded alternatives if it bites: `qmd init` project-local store (dies with the repo), collection remove+re-add as a compaction event, or an archived-runs collection with embeddings dropped (BM25-only for cold corpora). Exit criteria: a written lifecycle policy in the protocol, or a measured all-clear.
- [ ] **empty-bin window after a version bump — hooks crash-storm until doctor --fix** (bit hard on the 0.7.0 dance, 2026-07-14): `/plugin update` creates the new cache dir from git; hook binaries are CI-built and never in git, so EVERY hook event (PreToolUse Bash included) spawn-fails noisily until `/prosthetic-conscience:doctor --fix` populates `bin/` — and the SessionStart nudge can't warn because it is itself one of the missing binaries (chicken-and-egg). FIX (class-of-problem): wrap each hooks.json command in a bootstrap guard that turns the crash storm into a single terse pointer — `[ -x "$B" ] && exec "$B"; echo "prosthetic-conscience: hook binaries missing — run /prosthetic-conscience:doctor --fix" >&2; exit 0` (hook shell is Git Bash even on Windows — verified live: the 0.7.0 failures came from /usr/bin/bash). Shell-in-hooks doctrine exception is justified: the guard is the environmental precondition check for the only layer that exists before the binary does; keep it one line, no logic. Alternative rejected: committing binaries to git (bloat + unverifiable). Ship in the next PC bump — every future update dance hits this window otherwise.
- [ ] **jq + qlty joined the box (2026-07-17, user-installed)** — add `jq` to prosthetic-conscience requirements.json (optional tier: "JSON slicing for diagnostics and key-scoped settings reads"; install: winget/choco/scoop per OS) so the doctor reports it; ride the next PC bump. qlty needs no manifest change (already recommended tier) — next session it resolves on PATH and sc-quality-gate runs real fmt/check for the first time: watch the first sessions for gate noise/latency and tune the hook if it barks on generated files.
- [ ] **Context-forking / common-preamble pattern — DOUBT-FLAGGED, at best a plans PR** (2026-07-17 discussion; measured hook: blue/report.md read 110x by 36 agents in run 4, ~90-100 ingestion rounds + per-agent cache-write of the same bytes). Three rungs: (1) corpus-snapshot single-read per phase (doctrine-neutral, mechanical); (2) fork semantics in Workflow agent() — upstream feature ask; (3) shared-prefix orchestration at the API layer (Phase-4 custom harness — each lens = shared cached prefix + per-seat suffix). STANDING OBJECTION (gb): rungs 2-3 trade away STRUCTURAL context isolation for promised isolation — lens independence would rest on a mechanical loader's discipline instead of physics; "I don't want to weaken structural promises." House gate applies: no build without debate ratification + this objection argued down in review. Rung 1 may ride the structural design pass.
- [ ] **Wall-clock program status (forensics 2026-07-17 + A/B smoke)**: SHIPPED — speedClause all 9 seats, recall-hook 60s debounce, native-peek doctrine, CI burn quartered. A/B RESULT (haiku smoke vs baseline): Bash calls HALVED 85->41 (native-peek engaged), rounds -14%, agent-min -19%, md-write median 2.7->2.2s (debounce engaged); **BATCHING DID NOT ENGAGE at haiku (0/175 multi-call messages)** — the biggest lever (~25-45 min at keeper scale) is UNPROVEN until observed at the Fable tier: run 5 is its trial; if Fable ignores the prose too, batching needs a structural mechanism (engine-fed file lists), per the script-vs-prose doctrine. QUEUED (design pass): parallel blue-response via lane/union (~25-35 min), overlapped red-merge (~15-20 min), seat checkpointing (~12 min on crashes). REFUTED: bibliography cache (1-2 wall-min); Bash 12s floor was environmental-that-day (0.5s today, controlled probe); indexing acquitted for the floor (near/far medians 13.5 vs 12.6s).
- [ ] **Record layer — structured JSONL per concern** (user-driven, run-5 exhibits): `records/` convention — append-only schema-versioned seats.jsonl (agent self-registration: seat/round/lens/slice — solves true-seat-names the harness never persists), board.jsonl (gap lifecycle events), friction.jsonl (kills the paraphrase-blind parity audit: run-5 capture FAILed on 9 entries, 8 of which were rewordings). Prose stays markdown. Exhibits: dashboard regexes prompts for seat identity; journal has no labels/timestamps/failure events (quota-killed seats show live); capture counts heuristically; friction parity is exact-match. Plans-PR, debate-gated.
- [ ] **Gap classes + repair quality program** (user-originated; full text ideas/gap-classes-proposal.md): track problem CLASSES not point instances (anti-shell-game: successors inherit class, splits trip a recurrence escalator that forces generator fixes); acceptance-check-at-mint, per-repair correctness manifest, claim provenance tags, class-sweep clauses both sides, repair_regression_rate telemetry (baseline 0.37-0.72 lineage mints/closure, rising late — measured 2026-07-17). Debate-gated; composes with record layer.
- [ ] **Constitutional reform — telos, memory, and the bench** (user-directed, THE priority; full plan plans/constitutional-reform.md): every seat gets goal/win-condition/craft/measured-quality; blue memory as compiled manifest lines (memory-as-reading measured worthless: run-5 lanes verifiably read gap-patterns and committed both warned patterns); judge rebuilt as bench with public LAW (statute > precedent > argument; persuasive until human-affirmed — the human is the supreme court), opinions, safety HALT, petition short-circuit. Empirics: 13/13 red FAIL ever, plateau ~40, citations (measured) fail at ~4% vs repairs (unmeasured) at ~60%. Route: plan-audit → FEOV debate under the OLD constitution → 0.10.0 → A/B vs baselines.
- [ ] **Experiments queue** (full text ideas/experiments-queue.md): E1 variance pair (smoke) → E2 reform A/B → E3 single-agent null → E4 peer-review benchmark (human review paper's bibliography+question as external ground truth; Cochrane/Annual Reviews for human-authorship certainty; blinding verified from transcripts; GRADE-vs-MASS and PRISMA-vs-protocol as learn-from-humans arms) → E5 surprise ledger on the Phase-4 build → E6 lens economics (L4 found 0 in r5; L5 found 7 — seat-count lever once attribution is end-to-end).
- [ ] **Journal failure events** (bit twice in run 5): quota-killed seats show as live forever — journal records started/result only. Dashboard shows ghosts (12× red-lens-r4); seats-done denominators wrong. Harness gap; record-layer seats.jsonl self-registration + heartbeat is our own fix.
- [ ] **sc-push-freeze-guard extension**: warn on `git add -A`/`git add .`/`git checkout <branch>`/`git stash` (not just push) while run-live marker exists — the 2026-07-17 blackboard deletion class. Go change + tests, next PC bump.
- [ ] **Supersedes-edge grade-delta telemetry**: accepted-delta docket threshold only sees the dispute channel (0 uses in 3 runs) while ~20 mass moved along supersedes edges per run. Merge computes edge deltas into the round line; script-visible, closes the audit blind spot. Rides gap-classes.
- [ ] **Dashboard openRows fallback**: ledger open-section heuristic parse showed 1 while telemetry said 16 — fallback engages only at zero. Make telemetry authoritative when parse < telemetry, or parse the new sharded format properly. Small fix, feat/run-dashboard.
