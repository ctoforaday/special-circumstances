# proposed holdings — 2026-08-22_sqlite-schema-source [ALL PERSUASIVE — awaiting human review per law/README.md]

Each entry below is a RULING the bench recorded, not yet a holding. `facts`, `holding` and
`scope-limits` are the reviewer's to write from the cited record: the harvest states what it
observed and never invents the rule. A ruling promoted with its placeholders intact is not
citable — law/README.md: a holding without its factual predicate is not citable.

## 2026-08-22_sqlite-schema-source-1 [PERSUASIVE]
facts: <reviewer: fill from the cited record — the harvest never invents>
question: disposition of 
holding: Construction, binding how this record's telemetry is read, not moving any gap (both docket gaps were already disposed by the ancestor bench sitting before I registered). 'Red raised nothing new this round', the second deadlock precondition, is measured by DEFECT CLASS, not by raw new_mint count or fresh gap id. Round 2's telemetry shows new_mint.count=1 (R2-1) alongside class_repeat_rate=1.0 -- a new gap ID (R2-1, supersedes R1-3) carrying the SAME class (figure-recount-fails) as a gap already ruled the prior round. A literal reading of new_mint.count>0 would make deadlock nearly unreachable, since a bench ruling on round N's gaps routinely surfaces round-(N+1) precision residuals in the same repaired sentence (exactly what happened here: R1-3's own repair left the 15-vs-17 sibling defect that became R2-1). Holding: a successor gap that recurs in an already-seen class, at falling severity/mass (round 1 mass 22.75 at high severity; round 2 mass 0 at the successor already closed), is recycling for deadlock purposes even though it mints a fresh id -- it is new PROSE, not a new FINDING CHARACTER. A gap in a genuinely unseen class, even a small one, would NOT qualify as 'nothing new' under this construction. This reading is what let this sitting's outcome stamp UNVERIFIED-by-deadlock rather than treating R2-1's mint as disqualifying; a future bench reading this run's telemetry should apply the same class-level test, not a raw-count one.
scope-limits: <reviewer: state what this assumed>
source: 2026-08-22_sqlite-schema-source, 

## 2026-08-22_sqlite-schema-source-2 [PERSUASIVE]
facts: <reviewer: fill from the cited record — the harvest never invents>
question: disposition of R2-1
holding: <reviewer: state the rule this ruling applied — the harvest never invents; the reasoning is in `rationale` below>
disposition: closed
rationale: Ancestor read: R1-3's closure record (show board), closed_with_regression, successor R2-1 — states the original 653/105-vs-632/125 census defect was fully repaired and proof-anchored (p-d86e9cd2, p-1351bbea), with two sibling precision residuals in the same repaired sentence carried forward as R2-1 rather than reopening R1-3. I verified both required fixes directly against the live artifact, not the docket text. (1) show report --anchor p-dfe09b05 (Technical foundations, line 40): the .Get( reconciliation sentence now states 15 is the plan's stale pinned-round figure and 17 is this round's re-verified current-HEAD total, names the same plan-vintage-vs-current-HEAD drift mechanism already used for 653->632, and gives the actual accessor mix (7 Fields/5 Values/2 Messages/1 Enums/1 Oneofs/1 bare) instead of a false uniform 'Fields().Get(i)' claim. I independently ran blue/get-breakdown.sh myself against repo HEAD f55c12d (bash research/2026-08-22_sqlite-schema-source/blue/get-breakdown.sh) and got unfiltered=41 filtered=24 recordpb-alone=17, breakdown 7/5/2/1/1/1 -- an exact match to both the report text and red's L5-F4 finding. sha256sum of the script on disk (37c0d60d...) matches the recorded proof anchor p-dfe09b05 exactly, so the artifact I read and the artifact the anchor names are the same bytes. (2) show report --anchor f-a4b0936e / f-8f85782a (Q6 sentence, line 63): a proof anchor (p-610a6353, same script blue/census.sh and identical sha256 f3c92db... as the pre-existing p-d86e9cd2) now trails the 'zero SQL-relevant mentions, one heading-level schema hit' claim, satisfying the acceptance check's 'or an equivalent freshly-proven script covering the same grep' clause exactly. Both required_fix items and both acceptance_check clauses are met on direct re-read plus my own leaf re-execution, not on blue's or red's say-so. Closing.
scope-limits: <reviewer: state what this assumed>
source: 2026-08-22_sqlite-schema-source, R2-1

## 2026-08-22_sqlite-schema-source-3 [PERSUASIVE]
facts: <reviewer: fill from the cited record — the harvest never invents>
question: disposition of R1-1
holding: <reviewer: state the rule this ruling applied — the harvest never invents; the reasoning is in `rationale` below>
disposition: routed_to_infrastructure
rationale: R1-1 has no ancestor id (docket supersedes: [] -- this is a re-raised, regraded instance of itself, not a successor gap), so no separate ancestor closure record applies; I read R1-1's own round-1/round-2 history directly off show board (the round-2 regrade note) instead. I independently re-verified the live artifact rather than trusting either closing: show report --anchor c-65ca0a9e (Technical foundations bullet, line 54) now states in-line, at the anchor's own location: 'No verb exists to repoint or drop the stray, still-misdirected c-65ca0a9e anchor... edit refuses a replacement that drops an anchor, and retire refuses to explain a removal edit has not first performed -- confirmed empirically in round 1 (a probe edit) and reconfirmed this round (a direct retire attempt against this exact anchor, refused with remove the text with blue edit first)' -- matching blue's closing claim exactly, not merely asserting it. show evidence confirms the two replacement citations (c-776b0c91, c-a49be6fd) both carry independent 'supports'/high-confidence verifications from red-lens-r2-L1, and the underlying entproto disclaimer/direction claim is true. Red's own round-2 closing already conceded the substance is fixed and explicitly declined to make the risk-acceptance call on blue's behalf, inviting the bench to decide. Blue argued risk_accepted; I find routed_to_infrastructure the more precise disposition on these facts, because what is missing is not a judgment about whether a fix is worth its cost -- it is the fix itself, which does not exist in this seat's available verb set at any cost. The residual is trivial (an inert Bibliography footnote pointing to an oppositely-directioned but harmless page, disclosed in-line, sitting beside two correct citations that already fully back the sentence) and certain but harmless, so nothing further is owed from blue on the content side.
scope-limits: <reviewer: state what this assumed>
source: 2026-08-22_sqlite-schema-source, R1-1
